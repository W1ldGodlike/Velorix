Продолжай как будто пользователь написал «продолжай» / `+`.
Контракт marathon уже в `.cursor/rules/fluxalloy-marathon.mdc` — ничего дополнительно не создавать.

В конце итерации: `npm run agent:session -- bump` (счётчик; файл создаст скрипт).
Перед коммитом/push/re-anchor: `npm run agent:session` — смотреть `continue_count` (% 5 коммит, % 10 push + re-anchor OK).

Не перечитывай целиком agent-contract, ТЗ, весь чеклист/журнал без причины.
План: sprint TODO в IMPLEMENTATION_CHECKLIST.md. Одна сводная J-* (`journal:stamp`, [SDK]). Крупный срез. `check:quiet`. Отчёт 2–4 пункта.
