Продолжай по уже прочитанному `agent-contract.txt`; не перечитывай его целиком без причины.
Для контекста прочитай только:
1. `IMPLEMENTATION_CHECKLIST.md`: блок `## Ближайший TODO спринта` и связанный тематический §.
2. Последние 30–50 строк `IMPLEMENTATION_JOURNAL.md`: последний `J-NNN` и последние сделанные блоки.
3. `docs/UX_REFERENCE_V0.md` только если выбранная задача реально UI/layout/v0.

Продолжай крупным блоком по текущему спринту, не одной мелочью: реализация → `npm run check` → короткая запись в журнал при необходимости → один коммит.
ВАЖНО: timestamp для IMPLEMENTATION_JOURNAL.md не придумывать. Перед записью в журнал обязательно выполни `Get-Date -Format "yyyy-MM-dd HH:mm:ss"` (или эквивалент в текущей shell) и используй ровно это локальное время.
ВАЖНО: ты SDK-агент. Все новые записи в IMPLEMENTATION_JOURNAL.md должны быть формата `- [J-NNN] YYYY-MM-DD HH:mm:ss [SDK]: ...`, где `J-NNN` — следующий номер после последней записи. Не ставь `[Assistant]`.
ВАЖНО: после итерации держи `## Ближайший TODO спринта` в IMPLEMENTATION_CHECKLIST.md актуальным: 3-7 ближайших пунктов, не архив закрытых задач.

Не дублируй длинный отчёт — 3–5 пунктов достаточно. Если упёрся в блокер, явно напиши «БЛОКЕР» и что нужно для снятия.
