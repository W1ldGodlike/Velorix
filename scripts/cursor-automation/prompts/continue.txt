Продолжай как будто пользователь написал «продолжай» / `+`.
Контракт marathon уже в `.cursor/rules/fluxalloy-marathon.mdc` — ничего дополнительно не создавать.

Порядок в конце итерации: `check:quiet` → `journal:stamp` + одна J → `npm run agent:session -- bump` → **после** bump: если `continue_count % 5 === 0` и quiet зелёный — `git commit`; если `% 10 === 0` — push + re-anchor OK.

Не перечитывай целиком agent-contract, ТЗ, весь чеклист/журнал без явной причины в задаче.
План: sprint TODO в IMPLEMENTATION_CHECKLIST.md. Пакет полей (fluxalloy-iteration-batch.mdc). Одна J на весь ответ ([SDK]). Крупный срез (глоссарий). Реестр вместо копипасты. Отчёт 3–5 буллетов.
