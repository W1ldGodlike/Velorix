Продолжай как будто пользователь написал в чате `+` или «продолжай работать».
Используй контекст уже живого Agent instance, если он есть. В long-loop runner может пересоздавать Agent короткими сессиями для экономии Cache Read; в новой сессии опирайся на компактный handoff и точечные файлы, не пытайся восстановить старый conversation context.
Не перечитывай `agent-contract.txt`, `FLUXALLOY_TZ.md`, `docs/UX_REFERENCE_V0.md`, весь чеклист или весь журнал без причины.

Если ты помнишь текущий блок — продолжай его сразу. Читай файлы только точечно:
1. `IMPLEMENTATION_CHECKLIST.md` — только если не уверен, что брать дальше, или нужно обновить спринт.
2. `IMPLEMENTATION_JOURNAL.md` — только перед записью новой строки, чтобы взять последний `J-NNN`, или если есть риск дублирования после чужих изменений.
3. `docs/UX_REFERENCE_V0.md` — только если выбранная задача реально UI/layout/v0.

Работай максимумом за итерацию: если взял задачу и остаётся лимит, продолжай следующий близкий пункт того же блока; не останавливайся после одной мелочи. Финал: реализация → `npm run check:quiet` (полный `npm run check` только при необходимости/перед релизом) → короткая запись в журнал при необходимости → один коммит.
Оптимизация токенов обязательна: максимум полезного кода/прогресса за минимум чтений и вывода. Читай только нужные ranges/search, не печатай простыни, не тащи большие логи целиком.
ВАЖНО: timestamp для IMPLEMENTATION_JOURNAL.md не придумывать. Перед записью в журнал обязательно выполни `Get-Date -Format "yyyy-MM-dd HH:mm:ss"` (или эквивалент в текущей shell) и используй ровно это локальное время.
ВАЖНО: ты SDK-агент. Все новые записи в IMPLEMENTATION_JOURNAL.md должны быть формата `- [J-NNN] YYYY-MM-DD HH:mm:ss [SDK]: ...`, где `J-NNN` — следующий номер после последней записи. Не ставь `[Assistant]`.
ВАЖНО: после итерации держи `## Ближайший TODO спринта` в IMPLEMENTATION_CHECKLIST.md актуальным: 3-7 ближайших пунктов, не архив закрытых задач.

Отчёт максимально краткий: 2–4 пункта, без простыни. Если упёрся в блокер, явно напиши «БЛОКЕР» и что нужно для снятия.
