Продолжай или + — равнозначно. Задача №1: только активный ref (сейчас ref.1) до 1:1 vs PNG; порядок ref.1→…→27. Запрещено: ref.2+ до sign-off ref.1; ref.27/26 «сначала». Канон: `VELORIX_NEON_THEME.md`, sprint `IMPLEMENTATION_NEON_CHECKLIST.md`. Запрещено: `docs/archive/*`; git-restore без явной просьбы (`velorix-no-git-restore.mdc`). Skills: velorix-continue, journal-entry при diff.

Конец: `check:quiet` → при diff `journal:stamp` + одна J. Git по J: `NNN % 5` → commit, `NNN % 10` → push; **следующий commit по J** **J-1580** — `velorix-agent.mdc`. Контракт: `scripts/cursor-automation/prompts/agent-contract.txt`.
