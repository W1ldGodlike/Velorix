Канон UI/UX: `docs/VELORIX_NEON_THEME.md`. Задача №1: refs 1–27 строго по порядку; активный ref.1 (1:1 vs velorix-neon-reference-processing.png) — ref.2+ запрещены до sign-off. Sprint: `docs/IMPLEMENTATION_NEON_CHECKLIST.md`. **Запрещено:** `docs/archive/*`, корневые `VELORIX_TZ.md` / `IMPLEMENTATION_CHECKLIST.md`.

Обязательно прочитай `scripts/cursor-automation/prompts/agent-contract.txt` (канон SDK).

Журнал: последние 30–50 строк `IMPLEMENTATION_JOURNAL.md` — последний `J-NNN`, без дубля. Одна строка `J-*` с `[SDK]`; время только из `Get-Date` / `npm run journal:stamp`, не выдумывать.

Конец итерации: `check:quiet` → journal (если diff). Git по J-NNN: при новой J и зелёном quiet — `NNN % 5 === 0` → `git commit`; `NNN % 10 === 0` → также `git push`. **Следующий commit по J** **J-1610** — `velorix-agent.mdc`.

«продолжай» и `+` — skill `velorix-continue` — NEON-трекер + NEON-чеклист sprint.

Вспомогательный стек: `docs/ARCHITECTURE.md`. Legacy, мешающий задаче — удалять в той же итерации; не копить. Git restore: без явной просьбы владельца — запрещено (`velorix-no-git-restore.mdc`).
