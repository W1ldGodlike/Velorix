Ты работаешь в репозитории Velorix (Electron + TypeScript + React).

Задача №1: UI 1:1 PNG refs 1–27. Порядок: ref.1 → ref.2 → … → ref.27; активный ref.1 — к ref.2 не переходить до sign-off [x] ref.1 vs velorix-neon-reference-processing.png (шрифты, кнопки, glow, весь экран). Канон: `docs/VELORIX_NEON_THEME.md`, sprint `docs/IMPLEMENTATION_NEON_CHECKLIST.md` → `## Ближайший TODO спринта`. **Запрещено:** ref.27/26 «сначала», ref.2+, IPC вне активного ref; `docs/archive/*`, корневые `VELORIX_TZ.md` / `IMPLEMENTATION_CHECKLIST.md`.

Обязательно прочитай `scripts/cursor-automation/prompts/agent-contract.txt` (канон SDK; не перечитывай целиком на каждом «продолжай» / `+` без причины).

Журнал: последние 30–50 строк `IMPLEMENTATION_JOURNAL.md` — последний `J-NNN`, без дубля. Одна строка `J-*` с `[SDK]`; время только из `Get-Date` / `npm run journal:stamp`, не выдумывать.

Конец итерации: `check:quiet` → journal (если diff). Git по J-NNN: при новой J и зелёном quiet — `NNN % 5 === 0` → `git commit`; `NNN % 10 === 0` → также `git push`. **Следующий commit по J** **J-1610** — `velorix-agent.mdc`.

«продолжай» и `+` — равнозначные триггеры: skill `velorix-continue` — NEON-трекер + NEON-чеклист sprint.

Вспомогательный стек: таблица в `docs/ARCHITECTURE.md` при первом внедрении инструмента вне Electron/React/TS/ffmpeg/yt-dlp.

Legacy: если мешает задаче/NEON — удалить или заменить в той же итерации; не оставлять «на всякий случай»; check:quiet.

Git restore (блокер): без явной просьбы владельца в чате — запрещено возвращать удалённый/старый код из git (checkout <rev> --, restore --source=, git show → файл, revert). Rebuild — новые файлы по PNG/THEME. Канон: .cursor/rules/velorix-no-git-restore.mdc.
