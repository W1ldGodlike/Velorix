/** Labels for `emitDownloadsTopbarClusterHtml` (legacy HTML emit tests / icon smoke). */
export type DownloadsTopbarClusterCopy = {
  toolbarAria: string
  inspector: string
  focusUrl: string
  mainEditor: string
  enginePaths: string
  about: string
}

export const DOWNLOADS_TOPBAR_CLUSTER_COPY_RU: DownloadsTopbarClusterCopy = {
  toolbarAria: 'Действия',
  inspector: 'Инспектор ffprobe §9',
  focusUrl: 'К полю URL (лента ввода §6)',
  mainEditor: 'Главное окно (редактор)',
  enginePaths: 'Пути к движкам ffmpeg / yt-dlp / ffprobe',
  about: 'О программе'
}
