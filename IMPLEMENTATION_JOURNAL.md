# FluxAlloy — журнал решений и проверок

Хроника инфраструктурных решений, проверок окружения и заметок, которые не должны раздувать основной **[чек‑лист](IMPLEMENTATION_CHECKLIST.md)**.

**ТЗ [`FLUXALLOY_TZ.md`](FLUXALLOY_TZ.md)** по-прежнему не трогаем без явной договорённости.

## Правило записей

Формат метки времени: **`YYYY-MM-DD HH:mm:ss`** (до секунд).
Формат источника: **`[Assistant]`** — обычный Cursor-чат; **`[SDK]`** — агент из `scripts/cursor-automation`.

Нумерация записей в разделе **«Записи»** ниже: префикс **`[J-NNN]`** сразу после маркера списка (`- [J-042] 2026-…`). Номера идут подряд по мере добавления строк в журнал; **перед дописыванием смотреть последнюю строку раздела и взять следующий номер** (например, после **J-255** идёт **J-256**). При вставке нескольких записей за раз нумеровать по возрастанию без пропусков и без повторов. После **J-999** допускается **J-1000** и четырёхзначные номера.

---

## Записи

- [J-001] 2026-05-09 17:02:00 [Assistant]: повторно проверено окружение перед разработкой. Node/npm/Git доступны через установленный Node.js/Git; `npm install`, `npm run check`, `npm run build`, `npm audit --audit-level=moderate` проходят, уязвимостей `0`.
- [J-002] 2026-05-09 17:02:00 [Assistant]: `dist/`, `out/`, `.eslintcache` — только сгенерированные артефакты проверок/сборки; удалены из рабочей папки после аудита, остаются в `.gitignore`.
- [J-003] 2026-05-09 17:02:00 [Assistant]: `package-lock.json` создан `npm install`; его нужно хранить в Git для воспроизводимой установки, поэтому он больше не игнорируется.
- [J-004] 2026-05-09 17:11:00 [Assistant]: добавлены архитектурные комментарии в `main`, `preload`, `settings-store` и renderer bootstrap. Дальше комментировать чуть подробнее обычного: объяснять назначение модулей, границы IPC, причины проверок/ограничений и неочевидные решения; не пересказывать только совсем очевидный синтаксис.
- [J-005] 2026-05-09 17:17:00 [Assistant]: начат §3. Добавлены `app-paths` и `engine-service`: dev/prod пути ресурсов, bundled/user `bin`, безопасная проверка `ffmpeg`/`ffprobe`/`yt-dlp` через `execFile(..., ['--version'])`, IPC `fluxalloy:engines-status` и краткий статус внизу окна.
- [J-006] 2026-05-09 17:21:00 [Assistant]: выполнен полный проход по комментариям в исходниках и конфигурации. Подробные пояснения добавлены в main/preload/renderer/CSS/build/lint/gitignore/editorconfig; JSON/lockfile не комментируются, потому что формат не поддерживает комментарии.
- [J-007] 2026-05-09 17:26:00 [Assistant]: включён Windows Developer Mode и выдано право `SeCreateSymbolicLinkPrivilege` пользователю `truno`; для текущего процесса право появится после нового входа в Windows/перезапуска сессии.
- [J-008] 2026-05-09 17:28:00 [Assistant]: разрешено оставлять короткие `TODO(...)` прямо в файлах проекта, если уже понятно, что модуль надо доработать позже. Формат: кратко, с привязкой к разделу ТЗ, без длинных планов вместо чек-листа.
- [J-009] 2026-05-09 17:28:30 [Assistant]: правило на будущее — в этом журнале писать дату и время до секунд в формате `YYYY-MM-DD HH:mm:ss`.
- [J-010] 2026-05-09 17:44:44 [Assistant]: после перезагрузки ПК проверено окружение без ручного PATH: `node` v22.22.0, `npm` 11.12.1, `git` 2.54; `whoami /priv` показывает `SeCreateSymbolicLinkPrivilege` (в колонке состояния для этой оболочки — `Disabled`, это нормально для токена до явного enable в процессе).
- [J-011] 2026-05-09 17:44:44 [Assistant]: убран временный `win.signAndEditExecutable: false` из `electron-builder.yml`; `npm run build:unpack` проходит: winCodeSign распаковывается, выполняется `signing with signtool.exe` для `FluxAlloy.exe`.
- [J-012] 2026-05-09 17:46:19 [Assistant]: Git не делал коммит без автора (`Author identity unknown`). Для этого репозитория локально выставлено `git config user.name truno` и `git config user.email truno@local.fluxalloy` (без `--global`). Перед пушем на GitHub лучше заменить почту на свою: `git config user.email "ты@example.com"` (при необходимости поправить и `user.name`).
- [J-013] 2026-05-09 17:54:43 [Assistant]: крупный шаг §3 + §4.B + база §7. Добавлены `fluxmedia`-протокол с allowlist, IPC открытия файла (`dialog`/`grantPath`), DnD через `webUtils.getPathForFile`, `<video>` в превью, прогресс IPC загрузки движков, `extract-zip`, `trusted-hashes-store` и расширенный формат `Data/trusted_hashes.json` (`schema`, `windows-x64`). Отдельное окно первого запуска и macOS/Linux-загрузчики пока не делались — не запускать `prettier .` по репозиторию без исключений: случайно трогает `FLUXALLOY_TZ.md`.
- [J-014] 2026-05-09 20:10:44 [Assistant]: раздел «Журнал решений и проверок» вынесен из `IMPLEMENTATION_CHECKLIST.md` в этот файл (`IMPLEMENTATION_JOURNAL.md`): чек-лист остаётся про статусы по § и спринту, хроника — отдельным потоком.
- [J-015] 2026-05-09 20:13:15 [Assistant]: проверка репозитория на мусор: `dist/`, `out/`, `node_modules` в рабочей копии нет, в Git не трекаются (как и должно). Удалены неиспользуемые заготовки `src/renderer/src/assets/electron.svg` и `wavy-lines.svg` (не импортировались). В `.gitignore` добавлены `Thumbs.db`, `Desktop.ini`, `.idea`, `*.swp`.
- [J-016] 2026-05-09 20:20:25 [Assistant]: связка §7 + §4.1 — `applyTheme` больше не затирает `cachedSettings` целиком; `ffprobe-service` + IPC `fluxalloy:media-probe` (только `isGrantedMediaPath`); таймлайн под `<video>`; `lastOpenedSourcePath` и restore при старте; второе окно `downloads-window` (data HTML-заглушка); поле URL и буфер в UI; сохранение последнего файла после диалога/меню/`grantPath`.
- [J-017] 2026-05-09 20:28:02 [Assistant]: окно yt-dlp — очередь URL в main (`downloads-queue`), второй preload `downloadsWindow.mjs`, меню Ctrl+Shift+V, глобальная вставка ссылки в главном окне; статусбар — первая строка `--version` по каждому движку; `resolvePreloadOutFile` для `.mjs` после electron-vite 5.
- [J-018] 2026-05-09 20:38:48 [Assistant]: добавлен изолированный `scripts/cursor-automation` (`@cursor/sdk`, `npm run loop` / `once`, файлы `prompts/`, стоп-файл `STOP`); root eslint игнорирует эту папку (`node_modules` внутри не линтим).
- [J-019] 2026-05-09 20:42:28 [Assistant]: окно yt-dlp — реальный spawn yt-dlp (`ytdlp-download-service`, без shell), очередь последовательная, колонки прогресс/имя, IPC-снимок очереди без reload страницы; вывод в `userData/downloads/ytdlp`; отмена через kill дочернего процесса.
- [J-020] 2026-05-09 21:05:36 [SDK]: §3 — `engineExecutablePaths` в `settings.json`, приоритет резолва override → bundled → user bin; IPC выбора exe и сохранения; минимальный UI (кнопка + модалка) и пункт меню «Настройки → Пути к движкам»; снапшот для `downloads-queue-runner`, ffprobe и статуса движков.
- [J-021] 2026-05-09 21:06:43 [SDK]: §6.1 — `startDownloadSingleRow`, IPC `fluxalloy-downloads-start-row`, кнопка «▶» для строк «Ожидание»; общий флаг занятости с «Старт очереди».
- [J-022] 2026-05-09 21:08:21 [SDK]: §7.1 — маркеры In/Out у превью: индикатор диапазона, кнопки «In/Out здесь» и «Весь клип», синхронизация границ при появлении duration; смена файла — remount `VideoTimeline` по `key`.
- [J-023] 2026-05-09 21:12:26 [SDK]: §7.1 — базовый экспорт в MP4 (`ffmpeg-export-service`: spawn + массив аргументов, libx264/aac); диалог сохранения, маркеры через `-ss`/`-t`; IPC `export-start`/`export-cancel`/`export-progress` и строка прогресса в статусбаре.
- [J-024] 2026-05-09 21:14:45 [SDK]: §4.1 — `windowBounds.main/downloads` в `settings.json`, debounced persist при resize/move и flush на `close`; восстановление через `rectifyBoundsForRestore` (fallback на центр основного монитора).
- [J-025] 2026-05-09 21:19:17 [SDK]: §9 — расширен `ffprobe-service` (битрейт, `tracks`, tooltip формата) и UI под превью; §19 — `bin/.gitkeep` + `extraResources` для пустого `resources/bin` в сборке.
- [J-026] 2026-05-09 21:20:42 [SDK]: §4.5 — меню «Справка → О программе», IPC `app-about-info`, модалка; §9 — `rawJson` в probe, блок JSON + «Копировать», IPC `clipboard-write-text` (лимит 24 MiB).
- [J-027] 2026-05-09 21:22:11 [SDK]: §6.4 — `downloads-log-ipc.ts`, стрим строк yt-dlp в окно загрузок (`onLog`, авто-раскрытие `<details>`, лимит буфера в UI).
- [J-028] 2026-05-09 21:23:40 [SDK]: §7.6 — `ffmpeg-frame-snapshot-service`, IPC `snapshot-frame`, кнопка «Кадр»; §4.2 — диалог закрытия главного окна при экспорте или активной загрузке yt-dlp.
- [J-029] 2026-05-09 21:26:24 [SDK]: §6.2 — настраиваемый каталог yt-dlp (`ytdlp-download-output.ts`, поле `ytdlpDownloadDirectory` в settings, UI «Выбрать…»/«По умолчанию», IPC в окне загрузок).
- [J-030] 2026-05-09 21:38:31 [SDK]: §6.1/§6.4 — парсинг stderr yt-dlp: колонка «Прогресс» — процент, скорость и ETA (`parseYtdlpDownloadProgressLine`, `formatYtdlpProgressCell`); разметка ячейки в окне загрузок под длинные строки.
- [J-031] 2026-05-09 21:41:13 [SDK]: §6.2 — сохраняемый шаблон `-o` (`ytdlpFilenameTemplate`) и пресеты `-f` (`ytdlpFormatPreset`); IPC/preload `getCliOptions`/`setCliOptions`; `resolveSafeYtdlpOutputPattern`; снимок опций для runner (`ytdlp-run-options-sync`).
- [J-032] 2026-05-09 21:42:07 [SDK]: §7.1 — прогресс экспорта ffmpeg: парсинг `speed=`, склейка stderr по `\r`/`\n`; статусбар показывает множитель после процента (`FfmpegExportProgressPayload.speed`).
- [J-033] 2026-05-09 21:43:43 [SDK]: §7.2 — три системных пресета libx264 (`balance`/`smaller`/`quality`), `ffmpegExportEncodePreset` в settings, `<select>` в тулбаре, IPC `settings-set-ffmpeg-export-encode-preset`, передача в `runFfmpegExportJob`.
- [J-034] 2026-05-09 21:45:14 [SDK]: §6.2 — плейлист (`ytdlpDownloadPlaylist` → `--yes-playlist`/`--no-playlist`) и только аудио (`ytdlpAudioOnly` → `-x --audio-format best`, без `-f` пресета); чекбоксы в окне загрузок, расширен снимок `YtdlpRunOptionsSnapshot` и `runYtdlpOnce`.
- [J-035] 2026-05-09 21:48:01 [SDK]: §6.3 — `ytdlpExtraArgsLine` + `parseExtraYtdlpArgsLine`/`buildYtdlpSpawnArgvTokens`, превью `yt-dlp …` в окне загрузок; блок конфликта `-o`/batch/`@`; раннер берёт `extraArgs` из снимка.
- [J-036] 2026-05-09 21:49:21 [SDK]: §6.3 — подсказки из `Data/ytdlp_commands.json`: `getYtdlpCommandHints` в main, `commandHints` в payload окна загрузок, сворачиваемый справочник и вставка токена в поле доп. argv с кратким описанием выбранного флага.
- [J-037] 2026-05-09 21:51:30 [SDK]: §6.2 — пресет субтитров (`ytdlpSubtitlePreset`/`ytdlpSubLangs` в settings), argv через `buildYtdlpSpawnArgvTokens`, UI в окне загрузок и раннер очереди.
- [J-038] 2026-05-09 21:54:00 [SDK]: §6.2 — cookies: `ytdlpCookiesFile`/`ytdlpCookiesBrowser`, диалог выбора файла в окне загрузок, приоритет файла над браузером; запрет дублей `--cookies*` в доп. argv.
- [J-039] 2026-05-09 21:55:39 [SDK]: §6.2 — `--impersonate`: whitelist chrome/edge/firefox (`ytdlpImpersonate`), сбор argv и окно загрузок; запрет дубля `--impersonate` в доп. argv.
- [J-040] 2026-05-09 21:59:33 [Assistant]: ревью SDK-изменений: `npm run check` и `npm run build` проходят; найден и закрыт риск в доп. аргументах yt-dlp — запрещены runtime-флаги вроде `--exec`, внешние downloader/postprocessor/config/plugin пути и `-P/--paths`.
- [J-041] 2026-05-09 22:12:07 [Assistant]: follow-up после ревью SDK: прогресс yt-dlp теперь парсится из stdout и stderr через общий обработчик; ручные пути движков сохраняются только как существующие абсолютные файлы; главное окно получило `minWidth/minHeight`, таймер bounds-персиста локализован на окно.
- [J-042] 2026-05-09 22:21:20 [Assistant]: §6.2 — добавлены безопасные настройки yt-dlp для `--limit-rate` и `--retries`: поля UI в окне загрузок, persist в `settings.json`, включение в preview argv и spawn; дубли `--limit-rate`/`-r`/`--retries` в доп. argv запрещены.
- [J-043] 2026-05-09 22:29:58 [Assistant]: §6.2/§17 — в окне yt-dlp добавлена кнопка «Открыть» для текущего каталога загрузок через guarded IPC и `shell.openPath`; «Ближайший TODO спринта» сжат до актуальных 6 ориентиров.
- [J-044] 2026-05-09 22:34:15 [SDK]: §17/§18 — добавлен `diagnostics-paths` (whitelist userData/logs/ytdlp/bin/resources), меню «Инструменты → Открыть папку…», IPC `fluxalloy:diagnostics-list-folders`/`open-folder` и preload `fluxalloy.diagnostics`; каталог `userData/logs` гарантированно создаётся при первом обращении как заготовка под §18.
- [J-045] 2026-05-09 22:39:44 [SDK]: §6.4 — профили повтора строки очереди (`ytdlpQueueRetryProfile` + UI), паузы с отменой по тому же AbortSignal; `extractYtdlpErrorSummary` для статуса; раннер делает до 3 запусков на URL при ненулевом exit.
- [J-046] 2026-05-09 22:43:44 [SDK]: §18 — добавлен `logger-service` (`userData/logs/main.log`, scope/level/timestamp, ротация 1 MiB → `main.log.1`, `uncaughtException`/`unhandledRejection`); IPC `fluxalloy:log-renderer` + `window.fluxalloy.log.send`; renderer-перехват `error`/`unhandledrejection`; `console.error` в раннере очереди заменён на `logError`.
- [J-047] 2026-05-09 22:50:11 [SDK]: §21 — подключён Vitest (+`tsconfig.tests.json`, `npm run test`/`test:watch`); чистые парсеры yt-dlp вынесены в `ytdlp-progress-parser.ts`; 62 теста для `ytdlp-extra-args`/`ytdlp-progress-parser`/`ytdlp-queue-retry`; найден и исправлен обход whitelist `-P` в доп. argv yt-dlp (toLowerCase ломал сравнение).
- [J-048] 2026-05-09 22:55:59 [SDK]: §6.4 — `ytdlp-download-history.ts` + запись из раннера после фактического `runYtdlpOnce`; IPC `fluxalloy-downloads-get/clear-history`, preload, сворачиваемая таблица в окне загрузок; тесты `outcomeFromQueueStatus`.
- [J-049] 2026-05-09 23:12:19 [Assistant]: ревью SDK-коммитов `c604794..2ce01e1`: исправлены метки журнала на `[SDK]`, спринт-блок снова приведён к 6 пунктам; история yt-dlp теперь ставит флаг записи через `runYtdlpOnce.onStarted`, то есть только после создания child process, а не до валидационных ошибок.
- [J-050] 2026-05-09 23:23:14 [Assistant]: §6.3/§21 — закрыт обход blacklist через glued short-options yt-dlp (`-Ptmp`, `-a/list`, `-oX`, `-r5M`) и добавлены Vitest-кейсы для этих форм.
- [J-051] 2026-05-09 23:26:16 [Assistant]: §18 — renderer log IPC закреплён за main window, получил token bucket rate limit и очистку control chars в `scope/message`; `attachProcessErrorHandlers()` перенесён на ранний старт main до `app.whenReady`.
- [J-052] 2026-05-09 23:28:03 [Assistant]: §6.4/§21 — запись и очистка `downloads/history.json` переведены на атомарный temp+rename; тесты истории расширены сценариями append/read newest-first и clear с валидной схемой.
- [J-053] 2026-05-09 23:31:11 [Assistant]: §17 — меню «Инструменты → Открыть папку…» пересобирается при фокусе окна и после изменения диагностически важных путей (`ytdlpDownloads`, engine bin), чтобы `enabled` отражал актуальное существование каталогов.
- [J-054] 2026-05-09 23:33:23 [Assistant]: §21 — добавлены unit-тесты `ytdlp-download-options`: шаблон имени/безопасный output pattern, нормализация `--limit-rate`, границы `--retries`; Electron-зависимость справочника yt-dlp замокана в тесте.
- [J-055] 2026-05-09 23:38:26 [Assistant]: §6.4 — в таблицу истории загрузок добавлено действие «В очередь»: URL из завершённой записи можно повторно добавить в очередь без автозапуска скачивания.
- [J-056] 2026-05-09 23:39:38 [Assistant]: §6.4 — история загрузок получила локальный фильтр по исходу (`Все`/`Успех`/`Ошибка`/`Отмена`), без изменения формата `history.json` и IPC.
- [J-057] 2026-05-09 23:43:40 [Assistant]: §6.2/§6.4 — добавлено отдельное поле `--fragment-retries` (settings/UI/IPC/snapshot/spawn/preview), запрет дубля в доп. argv оставлен; тесты yt-dlp аргументов и опций расширены.
- [J-058] 2026-05-09 23:45:44 [Assistant]: §6.4 — в окне загрузок добавлено сохранение видимого stdout/stderr лога yt-dlp через guarded IPC + save dialog; renderer передаёт только текущий обрезанный буфер.
- [J-059] 2026-05-09 23:49:11 [Assistant]: §6.4 — для завершённых/ошибочных/отменённых строк очереди добавлено действие retry `↻`: строка сбрасывается в `Ожидание` и запускается повторно как одиночная загрузка.
- [J-060] 2026-05-09 23:54:37 [Assistant]: §6.1/§6.3/§21 — экспертные аргументы yt-dlp свернуты в отдельный `details`; очередь получила фильтр по статусу и очистку завершённых строк; добавлен unit-тест `downloads-queue` на cleanup.
- [J-061] 2026-05-09 23:56:27 [Assistant]: §6.1 — над таблицей очереди добавлена живая сводка статусов (всего/ожидание/в работе/готово/ошибки/отмена), обновляется вместе со snapshot.
- [J-062] 2026-05-09 23:57:53 [Assistant]: §21 — добавлены unit-тесты `settings-store` для persisted yt-dlp полей (`ytdlpRateLimit`, `ytdlpRetries`, `ytdlpFragmentRetries`, queue retry profile) и сохранения settings.json.
- [J-063] 2026-05-09 23:59:33 [Assistant]: §7/§21 — pure helpers ffmpeg export (`parseFfmpegTimeSeconds`, `parseFfmpegSpeedToken`) экспортированы и покрыты unit-тестами вместе с encode preset/segment duration.
- [J-064] 2026-05-10 00:05:53 [Assistant]: §6.1/§6.4 — очередь и история yt-dlp сохраняют best-effort `outputPath` из stdout/stderr (`Destination`, merge, already downloaded); добавлены guarded IPC-действия открыть файл/показать в папке и тесты парсера/модели.
- [J-065] 2026-05-10 00:10:59 [Assistant]: §18/§21 — добавлены действия диагностики `Открыть main.log` и `Собрать Support ZIP`, диалог необработанной ошибки с копированием деталей/логом/архивом; ZIP builder покрыт unit-тестами.
- [J-066] 2026-05-10 00:18:13 [Assistant]: §18/§21 — stdout/stderr внешних процессов (`yt-dlp`, `ffmpeg`, `ffprobe`) пишутся в `main.log` через sanitizer без полного argv; на старте добавлен prune старых crash dumps с сохранением последних 20.
- [J-067] 2026-05-10 00:28:22 [Assistant]: §6.4/§7/§21 — из очереди и истории yt-dlp добавлено действие «В обработчик» для открытия готового файла в главном preview; экспорт получил фильтры MP4/MKV/MOV и нормализацию расширения.
- [J-068] 2026-05-10 00:32:53 [Assistant]: §7.2/§21 — контейнер экспорта MP4/MKV/MOV вынесен в toolbar и persisted settings; export-start возвращает путь результата, UI показывает имя сохранённого файла.
- [J-069] 2026-05-10 00:36:32 [Assistant]: §7.2/§21 — добавлены persisted настройки CRF libx264 и AAC bitrate в toolbar/settings/preload; ffmpeg export принимает валидированные overrides без shell-строк.
- [J-070] 2026-05-10 00:40:30 [Assistant]: §7.2/§21 — добавлены persisted настройки FPS и scale preset (исходный/480p/720p/1080p) для ffmpeg export; фильтры собираются через `-vf` без shell-строк, тесты settings/export helpers расширены.
- [J-071] 2026-05-10 00:44:44 [Assistant]: §7.2/§21 — добавлен persisted video bitrate mode для экспорта: при выборе bitrate ffmpeg получает `-b:v`, без него остаётся CRF mode; preload/settings/UI и unit-тесты расширены.
- [J-072] 2026-05-10 00:47:47 [Assistant]: §7/§21 — после успешного экспорта main запоминает результат в allowlist и renderer показывает действия «Файл экспорта»/«Папка экспорта»; IPC не открывает произвольные пути.
- [J-073] 2026-05-10 00:50:13 [Assistant]: §7/§21 — результат экспорта можно вернуть в preview через кнопку «В превью»; main использует тот же allowlist export output и существующий `grantMediaPath`.
- [J-074] 2026-05-10 00:52:27 [Assistant]: §7/§21 — toolbar получил отмену активного ffmpeg export через существующий `export.cancel`; отмена возвращается как `cancelled`, а для последнего результата добавлено копирование пути.
- [J-075] 2026-05-10 00:55:11 [Assistant]: §7/§21 — успешный ffmpeg export теперь запоминает последнюю папку результата в settings; следующий save dialog использует её как defaultPath, если каталог ещё существует.
- [J-076] 2026-05-10 00:57:45 [Assistant]: §7.6/§21 — снимок кадра теперь возвращает путь результата, запоминает последнюю папку кадра и даёт toolbar-действия открыть файл/папку/скопировать путь.
- [J-077] 2026-05-10 01:01:46 [Assistant]: §7.2/§21 — экспорт получил persisted audio mode `AAC`/`Без аудио`; при `none` ffmpeg получает `-an`, а AAC bitrate в toolbar отключается.
- [J-078] 2026-05-10 01:10:11 [Assistant]: §7.6/§21 — снимок кадра получил persisted формат `PNG`/`JPEG` в toolbar; save dialog стартует с нужным расширением, путь без расширения нормализуется helper-ом, settings/snapshot helpers покрыты тестами.
- [J-079] 2026-05-10 01:16:21 [SDK]: §21 — реестр IPC `src/shared/ipc-channels.ts`, main/preload на общих константах, Vitest на уникальность строк каналов.
- [J-080] 2026-05-10 01:18:24 [SDK]: §21 — `src/shared/ffmpeg-export-contract.ts`: типы экспорта ffmpeg для preload/main, сервис реэкспортирует из shared.
- [J-081] 2026-05-10 01:22:21 [SDK]: §21 — shared-контракты yt-dlp окна/лога/истории, диагностики, прогресса движков, формата снимка; main реэкспортирует, preload без импортов типов из main по этим доменам.
- [J-082] 2026-05-10 01:25:00 [SDK]: §21 — `ffprobe-contract`, `settings-contract`, `engine-contract`, `about-contract`, `preview-dialog-contract`; main реэкспортирует, главный preload импортирует типы только из `src/shared`.
- [J-083] 2026-05-10 01:26:59 [SDK]: §21 — `noImplicitAny` в tsconfig node/web/tests; константа `ENGINE_IDS` в `engine-contract`, main/renderer на общем списке; Vitest `tests/shared/engine-contract`.
- [J-084] 2026-05-10 01:28:36 [SDK]: §21 — `noUncheckedIndexedAccess` в tsconfig node/web/tests; точечные правки main и Vitest под безопасный доступ по индексу.
- [J-085] 2026-05-10 01:30:06 [SDK]: §21 — `exactOptionalPropertyTypes` в tsconfig node/web/tests; экспорт IPC передаёт `trim` только если задан (main/renderer).
- [J-086] 2026-05-10 01:32:06 [SDK]: §21 — `noPropertyAccessFromIndexSignature`; bracket-доступ для `Record<string, unknown>` в main и для `dataset`/версий в renderer.
- [J-087] 2026-05-10 01:33:13 [SDK]: §21 — явный `useUnknownInCatchVariables` в tsconfig node/web/tests (политика catch уже совместима; без изменений в логике).
- [J-088] 2026-05-10 01:35:57 [SDK]: §6.4 — статус неуспеха очереди: `formatYtdlpQueueFailureStatus` (сигнал ОС, stderr fallback); Vitest.
- [J-089] 2026-05-10 02:08:37 [SDK]: §6.4 — «Скачать и сразу обработать»: `ytdlpOpenInHandlerOnComplete` в settings, чекбокс в окне загрузок, после успеха yt-dlp — авто-открытие в preview через общий guard пути; тесты `resolveAllowedYtdlpDownloadOutputFile` и snapshot.
- [J-090] 2026-05-10 02:11:06 [SDK]: §6.3 — справочник yt-dlp: карта категорий токенов в main, сортировка списка, `<optgroup>` в окне загрузок; опциональный `category` в JSON переопределяет категорию; Vitest `sortYtdlpCommandHintsForUi`.
- [J-091] 2026-05-10 02:15:15 [SDK]: §6.3 — превью argv yt-dlp: контекст с реальным каталогом `-o`, первый URL из очереди или `example.com`; debounce обновления превью при снимке очереди; Vitest `sanitizeYtdlpPreviewUrl`/`payloadFromSnapshot`.
- [J-092] 2026-05-10 02:16:39 [SDK]: §6.4 — профиль повтора очереди `persistent`: 3 дополнительных запуска с паузами 5/15/45 с; типы settings/контракт/UI/select; Vitest `ytdlp-queue-retry`.
- [J-093] 2026-05-10 02:18:06 [SDK]: §6.4 — `shouldSkipYtdlpQueueRetriesAfterFailure`: при маркерах stderr не делать дальнейшие повторы очереди; Vitest; лог `[FluxAlloy]` в раннере.
- [J-094] 2026-05-10 02:21:04 [SDK]: §6.4 — пауза/продолжить yt-dlp: SIGSTOP/SIGCONT в сервисе, IPC + кнопка в окне загрузок; Windows отключено; `ytdlp-os-pause-support` + Vitest.
- [J-095] 2026-05-10 02:22:44 [SDK]: §6.4 — `classifyYtdlpQueueFailureKind`, транзиентные маркеры с приоритетом над «не повторять»; парсинг `fragment X of Y` в прогресс; реэкспорт из `ytdlp-download-service`.
- [J-096] 2026-05-10 02:24:10 [SDK]: §6.4 — статус ошибки строки очереди: суффиксы «вероятно сеть» / «отказ источника» через `formatYtdlpQueueFailureStatus(..., failureKind)`; раннер передаёт `classifyYtdlpQueueFailureKind`; Vitest.
- [J-097] 2026-05-10 02:26:20 [SDK]: §6.4 — коды выхода yt-dlp 2/100/101 в `classifyYtdlpQueueFailureKind`; `shouldSkipQueueRetriesForFailureKind`; суффиксы статуса; раннер передаёт exitCode в классификацию.
- [J-098] 2026-05-10 02:27:17 [SDK]: §6.4 — парсер прогресса: `Total progress:`, `Downloading video|item X of Y` (плейлист); Vitest.
- [J-099] 2026-05-10 02:33:05 [Assistant]: ревью SDK-изменений после `9d0a36f`: метки журнала после 01:10:11 корректно `[SDK]`, `npm run check` проходит; исправлен пропуск persisted `ytdlpOpenInHandlerOnComplete` в `loadSettings`, чтобы флаг авто-открытия yt-dlp в обработчике переживал рестарт.
- [J-100] 2026-05-10 02:37:22 [Assistant]: follow-up §6.4 после ревью: авто-открытие yt-dlp в обработчике теперь пишет причину пропуска/ошибки в лог строки; `Total progress` имеет приоритет над fragment-строкой в парсере прогресса; чек-лист уточнён по POSIX-паузе.
- [J-101] 2026-05-10 02:42:24 [Assistant]: follow-up §21 после ревью shared IPC: `restoreLastSource` получил честный shared-тип `RestoredSourceInfo`, renderer импортирует ffmpeg/snapshot unions из `src/shared`, убран shadowing `d` в downloads IPC, чек-лист тестов синхронизирован.
- [J-102] 2026-05-10 02:46:24 [Assistant]: аудит чек-листа по вопросу пользователя: исправлены устаревшие статусы/формулировки про отсутствие ffmpeg, «заглушку» yt-dlp окна, живой yt-dlp, сохранение папок, системные пресеты и частично вынесенные shared-модели.
- [J-103] 2026-05-10 02:59:02 [SDK]: §7.2/§21 — `src/shared/ffmpeg-export-argv.ts` (pure `buildFfmpegExportArgv`/`buildFfmpegExportPreviewCommand`/format helpers) с реэкспортом из main; live preview команды ffmpeg в App.tsx (collapsible `<details>` + копирование) и Vitest на shared.
- [J-104] 2026-05-10 03:05:51 [SDK]: §7.2/§21 — `shouldApplyFfmpegExportTrim` вынесен в shared и переиспользован в main-сервисе; preview команды ffmpeg в App.tsx подмешивает In/Out маркеры и `probeDurationSec` (повторяет логику spawn), показывает `-ss/-t` или подсказку о почти полном клипе; Vitest расширен на shared trim-логику.
- [J-105] 2026-05-10 03:25:34 [SDK]: §6.3 — превью argv yt-dlp: IPC `getCliOptions` принимает опциональный override каталога `-o` и черновик полей (`draft`) без записи settings; при снимке очереди обновляется только превью/предупреждение парсера, форма не сбрасывается; `mergeYtdlpDownloadCliPatchOntoSettings` + тесты нормализации/override.
- [J-106] 2026-05-10 03:27:12 [SDK]: §6.4 — `ytdlp-progress-parser`: прогресс «N of M videos», транзиентные маркеры (500/504, broken pipe, read timed out…), пути из строк Writing thumbnail/subtitles; расширены Vitest.
- [J-107] 2026-05-10 03:28:44 [SDK]: §6.4 — `extractYtdlpOutputPath`: `[ffmpeg] Destination`/merge, `[EmbedSubtitle] Embedding…`, `[Metadata] Writing metadata to`; tests Vitest.
- [J-108] 2026-05-10 03:31:03 [SDK]: §18 — сессионный `logs/session.log` (дубликат строк с main), архив прошлого запуска в `logs/sessions/session-*.log` + prune; Support ZIP и меню «Открыть session.log».
- [J-109] 2026-05-10 03:32:59 [SDK]: §7.2/§21 — `buildFfmpegExportArgv` учитывает контейнер (MKV без `-movflags +faststart`, MP4/MOV — с faststart); preview и `runFfmpegExportJob` пробрасывают `container`; Vitest на mkv/mp4 хвост argv и preview.
- [J-110] 2026-05-10 03:34:52 [SDK]: §6.4 — `extractYtdlpOutputPath`: строки FFmpeg PP (`…; Destination:`, общий `[PP] Destination:`), EmbedThumbnail (`exe: Adding thumbnail to`), `Adding metadata to`, Concat Moving/to и Concatenate…Destination; Vitest.
- [J-111] 2026-05-10 03:36:04 [SDK]: §6.4 — прогресс: `(frag N/M)` без `%`, Sleeping/Waiting for reconnect; маркеры retry: гео/bad gateway/service unavailable/connection lost; Vitest.
- [J-112] 2026-05-10 03:39:59 [SDK]: §7.2/§21 — пользовательские пресеты экспорта: типы/snapshot в shared, `ffmpegExportUserPresets` в settings + парсер списка; IPC set list / apply snapshot; тулбар App.tsx (+ превью ffmpeg); Vitest merge/snapshot/list.
- [J-113] 2026-05-10 03:41:34 [SDK]: §7.1 — `runFfmpegExportJob`: фильтр строк для IPC прогресса (`isFfmpegExportProgressStatusLine`), сброс хвоста stderr на `close`; Vitest.
- [J-114] 2026-05-10 03:42:42 [SDK]: §7.2 — пользовательские пресеты: кнопки «Имя…» (переименование) и «Обновить пресет» (перезапись снимка из тулбара в выбранный слот).
- [J-115] 2026-05-10 10:55:39 [SDK]: §4.5/§17/§18 — «О программе»: папка логов, main.log, Support ZIP через IPC; результат диалога ZIP различает отмену и ошибку.
- [J-116] 2026-05-10 10:57:13 [SDK]: §6.4 — прогресс «Waiting for …», маркеры transient (408/premature close) и skip live/premiere; `extractYtdlpOutputPath`: VideoRemuxer `into`, `→ Destination`; Vitest.
- [J-117] 2026-05-10 10:58:31 [SDK]: §6.4 — `extractYtdlpOutputPath`: ASCII `-> Destination`, SubtitlesConvertor `->`/`into`; классификация: transient signature/rate limit, skip DRM protected; Vitest.
- [J-118] 2026-05-10 10:59:54 [SDK]: §6.3 — окно yt-dlp: превью argv после выбора/сброса каталога загрузки, вставки флага из справочника, «Шаблон по умолчанию» (`schedulePreviewRefresh`).
- [J-119] 2026-05-10 11:01:25 [SDK]: §6.4 — прогресс `Resuming download at byte…`; skip повторов: нет форматов / unsupported URL / requested format…; `extractYtdlpOutputPath`: `[download] Writing metadata to:`; Vitest.
- [J-120] 2026-05-10 11:02:42 [SDK]: §6.1 — окно yt-dlp: DnD `text/plain`/`text/uri-list` на document.body → очередь; не перехватываем drop на textarea/select и текстовых input параметров §6.2.
- [J-121] 2026-05-10 11:07:48 [SDK]: §9 — сохранение форматированного JSON ffprobe в файл: `save-text-dialog-contract`, IPC `saveTextWithDialog`, кнопка «Сохранить JSON…» под превью.
- [J-122] 2026-05-10 11:09:18 [SDK]: §9 — экспорт сводки инспектора TXT/HTML (`ffprobe-summary-export`), UI + фильтр HTML в диалоге сохранения; Vitest.
- [J-123] 2026-05-10 11:11:25 [SDK]: §9 — главы: `-show_chapters`, `chapters` в `MediaProbeSuccess`, парсер `ffprobe-chapters`, таблица в превью и блок в TXT/HTML сводке; Vitest.
- [J-124] 2026-05-10 11:13:22 [SDK]: §9 — контекстное меню строк дорожек/глав: портал, копирование TSV/полей в буфер (preload clipboard).
- [J-125] 2026-05-10 11:15:02 [SDK]: §9 — таблица дорожек: `language`/`titleTag` из ffprobe tags, колонки в UI и TXT/HTML сводке; расширен TSV и ПКМ.
- [J-126] 2026-05-10 11:16:20 [SDK]: §9 — битрейт дорожки из `stream.bit_rate` (`streamBitrateKbps`), колонка и ПКМ; сводка TXT/HTML; §367 JSON блок отмечен готовым в чеклисте.
- [J-127] 2026-05-10 11:19:50 [SDK]: §9 — `dispositionSummary` из `stream.disposition` (`ffprobe-disposition`), колонка/TSV/сводка/ПКМ; Vitest.
- [J-128] 2026-05-10 11:22:13 [SDK]: §9 — видео `pix_fmt`/SAR/DAR из ffprobe в дорожках, колонки UI/ПКМ/TSV и TXT/HTML сводка; Vitest.
- [J-129] 2026-05-10 11:28:40 [SDK]: §9 §363 — отдельное окно инспектора `#inspector`: `inspector-window.ts`, IPC (`openInspectorWindow`, bootstrap, push пути), `windowBounds.inspector`, меню «Инструменты», `MediaProbePanel` + toolbar «Инспектор…».
- [J-130] 2026-05-10 11:31:22 [SDK]: §9 — цветовые поля видеопотока ffprobe (`color_space`, `color_primaries`, `color_transfer`/`color_trc`, `color_range`) в `MediaProbeTrackRow`, таблице/ПКМ/TXT/HTML сводке; Vitest.
- [J-131] 2026-05-10 11:39:45 [SDK]: §7.2 — экспорт ffmpeg: whitelist поворот/зеркало (`transpose`/`hflip`/`vflip`) в `-vf` до scale/fps; `ffmpegExportVideoTransform`, IPC, пользовательские пресеты и live preview; `npm run check` ок.
- [J-132] 2026-05-10 11:59:45 [Assistant]: Документация архитектуры и точек входа: `docs/ARCHITECTURE.md`; ссылки в `README.md` и `.cursor/rules/fluxalloy-project.md`. Удалены: дублирующее правило `.cursor/rules/fluxalloy.md`, неиспользуемый `ipcMain.on('ping')`, локальный `scripts/cursor-automation/STOP` (мешал циклу SDK).
- [J-133] 2026-05-10 12:02:33 [Assistant]: ревью SDK/соседнего агента: `openInspectorWindow` больше не выдаёт доступ к произвольному строковому пути из renderer, а принимает только уже выданные main пути; восстановлен `STOP=1/0` для Cursor SDK automation; docs/journal приведены к формату.
- [J-134] 2026-05-10 12:10:45 [Assistant]: follow-up по ревью сабагентов: bootstrap отдельного инспектора устойчив к React StrictMode double-mount; draft-превью yt-dlp не падает на нестроковых полях; сводки ffprobe не пишут отрицательные длительности глав; уточнён regex playlist-прогресса.
- [J-135] 2026-05-10 12:18:31 [Assistant]: §7.2 — crop-пресеты экспорта ffmpeg (`none`/1:1/16:9/4:3) добавлены через whitelist `-vf` после rotate/flip и до scale/fps; settings/preload/main/renderer, пользовательские пресеты, live preview и Vitest синхронизированы.
- [J-136] 2026-05-10 12:30:37 [Assistant]: UI/UX follow-up по скриншоту: toolbar главного окна переносит элементы и не раздувает ширину; меню различает main/downloads/inspector и отключает рекурсивное открытие дочерних окон; downloads window получил минимальный размер.
- [J-137] 2026-05-10 12:34:03 [Assistant]: §4.C/UI — стартовый размер главного окна изменён на FHD 1920x1080; ТЗ и чек-лист синхронизированы, сохранённые пользователем bounds продолжают иметь приоритет.
- [J-138] 2026-05-10 12:54:49 [Assistant]: UI/UX reference pass: v0-референс проверен через Playwright в состояниях «Редактор» и «Загрузки»; первый emergent-референс на момент проверки отдаёт пустой/Loading экран. В главном renderer начат перенос идеи v0 без копирования: почти чёрная монтажная палитра, компактный topbar с вкладками, FFmpeg controls вынесены из toolbar в правую сворачиваемую панель, quick yt-dlp URL свёрнут в details.
- [J-139] 2026-05-10 13:13:01 [Assistant]: §6/UI — окно загрузок перестроено ближе к v0: topbar с вкладкой «Загрузки», input band + action buttons, таблица очереди с progress bar/status dot/icon-only actions, нижние панели history/log и правый settings rail с секциями «Формат»/«Метаданные»/«Сохранение»/«Сеть»/«Экспертные argv». IPC/runner не менялись, только HTML/CSS/renderer-логика второго окна.
- [J-140] 2026-05-10 13:15:14 [Assistant]: UI/UX reference закреплён для SDK: добавлен [`docs/UX_REFERENCE_V0.md`](docs/UX_REFERENCE_V0.md) с картой v0 layout/icons/sections/click behavior; `IMPLEMENTATION_CHECKLIST.md` теперь прямо ссылается на этот документ в правилах ближайшего спринта и UI TODO.
- [J-141] 2026-05-10 13:17:20 [Assistant]: Cursor SDK automation теперь получает v0/UI как первый обязательный приоритет в `agent-contract.txt`, `initial.txt` и `continue.txt`: перед остальными задачами агент читает `docs/UX_REFERENCE_V0.md` и продолжает незакрытые UI/downloads/icons/sections пункты.
- [J-142] 2026-05-10 13:22:17 [Assistant]: SDK STOP-флаг стал самовосстанавливаемым: `run-loop` создаёт локальный игнорируемый `scripts/cursor-automation/STOP` со значением `0`, если файла нет; README уточняет это поведение.
- [J-143] 2026-05-10 13:28:23 [SDK]: §1.1/§4.1/v0 — топбар: lucide-style SVG; persist раскрытия FFmpeg/yt-dlp bar и секций загрузок в settings + IPC; Vitest whitelist `mainWindowUiPanels`/`downloadsWindowUiPanels`; статусы чеклиста §4.1/§4.C/спринт.
- [J-144] 2026-05-10 13:32:20 [SDK]: §6/v0 — таблица очереди загрузок: inline SVG icon-only действий; снимок очереди дополняется `isActiveRunner` и pause-state для кнопок паузы/отмены на активной строке (POSIX-child); спринт/§6.1/§4.C обновлены.
- [J-145] 2026-05-10 13:35:37 [SDK]: §1.1/§4.1/§6 — история загрузок: те же icon-only кнопки (`act-icons`); хелперы SVG/`RowIco` подняты выше `renderHistoryEntries`. Главное окно: persist `details` ffprobe под превью (`probe*` в `mainWindowUiPanels` + `PreviewProbeBody`); Vitest whitelist и чеклист §4.1/спринт.
- [J-146] 2026-05-10 13:39:31 [SDK]: §6/v0 — таблица очереди: колонки Формат/Размер/Скорость/ETA + прогресс (bar + первая часть `progress`); поля `DownloadsQueueRow.queue*` из `parseYtdlpDownloadProgressLine` (`sizeTotal`) и `parseYtdlpInfoFormatSnippet`; очистка меты при старте загрузки/retry; Vitest; чеклист §6.1/спринт.
- [J-147] 2026-05-10 13:42:30 [SDK]: §4.1/§6 — персист живой очереди yt-dlp: `userData/downloads/queue.json` (атомарная запись, гидратация при регистрации IPC, debounce + `will-quit` flush); сброс статуса «Загрузка…»/пауз повтора в «Ожидание» при восстановлении; парсер без Electron в `ytdlp-download-queue-persist-parse` + Vitest.
- [J-148] 2026-05-10 13:43:36 [SDK]: §1.1/v0 — окно загрузок плотнее: базовый шрифт ~11.5px, topbar ~54px, уже input band/очередь/таблица/rail/log; прогресс 3px; icon-only 24×24; monospace для URL-текстареа; нижние панели короче; `focus-visible` на кнопках/textarea.
- [J-149] 2026-05-10 13:46:08 [SDK]: §6/v0 таблица — доп. эвристики колонок: `parseYtdlpQueueFormatHint` (`[info]` регистронезависимо, «video in format», `[Merger]`/`[ffmpeg]` → «слияние → ext»), `parseYtdlpInfoQueueSizeHint` для Filesize до прогресса; runner заполняет `queueFmt`/`queueSize` без перезатира уже известного размера из `[download]`; Vitest и чеклист §6.1/спринт.
- [J-150] 2026-05-10 13:51:33 [SDK]: §1.1/v0/§6/§4.A — единые lucide-пути: `src/shared/lucide-downloads-icons.ts` + генерация `RowIco`/`emitInlineSvg` в окне загрузок; `LucideMiniIcons` (folder/download/film/settings) из того же набора; topbar загрузок: cluster инспектор/редактор/движки/«О программе» + preload IPC-мост (`bridge*`), ▸ на history/log/hints; каналы §21; Vitest shared; спринт/§6.1.
- [J-151] 2026-05-10 13:56:24 [SDK]: §1.1/v0 — окно загрузок: CSS `:root` дублирует тёмные `--fa-*` из `base.css` + алиасы прежних `--bg`/…; `focus-visible` как в редакторе (`--fa-focus-ring`, select/inputs/cmd/icon-btn/topbar); топбар-иконки с `fa-border-subtle`; шрифтовой стек/smoothing как у main; спринт.
- [J-152] 2026-05-10 14:00:12 [SDK]: §1.1/§6 — окно загрузок: `data-theme` light/dark при создании HTML из `cachedSettings.theme` + preload `onThemeChanged` на `themeChanged`; `npm run check` ок.
- [J-153] 2026-05-10 14:01:38 [SDK]: §1.1/§4.C — окно yt-dlp: HiDPI CSS (`min-resolution`/dppx) + дефолтный размер/`min*` от `scaleFactor`; `focus-visible` на переключателях rail/селектах/`summary`; чеклист §4.C/§1.1.
- [J-154] 2026-05-10 14:04:14 [SDK]: §1.1/v0 — окно загрузок: нижняя зона под таблицей — история и журнал **вертикально** (лог снизу на всю ширину), `pre` тянется в flex-ячейке; §4.A/спринт.
- [J-155] 2026-05-10 14:05:40 [SDK]: §9/§4.C — окно инспектора: тулбар на `app-icon-btn` + общие lucide (`refreshCw` в `lucide-downloads-icons`, `IconRefreshCw`/film/папка/тема); `main.css` марка бренда; чеклист.
- [J-156] 2026-05-10 14:07:41 [SDK]: §1.1/v0/§4.1 — редактор: главный `app-topbar` выше под ~70px-референс; секция «Вывод» — вложенный `details` превью ffmpeg + persist `exportCommandPreview` (IPC whitelist settings-store/main).
- [J-157] 2026-05-10 14:09:10 [SDK]: §1.1/§4.C — `main.css`: HiDPI @media topbar/toolbar/icon-btn + `focus-visible` на FFmpeg `summary`; yt-dlp: топбар ~62px DIP, вкладки плотнее, доп. dppx для topbar; в CSS внутри TS template literal без обратных кавычек в комментариях.
- [J-158] 2026-05-10 14:10:20 [SDK]: §1.1/v0/§4.C — топбар редактора: `IconCircleHelp` из shared `circleHelp`; кнопка открывает тот же «О программе», что пункт меню (`about.getInfo`); спринт обновлён (`npm run check` ок).
- [J-159] 2026-05-10 14:13:18 [SDK]: §1.1/§4.C — тема «как в системе»: `AppTheme` включает `system`, `settingsGet`/`setTheme` добавляют `effectiveTheme`, меню «Вид → Тема» трёхпозиционное, `nativeTheme.updated` синхронизирует окна; Vitest parse `theme`; загрузки через `ResolvedAppTheme`.
- [J-160] 2026-05-10 14:15:14 [SDK]: §1.1/§4.C — `window-hidpi.ts`: общий `displayMatchingRestoreRect`/пороги `min*` по `scaleFactor` для загрузок, главного редактора и инспектора; дефолтные размеры главного/инспектора от `workArea` (сохранённые bounds без изменений); Vitest порогов; чеклист §4.C/спринт.
- [J-161] 2026-05-10 14:17:55 [SDK]: §9/§4.1/§1.1 — окно инспектора: шапка `app-topbar` как у редактора; блоки ffprobe с persist через `mergeMainWindowUiPanels` (`probe*` те же что под превью); CSS `focus-visible` на `.app-probe-summary`, большие `max-height` таблицы/JSON в standalone; §4.1/спринт.
- [J-162] 2026-05-10 14:19:32 [SDK]: §4.1/§21 — после `persistMainWindowUiPanelsMerge` broadcast `mainWindowUiPanelsChanged` всем окнам; главный preload: `onMainWindowUiPanelsChanged`; `App.tsx` и инспектор обновляют UI без перечитывания JSON; канал в `ipc-channels`.
- [J-163] 2026-05-10 14:20:55 [SDK]: §1.1/§6 — окно загрузок rail v0: строки checkbox — подпись слева, pill-toggle справа; все select в rail + справочник — компактный pill + кастомный chevron (`appearance:none`, без обратных кавычек в комментариях внутри template); `hist-inline` для фильтра истории; чеклист спринт.
- [J-164] 2026-05-10 14:22:07 [SDK]: §1.1/§6.1/v0 — таблица очереди: колонка прогресса — полоска + отдельный процент, зелёная заливка при «Готово», подпись speed/ETA без дубля процента; лёгкая зебра и hover строк; `title` у статуса; выравнивание числовых колонок; чеклист спринт.
- [J-165] 2026-05-10 14:23:22 [SDK]: §6.4/§1.1 — журнал операций окна загрузок: строки DOM (теги out/err + тело), цвет stderr, обрезка по лимиту удалением старых узлов, кнопка «Очистить вид», счётчик строк/размера; «Сохранить лог» — плоский текст из узлов `.log-line`; чеклист.
- [J-166] 2026-05-10 14:33:22 [Assistant]: ревью SDK после падения на 24/75: незакоммиченный `run-loop` retry доведён до повторов той же итерации при `status=error`; старт главного окна возвращён к FHD 1920×1080 на подходящем дисплее; чеклист синхронизирован.
- [J-167] 2026-05-10 14:43:10 [Assistant]: не-SDK блок после ревью: restore `downloads/queue.json` перенумеровывает duplicate id и покрыт Vitest; `mainWindowUiPanels` принимает merge только от main/inspector + preload whitelist push-снимков; topbar bridge в окне загрузок показывает ошибки, ffprobe context menu доступен с клавиатуры и ограничен по высоте.
- [J-168] 2026-05-10 14:50:04 [Assistant]: SDK automation review: настройки вынесены в `scripts/cursor-automation/src/sdk-settings.ts` с комментариями; `run-loop` читает дефолты оттуда, `.env.example` стал списком override; полный повтор любого `status=error` снова только явный (`LOOP_RETRY_RUN_ERROR=1`), быстрые transient error-run всё ещё повторяются.
- [J-169] 2026-05-10 14:54:27 [SDK]: §1.1/v0 — редактор: `PreviewTransport` под превью (skip ±5 с, play/pause, mute, slider громкости, fullscreen по stack); топбар: `rotate-ccw`/`rotate-cw`/`scissors` → циклы transform/crop §7.2 через settings IPC; общие SVG в `EDITOR_*_ICONS` + Vitest; `npm run check` ок.
- [J-170] 2026-05-10 15:00:23 [SDK]: §1.1/v0 — топбар: компактная строка `ffmpeg • yt-dlp` (парсинг `--version`) + `app-topbar-trailing`; таймлайн: zoom-in/out (lucide), окно scrub 1×–8×, `TimelineWaveform` (Web Audio, `waveform-peaks` + Vitest), кнопка «Обрезать → экспорт»; `npm run check` ок.
- [J-171] 2026-05-10 15:03:12 [SDK]: §1.1/v0 — под waveform: линейка времени видимого окна (`timeline-ruler` + деления/playhead); строка **Видео/Аудио/Позиция** из ffprobe + оценка кадра по fps из `detail`; HiDPI @media для ruler/facts; Vitest на шаг делений; спринт/§1.1 обновлены.
- [J-172] 2026-05-10 15:04:17 [SDK]: §1.1/§4.C — второй проход DPI редактора: `@120dpi`/`@168dpi` в `main.css` для **PreviewTransport**, zoom/scrub/waveform/ruler/media facts/marker/InOut и `.app-preview-probe`; спринт вычищен от дубликатов (7 пунктов).
- [J-173] 2026-05-10 15:06:20 [SDK]: §6.1/§4.A — окно загрузок: второй DPI-проход в inline CSS (`buildDownloadsHtml`): @120dpi/@168dpi для URL-band, queue toolbar/таблица/прогресс/иконы строк, журнал/история, rail (summary, selects, checkbox-pill).
- [J-174] 2026-05-10 15:07:28 [SDK]: §1.1/v0 — клик по `app-timeline-ruler-track` и клавиатура (Home/End/стрелки) → seek в окне zoom; `cursor` + `focus-visible` в `main.css`; чеклист §1.1/спринт.
- [J-175] 2026-05-10 15:09:01 [SDK]: §1.1 — снап seek к кадру по fps ffprobe: `snapSeekTimeSec` + Vitest; scrub/линейка/стрелки/In/Out; счётчик «кадр ~» по снапу; чеклист/спринт.
- [J-176] 2026-05-10 15:11:21 [SDK]: §1.1/IPC — `MediaProbeSuccess.videoFpsApprox` из ffprobe (`ffprobe-video-fps`, fallback `nb_frames`/duration); сводки TXT/HTML; `VideoTimeline` приоритет поля над regex `detail`; Vitest + чеклист.
- [J-177] 2026-05-10 15:14:05 [SDK]: §4.1/§1.1 — главное окно: **сворачивание всей колонки FFmpeg** (`ffmpegSettingsRailOpen`→`mainWindowUiPanels`, whitelist main/settings-store), chevron + FAB «FFmpeg» на превью; HiDPI правки; Vitest settings-store.
- [J-178] 2026-05-10 15:15:58 [SDK]: §6/§4.A/v0 единые иконки — окно загрузок: в топбар-кластер добавлена lucide **`download`** (`dlTopUrl`) → скролл + фокус `#urls`; порядок: film → download → home → engines → help; Vitest на HTML; чеклист §4.A/§4.
- [J-179] 2026-05-10 15:17:27 [SDK]: §6/§4.A/v0 — вкладки «Редактор/Загрузки» в topbar окна загрузок: inline SVG **home**/**download** из shared (`emitInlineStrokeSvg`); стили `.workspace-tab-glyph`; чеклист §4.A/§4; `npm run check` ок.
- [J-180] 2026-05-10 15:18:35 [SDK]: §4.A/v0 единые иконки — `EDITOR_TOPBAR_ACTION_ICONS` (снимок, save, ban, cloud-download): данные в `lucide-downloads-icons.ts`, `LucideMiniIcons` через `renderStrokeParts`; Vitest сериализации; чеклист §4/спринт §1.1; `npm run check` ок.
- [J-181] 2026-05-10 15:19:48 [SDK]: §4.A/v0 единые иконки — **`EDITOR_THEME_ICONS`** (sun/moon) в shared, `IconSun`/`IconMoon` через `renderStrokeParts`; Vitest; чеклист §4/спринт §1.1 (`sun/moon` перенесены из «позже»); `npm run check` ок.
- [J-182] 2026-05-10 15:20:52 [SDK]: §6/§1.1/v0 — окно загрузок: **`focus-visible`** на сворачиваемых **история / журнал / hints** (`<summary>`), тот же `--fa-focus-ring`, что у rail; чеклист §1.1/спринт §6; `npm run check` ок.
- [J-183] 2026-05-10 15:22:02 [SDK]: §1.1/v0 раскрываемые секции — главное окно: **`app-url-summary:focus-visible`** (`--fa-focus-ring`), как у превью argv/`app-settings-summary`; чеклист §1.1/спринт; `npm run check` ок.
- [J-184] 2026-05-10 15:23:15 [SDK]: §1.1/§4.C — главное окно: HiDPI `@120dpi`/`@168dpi` для **быстрой yt-dlp-полосы** (`app-url-summary` min-height/padding/font, `app-url-body` padding); спринт §1.1; `npm run check` ок.
- [J-185] 2026-05-10 15:24:32 [SDK]: §1.1/v0 — главное окно: **`focus-visible`** на **`app-url-input`** и универсально на **`app-btn`** (--fa-focus-ring); чеклист §1.1/спринт; `npm run check` ок.
- [J-186] 2026-05-10 15:25:31 [SDK]: §6/§4.A — окно загрузок: HiDPI **SVG вкладок** «Редактор/Загрузки» — `.workspace-tab-glyph svg` **17px / 18px** в `@120dpi` / `@168dpi` inline CSS; спринт §6; `npm run check` ок.
- [J-187] 2026-05-10 15:26:38 [SDK]: §6/v0 topbar — окно загрузок: `.dl-topbar-ico svg` **19px / 20px** на @120dpi / @168dpi + `display: block` у глифа; спринт §6; `npm run check` ок.
- [J-188] 2026-05-10 15:27:59 [SDK]: §1.1/§6/v0 HiDPR stroke-icons — главный `main.css`: **topbar-кластер** SVG **21×22px** + вкладка **«Загрузки»** **17×18px**; downloads: **`td.act .icon-btn svg` 17×18px** @120/@168 + `display: block`; спринт; `npm run check` ок.
- [J-189] 2026-05-10 15:29:17 [SDK]: §1.1/v0 — под превью: HiDPR **транспорт** + **zoom таймлайна** SVG **21×22px**, **`app-ffmpeg-rail-restore`** / **`app-settings-rail-collapse-btn`** **20×21px** в `@120dpi`/`@168dpi`; `display: block` для глифов; спринт §1.1; `npm run check` ок.
- [J-190] 2026-05-10 15:30:36 [SDK]: §1.1/v0 доступность topbar — **`app-workspace-tab:focus-visible`** (`--fa-focus-ring`); HiDPR марк **инспектора** `IconFilm` — **14×15px** `@120dpi`/`@168dpi`; спринт §1.1; `npm run check` ок.
- [J-191] 2026-05-10 15:32:01 [SDK]: §6 downloads layout / §1.1 — окно загрузок: **`#urls`** `aria-describedby`→`urlsHint`, **`caption.sr-only`** у таблицы очереди; **Загрузки** `aria-current="page"`; главное окно — **Редактор** `aria-current`; спринт; `npm run check` ок.
- [J-192] 2026-05-10 15:33:27 [SDK]: §6/v0 нижняя зона — **caption.sr-only** для **истории**; **`#logPre`**: **`role=log`**, **`aria-live=polite`**, **`aria-relevant=additions`**, `aria-label`; спринт §6; `npm run check` ок.
- [J-193] 2026-05-10 15:34:32 [SDK]: §4.A/§1.1 — **`MediaProbePanel`**: **`caption.app-visually-hidden`** у таблиц **дорожек** и **глав** (ffprobe); спринт §1.1; `npm run check` ок.
- [J-194] 2026-05-10 15:36:05 [SDK]: §1.1/v0 progressive disclosure — **`app-url-bar`**: текст **`.app-url-hint`** + **`aria-describedby`** для поля URL; блок **`.app-url-field`**, HiDPR `.app-url-hint` в `main.css`; спринт; `npm run check`/`lint` ок.
- [J-195] 2026-05-10 15:37:23 [SDK]: §1.1/v0 FFmpeg — **«Превью команды ffmpeg»**: **`pre`** с **`aria-describedby="exportCommandPreviewHint"`**, **`id`** на **`.app-export-preview-hint`**; спринт; `npm run check` ок.
- [J-196] 2026-05-10 15:38:47 [SDK]: §4.A/§1.1 MediaProbePanel — **`app-probe-toolbar-hint`** + **`aria-describedby`** (**экспорт сводки TXT/HTML**, **JSON ffprobe**), **`pre`** JSON с **`aria-label`**; спринт §1.1; `npm run check` ок.
- [J-197] 2026-05-10 15:40:15 [SDK]: §6 окно загрузок — **a11y фильтров** (`inline-filter-field`/`hist-inline-field`, **`for`**, **`sr-only` hints**), **`role=group`** + **`aria-labelledby`** для строк **cookies/каталог**, **`label for=hintInsert`** (убран дублирующий **`aria-label`** у **queueRetrySelect**); спринт §6; `npm run check` ок.
- [J-198] 2026-05-10 15:41:40 [SDK]: §1.1/v0 progressive disclosure — **rail FFmpeg**: **`app-settings-section-hint`** под summary + **`aria-describedby`** на **grid/stack** (5 секций), HiDPR `@120dpi`/`@168dpi` для hint; спринт §1.1; `npm run check` ок.
- [J-199] 2026-05-10 15:43:11 [SDK]: §6/v0 downloads rail + queue — **`aria-describedby=urlsHint`** у **Добавить/Начать**, **`dlQueueToolbarHint`** (**`sr-only`**) для **пауза/отмена/очистки**; **rail**: **`opts-hint` id** + **`aria-describedby`** на **`settings-body`** (5 секций); спринт §6; `npm run check` ок.
- [J-200] 2026-05-10 15:44:44 [SDK]: §6/§1.1 — downloads **история/журнал**: **`sr-only`** секционные hints + **`aria-describedby`** (**кнопки**, **фильтр истории**, **`#logPre`**); редактор **`basenameForAriaLabel`** + **`video aria-label`**; §1.1 чеклист; `npm run check` ок.
- [J-201] 2026-05-10 15:46:11 [SDK]: §1.1/§9 — **`PreviewProbeBody`**: **`role=region`**, **`aria-label`**, **`probePanelOverviewHint`** (**`app-visually-hidden`**); **«Копировать»** (превью ffmpeg) **`aria-describedby=exportCommandPreviewHint`**; спринт; `npm run check` ок.
- [J-202] 2026-05-10 15:48:01 [SDK]: §4.C/v0 **circle-question** — **`AboutDialog`** (общий **`app-modal`**), главное окно на компонент; **инспектор §9**: **`IconCircleHelp`** + тот же диалог/`about.getInfo`; чеклист §4.C/спринт §1.1; `npm run check` ок.
- [J-203] 2026-05-10 15:49:39 [SDK]: §1.1/v0 rail FFmpeg — **`aria-describedby`** секционных hint id для **компактных кнопок**: последний **снимок** (`ffmpegAudioSectionHint`), управление **пресетами** (`ffmpegPresetsSectionHint`), последний **экспорт** (`ffmpegOutputSectionHint`); чеклист §1.1 progressive; `npm run check` ок.
- [J-204] 2026-05-10 15:51:08 [SDK]: §6 downloads **settings rail** — **`aria-describedby`** секционных **`*SectionHint`** на поля и кнопки (**Формат/Метаданные/Сохранение/Сеть/Эксперт**), **`pre#argsPreview`** с **`aria-label`**; спринт §6; `npm run check` ок.
- [J-205] 2026-05-10 15:54:10 [SDK]: §1.1/v0 **быстрая yt-dlp-полоса** — **`aria-describedby=quickYtdlpUrlHint`** у кнопок **«Открыть окно»** и **«Из буфера»** (рядом с полем URL); чеклист §1.1 progressive; `npm run check` ок.
- [J-206] 2026-05-10 15:55:48 [SDK]: §4.1/§6 — окно загрузок: **`suppressDetailsUiPersist`** при программном открытии журнала на **log reset** — **`toggle` больше не вызывает** **`mergeUiPanels`** (сохраняется пользовательское **log: false**); чеклист §4.A/§4.1/спринт; `npm run check` ок.
- [J-207] 2026-05-10 18:38:10 [Assistant]: ревью SDK после 37 успешных итераций и падения на 38/100: `npm run check`/`build` зелёные; исправлено подавление persist для async `toggle` в окне загрузок (автопоказ лога не пишет `log:true`), добавлен streaming-лимит waveform по размеру ответа (≤96 MiB), и trim-снимок экспорта привязан к текущему пути, чтобы не взять In/Out от прошлого файла.
- [J-208] 2026-05-10 18:49:36 [Assistant]: §6.4 — парсер yt-dlp прогресса распознаёт retry-счётчики (`Retrying (N/M)`, `Retrying fragment X (N/M)`) и размер из строк `of ~ 5.00MiB`; Vitest `ytdlp-progress-parser` расширен до 62 тестов.
- [J-209] 2026-05-10 18:55:20 [Assistant]: §7.2 trim — вынесена тестируемая валидация `parseFfmpegExportTrim` для IPC/renderer payload: принимаются только конечные неотрицательные `inSec < outSec`; main export path использует общий helper, чеклист `Trim` закрыт.
- [J-210] 2026-05-10 18:58:50 [Assistant]: §9 ffprobe — добавлен чистый `ffprobe-side-data` summarizer для Dolby Vision/HDR (`DOVI configuration record`, mastering display, content light); `ffprobe-service` добавляет summary в сведения видеодорожки, поэтому таблица инспектора и TXT/HTML экспорт получают side_data без нового IPC.
- [J-211] 2026-05-10 19:02:58 [Assistant]: §6.4 yt-dlp — `parseYtdlpQueueFormatHint` расширен на post-processing строки `ExtractAudio`, `VideoRemuxer`/`FFmpegVideoRemuxer`, `FFmpegVideoConvertor`; колонка «Формат» показывает итоговый контейнер/аудио из редких логов, Vitest `ytdlp-progress-parser` расширен до 63 тестов.
- [J-212] 2026-05-10 19:08:05 [Assistant]: runtime fix — в главном renderer убраны `window.prompt`/`window.alert` из управления пользовательскими FFmpeg-пресетами; имя нового/переименованного пресета теперь вводится через `app-modal`, лимит 8 пресетов показывается в статусбаре/модалке, `npm run check` ок.
- [J-213] 2026-05-10 19:20:10 [Assistant]: UX pivot по v0 — ТЗ/чеклист переведены с «отдельное окно загрузок как основной режим» на единый workspace `Редактор`/`Загрузки`; главный renderer получил активную вкладку `Загрузки` с URL-band, живой queue table через downloads IPC и pop-out для старого полного окна.
- [J-214] 2026-05-10 19:30:05 [Assistant]: v0 controls — заведён React pill-switch паттерн для бинарных настроек; `Без аудио` в FFmpeg rail больше не select из двух вариантов, а `role=switch` с русской подсказкой; UX_REFERENCE/ТЗ/чеклист закрепили правило: binary → pill switch, multi-choice → select.
- [J-215] 2026-05-10 19:35:20 [Assistant]: вкладка `Загрузки` усилена без новых IPC: добавлены summary cards по очереди, status filter chips, inline progress bars, status-dot chips и действия `Файл`/`Папка` для готовых строк; rail теперь объясняет, что вкладка использует тот же сервис, что pop-out.
- [J-216] 2026-05-10 19:40:40 [Assistant]: вкладка `Загрузки` получила основной settings rail в React: главный preload открыл валидированные `getCliOptions`/`setCliOptions`, UI управляет format preset, playlist/audio-only/open-after-success через pill switches, subtitles/cookies/impersonate/retry profile/selects и показывает command preview.
- [J-217] 2026-05-10 19:45:20 [Assistant]: исправлен sender pinning downloads IPC для embedded вкладки: каналы очереди/настроек/истории теперь явно принимают главное окно через `isMainWindowSender`; React-вкладка показывает последние записи истории, очистку и открытие файла/папки результата без pop-out.
- [J-218] 2026-05-10 19:47:05 [Assistant]: вкладка `Загрузки` получила базовое управление очередью без pop-out: основной preload открыл `removeRow`/`clearFinished`/`clearQueue`, UI добавил удаление строки, очистку завершённых и очистку всей очереди со статусными подсказками.
- [J-219] 2026-05-10 19:49:05 [Assistant]: вкладка `Загрузки` получила pause/resume активной yt-dlp строки: snapshot прокидывает `isActiveRunner`/pause flags в React, основной preload открыл `pauseYtdlp`/`resumeYtdlp`, строка показывает кнопку `Pause`/`Resume` при поддержке ОС.
- [J-220] 2026-05-10 19:54:05 [Assistant]: вкладка `Загрузки` получила live log yt-dlp: downloads log sink теперь fan-out в pop-out и главное окно, основной preload валидирует `DownloadsLogPayload`, React rail показывает stdout/stderr, очистку и сохранение видимого лога без pop-out.
- [J-221] 2026-05-10 19:56:40 [Assistant]: вкладка `Загрузки` получила управление каталогом yt-dlp: основной preload открыл `get/open/pick/clearOutputDirectory`, React rail показывает текущий путь, default/custom статус и кнопки `Открыть`/`Выбрать`/`По умолчанию`.
- [J-222] 2026-05-10 19:58:20 [Assistant]: вкладка `Загрузки` получила управление Netscape cookies-файлом: основной preload открыл `pickCookiesFile`/`clearCookiesFile`, React rail показывает сохранённый путь, предупреждение о приоритете файла над browser cookies и кнопки выбора/очистки.
- [J-223] 2026-05-10 20:00:20 [Assistant]: вкладка `Загрузки` получила network поля yt-dlp в React rail: `limit-rate`, `--retries` и `--fragment-retries` редактируются inline и сохраняются через существующий валидированный `setCliOptions`.
- [J-224] 2026-05-10 20:03:10 [Assistant]: актуализирован `IMPLEMENTATION_CHECKLIST.md`: добавлена оценка готовности полного итога (~38%), обновлён текущий снимок проекта, ближайший TODO спринта и статусы по основному yt-dlp workspace/editor/ffmpeg/ffprobe с учётом последних React-вкладок.
- [J-225] 2026-05-10 20:08:30 [Assistant]: UI switches — в `App.tsx` вынесен общий `PillSwitch` для бинарных настроек; `Без аудио`, `Весь плейлист`, `Только аудио`, `Открыть после успеха` теперь используют один v0-style pill pattern с `role=switch`, `aria-label` и привязкой к русским пояснениям.
- [J-226] 2026-05-10 20:12:30 [Assistant]: UI copy — в editor/downloads rail русифицированы оставшиеся англоязычные подписи быстрых controls (`Crop`, `AAC bitrate`, `Pause/Resume`, network labels, cookies/impersonate) и исправлена подсказка про доверенный cookies export.
- [J-227] 2026-05-10 20:16:10 [Assistant]: bugfix embedded downloads — `broadcastDownloadsSnapshot` больше не завязан на открытый pop-out: snapshot очереди с active/pause flags рассылается и во вкладку `Загрузки` главного окна, поэтому `Добавить`/`Удалить`/`Старт` обновляют React-таблицу без отдельного окна.
- [J-228] 2026-05-10 20:22:10 [Assistant]: аудит похожих UI no-op — основные mutating downloads IPC (`addLines`, `clearQueue`, `clearFinished`, `removeRow`, reset output/cookies) больше не возвращают `void`/ложный `0`: main/preload/renderer/pop-out используют явный `{ok,error}`/`{ok,added}`/`{ok,removed}`, а ошибки показываются в статусе/alert.
- [J-229] 2026-05-10 20:27:20 [Assistant]: follow-up no-op audit — `startDownloadsSequential` теперь синхронно отвечает ошибкой, если очередь уже занята или нет строк «Ожидание», а `moveRow` в pop-out возвращает `{ok,error}` вместо `void`; `npm run check` зелёный.
- [J-230] 2026-05-10 20:28:40 [Assistant]: UI copy — во вкладке `Загрузки` оставшиеся action labels `Start`/`Retry` и `Live log` заменены на русские `Старт`/`Повтор` и `Живой лог`.
- [J-231] 2026-05-10 20:31:20 [Assistant]: паритет вкладки `Загрузки` с pop-out — основной preload открыл `moveRow`, `openQueueOutputInHandler`, `openHistoryOutputInHandler`; React-вкладка получила ↑/↓ перестановку строк и действие «В редактор» для готовых строк/истории.
- [J-232] 2026-05-10 20:32:30 [Assistant]: UI плотность — кнопки действий строки во вкладке `Загрузки` уплотнены локальным CSS, чтобы новые ↑/↓/«В редактор» не раздували таблицу.
- [J-233] 2026-05-10 20:57:10 [Assistant]: §3 policy — зафиксирован bundled-first подход для движков: релиз должен поставлять проверенные `ffmpeg.exe`/`ffprobe.exe`/`yt-dlp.exe` в `resources/bin`; `userData/bin` остаётся fallback/update, ручные пути — override. Добавлен `bin/README.md`, `.gitignore` запрещает случайный коммит бинарников.
- [J-234] 2026-05-10 21:01:20 [Assistant]: §3 download fallback — источник FFmpeg переведён на список зеркал: сначала GitHub BtbN `ffmpeg-master-latest-win64-gpl.zip`, затем gyan.dev essentials; SHA256 теперь выбирается по конкретному архиву, legacy `FfmpegSha256` оставлен только для gyan.dev.
- [J-235] 2026-05-10 21:09:00 [Assistant]: §3 maintenance — добавлен IPC/preload/UI для удаления скачанных движков из `userData/bin` (только известные `ffmpeg`/`ffprobe`/`yt-dlp` и cache); bundled `resources/bin` и ручные override не трогаются.
- [J-236] 2026-05-10 21:14:20 [Assistant]: §3 dev bootstrap — добавлен `scripts/prepare-engines-win.mjs` и npm script `engines:prepare:win`; `predev` автоматически наполняет проектный `bin/` перед `npm run dev`, `prebuild:win` делает то же перед Windows installer.
- [J-237] 2026-05-10 21:18:10 [Assistant]: §3 engines local — фактически скачаны локальные ignored `bin/ffmpeg.exe`, `bin/ffprobe.exe`, `bin/yt-dlp.exe`; исправлена проверка версии: ffmpeg/ffprobe запускаются с `-version`, yt-dlp с `--version`, чтобы статус движков в dev не был ложной ошибкой.
- [J-238] 2026-05-10 21:25:40 [Assistant]: §6.4 downloads open fix — исправлен no-op после успешной загрузки YouTube с merge: yt-dlp запускается с UTF-8 stdout/env, а open file/folder/handler умеют найти финальный файл по `[videoId]` внутри каталога загрузок, если сохранённый путь указывал на удалённый промежуточный `.f*.mp4/.webm`.
- [J-239] 2026-05-10 21:34:10 [Assistant]: §6.4 preview compatibility — причина «В редактор переключает вкладку, но видео не играет» подтверждена как `.mkv`/Matroska preview issue в Chromium: добавлен MP4-пресет редактора по умолчанию, а для уже скачанных неподдержанных контейнеров `В редактор` создаёт `preview-cache/*.mp4` через bundled ffmpeg (copy video + AAC, fallback libx264).
- [J-240] 2026-05-10 21:40:20 [Assistant]: §6.4 preview UX — ручная проверка proxy на текущем ролике: MKV -> MP4/AAC создаётся ~15–16 секунд. Добавлен статус в UI перед открытием в редакторе и лог `mp4 preview proxy ready`, чтобы подготовка preview-cache не выглядела как зависание.
- [J-241] 2026-05-10 21:48:30 [Assistant]: §4.B/§7 preview playback — для `fluxmedia://` заменён `net.fetch(file://...)` на собственный file streaming с `Range`/`206 Partial Content`, `Accept-Ranges` и MIME по расширению; renderer теперь логирует код ошибки HTMLVideoElement в статус/лог.
- [J-242] 2026-05-10 22:12:39 [SDK]: §1.1/v0/§7 — `PillSwitch` 2-pass libx264 в rail «Формат» (только с видеобитрейтом), persist `ffmpegExportTwoPass`, двойной ffmpeg с temp `passlog`/очисткой, превью двух команд и прогресс 0–50/50–100; IPC/preload/argv; microtask для сброса `previewBlobUrl` (eslint); Vitest + чеклист.
- [J-243] 2026-05-10 22:15:41 [SDK]: §6.1/v0 — вкладка «Загрузки»: «История» и «Живой лог» под таблицей очереди, rail только настройки; копия кнопок «Добавить в очередь»/«Начать загрузку»; CSS `app-downloads-table-zone`/`app-downloads-lower-stack`/футер rail; ограничены высоты истории и лога в нижнем стеке. `npm run check` зелёный.
- [J-244] 2026-05-10 22:18:00 [SDK]: §6.1/v0 UX — действия строк очереди на React-вкладке «Загрузки» переведены в icon-only (`app-icon-btn` 26px); глифы из `lucide-downloads-icons` + `LucideMiniIcons` (chevron/play/pause/retry/file/folder/outbound/trash); в shared добавлен `x` для `RowIco`; тест bootstrap. `npm run check` зелёный.
- [J-245] 2026-05-10 22:20:27 [SDK]: §4.A/§6 — встроенная вкладка «Загрузки»: управляемые `<details>` «История»/«Живой лог» + persist в `downloadsWindowUiPanels` через главный preload `downloads.mergeUiPanels` (существующий IPC); гидратация из `settings.get`; автолог строки очереди не трогает раскрытие. `npm run check` зелёный.
- [J-246] 2026-05-11 20:35:00 [Assistant]: §6.1/§6.4 — снижен шум при активной загрузке: патч очереди/снапшота по прогрессу не чаще ~1 с (`downloads-queue-runner`), полоса в React и pop-out с плавным `transition` ширины; живой лог — строки `[download] … NN%` сэмплируются (шаг ≥0,1 п.п. или heartbeat 2,5 с), прочие строки без изменений; скорость в колонке сглаживается (EMA) и показывается в стабильных MiB/s vs KiB/s; в подписи ячейки и заголовках таблицы «Осталось» вместо ETA; добавлены `parseYtdlpSpeedToBytesPerSec` / `formatTorrentStyleSpeedFromBps` / `parseYtdlpProgressPercentNumber` и тесты. `npm run check` зелёный.
- [J-247] 2026-05-11 20:36:00 [Assistant]: снова добавлен `docs/AGENT_INSTRUCTIONS_AND_AGREEMENTS.md` (единый playbook для агента и владельца): текст восстановлен из транскрипта Cursor, в истории Git файла не было; в §16 файла зафиксировано восстановление.
- [J-248] 2026-05-11 21:05:00 [Assistant]: §6.1 — вкладка «Загрузки» rail: секции «Формат / Метаданные / Сохранение / Сеть / Эксперт и превью» на `<details>` с persist в `downloadsWindowUiPanels` (те же ключи и дефолты открытия, что pop-out); «Открыть после успеха» перенесён в метаданные; в «Формат» добавлено поле языков субтитров (`subLangs` / `subLangsLine`); подсказка про pop-out для argv; стили шеврона в `main.css`. `npm run check` зелёный.
- [J-249] 2026-05-11 21:28:00 [Assistant]: §6.1 — после `mergeDownloadsWindowUiPanelsPatch` main шлёт `downloadsWindowUiPanelsChanged` в главное окно; preload `downloads.onDownloadsWindowUiPanelsChanged` + гидратация вкладки «Загрузки» (история, лог, rail) без перезапуска. `npm run check` зелёный.
- [J-250] 2026-05-11 21:40:00 [Assistant]: §6 — дефолт шаблона `-o` без `[%(id)s]` в имени; `shortLabel` из basename вывода (срез хвоста `[id]`); скорость в UI — MB/s и KB/s (×1000); `updateDownloadsRow`: `undefined` для queue* не удаляет поле (колонка «Скорость» после «Готово»); пример в `Data/ytdlp_commands.json`. `npm run check` зелёный.
- [J-251] 2026-05-11 22:02:00 [Assistant]: §6 — ранний `shortLabel` из строки `[info] …: Downloading N format(s)` (`parseYtdlpInfoDownloadingTitlePrefix`, без 11-символьного id YouTube); runner вызывает подсказку на stdout/stderr; UX_REFERENCE колонка «Осталось»; чеклист спринта. `npm run check` зелёный.
- [J-252] 2026-05-11 23:15:00 [Assistant]: §6.1/§6.3 — вкладка «Загрузки» rail «Эксперт»: textarea `extraArgsLine` + optgroup‑селектор вставки токена; CSS `min(…rem, vh)` для таблицы/истории/лога; `Data/ytdlp_commands.json` — правка `-f`, новые флаги; UX_REFERENCE/чеклист. `npm run check` зелёный.
- [J-253] 2026-05-11 23:55:00 [Assistant]: §1.1 help links — `src/shared/external-doc-urls.ts`; внешние ссылки в быстрой полосе yt-dlp, rail «Эксперт», сводке ffprobe (`MediaProbePanel`), «О программе»; pop-out загрузок: подсказка + `setWindowOpenHandler` → `shell.openExternal`. Чеклист спринта. `npm run check` зелёный.
- [J-254] 2026-05-11 16:00:00 [Assistant]: журнал — пронумерованы все существующие строки **J-001…J-253**; в «Правило записей» зафиксированы правила **J-NNN**; выполнено обзорное ревью проекта (архитектура целостна, риски: монолитный `App.tsx`, дублирование UI вкладки/pop-out загрузок, незакрытые §8/§10/§11/локализация/DPI-матрица из чеклиста; движки Win-centric; нет e2e). Детали — в ответе ассистента в чате.
- [J-255] 2026-05-11 16:45:00 [Assistant]: из «Правило записей» удалён ошибочно добавленный блок «После паузы…» — рабочие инструкции после SDK не входят в назначение журнала; нумерация **J-NNN** сохранена.
- [J-256] 2026-05-11 21:04:12 [SDK]: §6/§4.C — встроенная вкладка «Загрузки»: HiDPI `@120dpi`/`@168dpi` в `main.css` (URL-band, сводка/фильтр, таблица, нижние панели, rail, icon-only SVG); спринт §6.1; `npm run check` ок.
- [J-257] 2026-05-11 21:06:42 [SDK]: §6.1/v0 — колонка прогресса: полоска + явный `%` (fallback текст без парсящегося %); «Начать загрузку» — primary; HiDPI `@192dpi` для вкладки; спринт; `npm run check` ок.
- [J-258] 2026-05-11 21:08:13 [SDK]: §6.1/v0 — `@media (max-width:1100px)`: rail загрузок не скрывается, сетка `1fr`+`auto`, rail под колонкой с `max-height`+scroll; подсказка в UI; чеклист §1.1/спринт; `npm run check` ок.
- [J-259] 2026-05-11 21:10:18 [SDK]: §4/§6 единые иконки — `DOWNLOADS_TOPBAR_CLUSTER_ICONS.clipboard`/`popOutWindow`, `IconClipboardPaste`/`IconPopOutWindow`, кнопки «Из буфера»/«Pop-out» на вкладке; Vitest; чеклист §4.C/спринт; `npm run check` ок.
- [J-260] 2026-05-11 21:12:08 [SDK]: §6/§4 — URL-band вкладки «Загрузки»: `IconQueuePlus`/`IconPlay`/`IconBan`/`IconQueueTrash` + `app-btn-icon-leading` на всех действиях очереди; Vitest `plus`; спринт; `npm run check` ок.
- [J-261] 2026-05-11 21:14:43 [SDK]: §6.1/v0 узкая ширина — `#downloads-ytdlp-settings-rail`, `matchMedia(1100px)`, кнопка «К настройкам»+`scrollIntoView`, `overflow-y` на workspace; чеклист §1.1/спринт; `npm run check` ок.
- [J-262] 2026-05-11 21:16:31 [SDK]: §6/§4 — «История»/«Живой лог» вкладки: `IconRefreshCw`, `IconQueueTrash`/`IconQueueX`, карточки `file`/`folder`/`outbound`, лог `IconSave`; `IconQueueX` в `LucideMiniIcons`; спринт; `npm run check` ок.
- [J-263] 2026-05-11 21:18:15 [SDK]: §6 rail v0-иконки — `IconHome` из `DOWNLOADS_TOPBAR_CLUSTER_ICONS.home`; cookies **Выбрать/Очистить**, каталог **Открыть/Выбрать/По умолчанию**, футер **Обновить/Остановить** с `app-btn-icon-leading` (`App.tsx`/`LucideMiniIcons`); спринт; `npm run check` ок.
- [J-264] 2026-05-11 21:19:57 [SDK]: §6.3/§4 — pop-out `buildDownloadsHtml`: `QUEUE_ROW_ACTION_ICONS` + `EDITOR_TOPBAR_*` для кнопок с текстом (`cmd-icon-leading` CSS); паритет иконок с вкладкой на ленте/тулбаре/истории/логе/rail; спринт; `npm run check` ок.
- [J-265] 2026-05-11 21:22:36 [SDK]: §6.3/v0 — pop-out rail: **pill-switch** (кнопки `role=switch`, Вкл/Выкл) вместо checkbox для плейлиста/только аудио/открыть в обработчике; `syncFmtPresetAudioLock`; превью argv на toggle; спринт; `npm run check` ок.
- [J-266] 2026-05-11 21:24:20 [SDK]: §6.1/v0 pop-out узкая ширина — `@media (max-width:960px)`: сетка `1fr` + `auto`, rail с `max-height`+`overflow`, `dl-main` вертикальный scroll; `#dl-ytdlp-settings-rail`, кнопка **«К настройкам»** (`matchMedia`+settings SVG); подсказка в ленте URL; спринт; `npm run check` ок.
- [J-267] 2026-05-11 21:26:49 [SDK]: DPI-матрица 100–200%: добавлены `@144dpi` для embedded downloads в `main.css`, и `@144/@192` для pop-out (`downloads-window.ts`); `npm run check` ок.
- [J-268] 2026-05-11 21:29:44 [SDK]: §6.3 pop-out справочник токенов — добавлены поиск + полный кликабельный список (группы по category) для вставки в `extraArgsInput` с авто-обновлением превью; `npm run check` ок.
- [J-269] 2026-05-11 21:31:22 [SDK]: §6.3 pop-out polish — клик по токену в длинном справочнике фокусирует и прокручивает `extraArgsInput` перед обновлением превью; `npm run check` ок.
- [J-270] 2026-05-11 21:32:59 [SDK]: §6.3 pop-out preview — `schedulePreviewRefresh()` на input/change для полей rail (tmpl/-f/sub/cookies/impersonate/network/extraArgs/previewOutDirOverride), чтобы превью argv обновлялось без «Сохранить параметры»; `npm run check` ок.
- [J-271] 2026-05-11 21:35:24 [SDK]: §1.1/§4.C DPI 150% — расширен `@144dpi` блок в `main.css` для editor: `app-url-summary`, transport strip и базовые размеры waveform/ruler; `npm run check` ок.
- [J-272] 2026-05-11 21:45:00 [Assistant]: renderer — вынесены панели «История» и «Живой лог» вкладки «Загрузки» из `App.tsx` в компоненты `components/downloads/*` без изменения поведения; `npm run check` ок.
- [J-273] 2026-05-11 22:05:00 [Assistant]: §17/security — централизован guard внешних ссылок `openAllowedExternalUrl`: main/downloads pop-out/inspector открывают через `shell.openExternal` только `http:`/`https:`, `file:`/`javascript:` отбрасываются; добавлен Vitest `external-url`; `npm run check` ок.
- [J-274] 2026-05-11 22:10:00 [Assistant]: §17/security follow-up — добавлен `installExternalNavigationGuard`: main/downloads pop-out/inspector блокируют навигацию текущего Electron-окна на внешние URL и открывают только разрешённые `http(s)` через системный браузер; `check:secrets` встроен в `npm run check`; CI больше не дублирует отдельный secrets-шаг; Vitest расширен, `npm run check` ок.
- [J-275] 2026-05-11 22:13:00 [Assistant]: `package.json` актуализирован под реальный репозиторий `W1ldGodlike/FluxAlloy`: description, `UNLICENSED`, author, homepage, repository, bugs, keywords; `npm run check` ок.
- [J-276] 2026-05-11 22:20:00 [Assistant]: §19/release — добавлены `docs/RELEASE.md` и `docs/BUNDLED_ENGINES_LICENSES.md`; README/bin README ссылаются на релизные документы; `package.json` получил `check:release` и `audit:moderate`; GitHub Actions `ci` теперь делает `npm run build` после `npm run check`; чеклист §19 обновлён. `npm run check` ок.
- [J-277] 2026-05-11 22:24:00 [Assistant]: рабочее правило спринта обновлено по запросу владельца: больше не держать ближайший спринт вокруг v0; работать крупными общими блоками продукта, v0 использовать только как ориентир при необходимых UI-правках; отчёты пользователю — минимальные.
- [J-278] 2026-05-11 22:30:00 [Assistant]: SDK automation prompt hardening — `continue.txt` больше не заставляет каждую итерацию заново читать контракт/v0; контракт/initial/continue переведены на общий продуктовый фокус и обязательный формат журнала `J-NNN`; добавлен `scripts/check-journal-numbering.mjs` и `check:journal` в `npm run check`; README SDK обновлён.
- [J-279] 2026-05-11 22:34:00 [Assistant]: SDK continue-mode уточнён под поведение живого чата `+`: использовать контекст текущего Agent instance, не перечитывать чеклист/журнал/v0 без необходимости, читать хвост журнала только перед новой записью или при риске дублирования; в контракт добавлено требование владельца работать «максимум максимум» за итерацию.
- [J-280] 2026-05-11 22:37:55 [SDK]: §19/§3 — `engines:verify-bundled` + `FLUXALLOY_ENGINES_STRICT`, поля `ffmpeg.exe`/`ffprobe.exe` в `trusted_hashes`, проверки в prepare/engine-download; CI: prepare+verify перед build; win target portable+NSIS; docs RELEASE/licenses; чеклист. `npm run check` ок.
- [J-281] 2026-05-11 22:39:25 [SDK]: §19/§3 — `engines:report-hashes` (+ `--json`), `check:release` с prepare+verify перед build, win target `zip` в electron-builder; RELEASE/чеклист спринта. `npm run check` ок.
- [J-282] 2026-05-11 22:41:05 [SDK]: §19/§3 — CI: кэш `bin/`, шаг `pack:dir`; `npm run pack:dir`; исправлен невалидный корневой `zip:` в electron-builder (zip в `win.target`); bin README + RELEASE; чеклист спринта. `npm run check` ок.
