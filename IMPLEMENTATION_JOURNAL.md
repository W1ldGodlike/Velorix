# FluxAlloy — журнал решений и проверок

Хроника инфраструктурных решений, проверок окружения и заметок, которые не должны раздувать основной **[чек‑лист](IMPLEMENTATION_CHECKLIST.md)**.

**ТЗ [`FLUXALLOY_TZ.md`](FLUXALLOY_TZ.md)** по-прежнему не трогаем без явной договорённости.

## Правило записей

Формат метки времени: **`YYYY-MM-DD HH:mm:ss`** (до секунд).
Формат источника: **`[Assistant]`** — обычный Cursor-чат; **`[SDK]`** — агент из `scripts/cursor-automation`.

Нумерация записей в разделе **«Записи»** ниже: префикс **`[J-NNN]`** сразу после маркера списка (`- [J-042] 2026-…`). Номера идут подряд по мере добавления строк в журнал; **перед дописыванием смотреть последнюю строку раздела и взять следующий номер** (например, после **J-255** идёт **J-256**). При вставке нескольких записей за раз нумеровать по возрастанию без пропусков и без повторов. После **J-999** допускается **J-1000** и четырёхзначные номера.

---

## Записи

- [J-001] 2026-05-09 17:02:00 [Assistant]: повторно проверено окружение перед разработкой. Node/npm/Git доступны через установленный Node.js/Git; `npm install`, `npm run check`, `npm run build`, `npm audit --audit-level=moderate` проходят, уязвимостей `0`.
- [J-002] 2026-05-09 17:02:00 [Assistant]: `dist/`, `out/`, `.eslintcache` — только сгенерированные артефакты проверок/сборки; удалены из рабочей папки после аудита, остаются в `.gitignore`.
- [J-003] 2026-05-09 17:02:00 [Assistant]: `package-lock.json` создан `npm install`; его нужно хранить в Git для воспроизводимой установки, поэтому он больше не игнорируется.
- [J-004] 2026-05-09 17:11:00 [Assistant]: добавлены архитектурные комментарии в `main`, `preload`, `settings-store` и renderer bootstrap. Дальше комментировать чуть подробнее обычного: объяснять назначение модулей, границы IPC, причины проверок/ограничений и неочевидные решения; не пересказывать только совсем очевидный синтаксис.
- [J-005] 2026-05-09 17:17:00 [Assistant]: начат §3. Добавлены `app-paths` и `engine-service`: dev/prod пути ресурсов, bundled/user `bin`, безопасная проверка `ffmpeg`/`ffprobe`/`yt-dlp` через `execFile(..., ['--version'])`, IPC `fluxalloy:engines-status` и краткий статус внизу окна.
- [J-006] 2026-05-09 17:21:00 [Assistant]: выполнен полный проход по комментариям в исходниках и конфигурации. Подробные пояснения добавлены в main/preload/renderer/CSS/build/lint/gitignore/editorconfig; JSON/lockfile не комментируются, потому что формат не поддерживает комментарии.
- [J-007] 2026-05-09 17:26:00 [Assistant]: включён Windows Developer Mode и выдано право `SeCreateSymbolicLinkPrivilege` пользователю `truno`; для текущего процесса право появится после нового входа в Windows/перезапуска сессии.
- [J-008] 2026-05-09 17:28:00 [Assistant]: разрешено оставлять короткие `TODO(...)` прямо в файлах проекта, если уже понятно, что модуль надо доработать позже. Формат: кратко, с привязкой к разделу ТЗ, без длинных планов вместо чек-листа.
- [J-009] 2026-05-09 17:28:30 [Assistant]: правило на будущее — в этом журнале писать дату и время до секунд в формате `YYYY-MM-DD HH:mm:ss`.
- [J-010] 2026-05-09 17:44:44 [Assistant]: после перезагрузки ПК проверено окружение без ручного PATH: `node` v22.22.0, `npm` 11.12.1, `git` 2.54; `whoami /priv` показывает `SeCreateSymbolicLinkPrivilege` (в колонке состояния для этой оболочки — `Disabled`, это нормально для токена до явного enable в процессе).
- [J-011] 2026-05-09 17:44:44 [Assistant]: убран временный `win.signAndEditExecutable: false` из `electron-builder.yml`; `npm run build:unpack` проходит: winCodeSign распаковывается, выполняется `signing with signtool.exe` для `FluxAlloy.exe`.
- [J-012] 2026-05-09 17:46:19 [Assistant]: Git не делал коммит без автора (`Author identity unknown`). Для этого репозитория локально выставлено `git config user.name truno` и `git config user.email truno@local.fluxalloy` (без `--global`). Перед пушем на GitHub лучше заменить почту на свою: `git config user.email "ты@example.com"` (при необходимости поправить и `user.name`).
- [J-013] 2026-05-09 17:54:43 [Assistant]: крупный шаг §3 + §4.B + база §7. Добавлены `fluxmedia`-протокол с allowlist, IPC открытия файла (`dialog`/`grantPath`), DnD через `webUtils.getPathForFile`, `<video>` в превью, прогресс IPC загрузки движков, `extract-zip`, `trusted-hashes-store` и расширенный формат `Data/trusted_hashes.json` (`schema`, `windows-x64`). Отдельное окно первого запуска и macOS/Linux-загрузчики пока не делались — не запускать `prettier .` по репозиторию без исключений: случайно трогает `FLUXALLOY_TZ.md`.
- [J-014] 2026-05-09 20:10:44 [Assistant]: раздел «Журнал решений и проверок» вынесен из `IMPLEMENTATION_CHECKLIST.md` в этот файл (`IMPLEMENTATION_JOURNAL.md`): чек-лист остаётся про статусы по § и спринту, хроника — отдельным потоком.
- [J-015] 2026-05-09 20:13:15 [Assistant]: проверка репозитория на мусор: `dist/`, `out/`, `node_modules` в рабочей копии нет, в Git не трекаются (как и должно). Удалены неиспользуемые заготовки `src/renderer/src/assets/electron.svg` и `wavy-lines.svg` (не импортировались). В `.gitignore` добавлены `Thumbs.db`, `Desktop.ini`, `.idea`, `*.swp`.
- [J-016] 2026-05-09 20:20:25 [Assistant]: связка §7 + §4.1 — `applyTheme` больше не затирает `cachedSettings` целиком; `ffprobe-service` + IPC `fluxalloy:media-probe` (только `isGrantedMediaPath`); таймлайн под `<video>`; `lastOpenedSourcePath` и restore при старте; второе окно `downloads-window` (data HTML-заглушка); поле URL и буфер в UI; сохранение последнего файла после диалога/меню/`grantPath`.
- [J-017] 2026-05-09 20:28:02 [Assistant]: окно yt-dlp — очередь URL в main (`downloads-queue`), второй preload `downloadsWindow.mjs`, меню Ctrl+Shift+V, глобальная вставка ссылки в главном окне; статусбар — первая строка `--version` по каждому движку; `resolvePreloadOutFile` для `.mjs` после electron-vite 5.
- [J-018] 2026-05-09 20:38:48 [Assistant]: добавлен изолированный `scripts/cursor-automation` (`@cursor/sdk`, `npm run loop` / `once`, файлы `prompts/`, стоп-файл `STOP`); root eslint игнорирует эту папку (`node_modules` внутри не линтим).
- [J-019] 2026-05-09 20:42:28 [Assistant]: окно yt-dlp — реальный spawn yt-dlp (`ytdlp-download-service`, без shell), очередь последовательная, колонки прогресс/имя, IPC-снимок очереди без reload страницы; вывод в `userData/downloads/ytdlp`; отмена через kill дочернего процесса.
- [J-020] 2026-05-09 21:05:36 [SDK]: §3 — `engineExecutablePaths` в `settings.json`, приоритет резолва override → bundled → user bin; IPC выбора exe и сохранения; минимальный UI (кнопка + модалка) и пункт меню «Настройки → Пути к движкам»; снапшот для `downloads-queue-runner`, ffprobe и статуса движков.
- [J-021] 2026-05-09 21:06:43 [SDK]: §6.1 — `startDownloadSingleRow`, IPC `fluxalloy-downloads-start-row`, кнопка «▶» для строк «Ожидание»; общий флаг занятости с «Старт очереди».
- [J-022] 2026-05-09 21:08:21 [SDK]: §7.1 — маркеры In/Out у превью: индикатор диапазона, кнопки «In/Out здесь» и «Весь клип», синхронизация границ при появлении duration; смена файла — remount `VideoTimeline` по `key`.
- [J-023] 2026-05-09 21:12:26 [SDK]: §7.1 — базовый экспорт в MP4 (`ffmpeg-export-service`: spawn + массив аргументов, libx264/aac); диалог сохранения, маркеры через `-ss`/`-t`; IPC `export-start`/`export-cancel`/`export-progress` и строка прогресса в статусбаре.
- [J-024] 2026-05-09 21:14:45 [SDK]: §4.1 — `windowBounds.main/downloads` в `settings.json`, debounced persist при resize/move и flush на `close`; восстановление через `rectifyBoundsForRestore` (fallback на центр основного монитора).
- [J-025] 2026-05-09 21:19:17 [SDK]: §9 — расширен `ffprobe-service` (битрейт, `tracks`, tooltip формата) и UI под превью; §19 — `bin/.gitkeep` + `extraResources` для пустого `resources/bin` в сборке.
- [J-026] 2026-05-09 21:20:42 [SDK]: §4.5 — меню «Справка → О программе», IPC `app-about-info`, модалка; §9 — `rawJson` в probe, блок JSON + «Копировать», IPC `clipboard-write-text` (лимит 24 MiB).
- [J-027] 2026-05-09 21:22:11 [SDK]: §6.4 — `downloads-log-ipc.ts`, стрим строк yt-dlp в окно загрузок (`onLog`, авто-раскрытие `<details>`, лимит буфера в UI).
- [J-028] 2026-05-09 21:23:40 [SDK]: §7.6 — `ffmpeg-frame-snapshot-service`, IPC `snapshot-frame`, кнопка «Кадр»; §4.2 — диалог закрытия главного окна при экспорте или активной загрузке yt-dlp.
- [J-029] 2026-05-09 21:26:24 [SDK]: §6.2 — настраиваемый каталог yt-dlp (`ytdlp-download-output.ts`, поле `ytdlpDownloadDirectory` в settings, UI «Выбрать…»/«По умолчанию», IPC в окне загрузок).
- [J-030] 2026-05-09 21:38:31 [SDK]: §6.1/§6.4 — парсинг stderr yt-dlp: колонка «Прогресс» — процент, скорость и ETA (`parseYtdlpDownloadProgressLine`, `formatYtdlpProgressCell`); разметка ячейки в окне загрузок под длинные строки.
- [J-031] 2026-05-09 21:41:13 [SDK]: §6.2 — сохраняемый шаблон `-o` (`ytdlpFilenameTemplate`) и пресеты `-f` (`ytdlpFormatPreset`); IPC/preload `getCliOptions`/`setCliOptions`; `resolveSafeYtdlpOutputPattern`; снимок опций для runner (`ytdlp-run-options-sync`).
- [J-032] 2026-05-09 21:42:07 [SDK]: §7.1 — прогресс экспорта ffmpeg: парсинг `speed=`, склейка stderr по `\r`/`\n`; статусбар показывает множитель после процента (`FfmpegExportProgressPayload.speed`).
- [J-033] 2026-05-09 21:43:43 [SDK]: §7.2 — три системных пресета libx264 (`balance`/`smaller`/`quality`), `ffmpegExportEncodePreset` в settings, `<select>` в тулбаре, IPC `settings-set-ffmpeg-export-encode-preset`, передача в `runFfmpegExportJob`.
- [J-034] 2026-05-09 21:45:14 [SDK]: §6.2 — плейлист (`ytdlpDownloadPlaylist` → `--yes-playlist`/`--no-playlist`) и только аудио (`ytdlpAudioOnly` → `-x --audio-format best`, без `-f` пресета); чекбоксы в окне загрузок, расширен снимок `YtdlpRunOptionsSnapshot` и `runYtdlpOnce`.
- [J-035] 2026-05-09 21:48:01 [SDK]: §6.3 — `ytdlpExtraArgsLine` + `parseExtraYtdlpArgsLine`/`buildYtdlpSpawnArgvTokens`, превью `yt-dlp …` в окне загрузок; блок конфликта `-o`/batch/`@`; раннер берёт `extraArgs` из снимка.
- [J-036] 2026-05-09 21:49:21 [SDK]: §6.3 — подсказки из `Data/ytdlp_commands.json`: `getYtdlpCommandHints` в main, `commandHints` в payload окна загрузок, сворачиваемый справочник и вставка токена в поле доп. argv с кратким описанием выбранного флага.
- [J-037] 2026-05-09 21:51:30 [SDK]: §6.2 — пресет субтитров (`ytdlpSubtitlePreset`/`ytdlpSubLangs` в settings), argv через `buildYtdlpSpawnArgvTokens`, UI в окне загрузок и раннер очереди.
- [J-038] 2026-05-09 21:54:00 [SDK]: §6.2 — cookies: `ytdlpCookiesFile`/`ytdlpCookiesBrowser`, диалог выбора файла в окне загрузок, приоритет файла над браузером; запрет дублей `--cookies*` в доп. argv.
- [J-039] 2026-05-09 21:55:39 [SDK]: §6.2 — `--impersonate`: whitelist chrome/edge/firefox (`ytdlpImpersonate`), сбор argv и окно загрузок; запрет дубля `--impersonate` в доп. argv.
- [J-040] 2026-05-09 21:59:33 [Assistant]: ревью SDK-изменений: `npm run check` и `npm run build` проходят; найден и закрыт риск в доп. аргументах yt-dlp — запрещены runtime-флаги вроде `--exec`, внешние downloader/postprocessor/config/plugin пути и `-P/--paths`.
- [J-041] 2026-05-09 22:12:07 [Assistant]: follow-up после ревью SDK: прогресс yt-dlp теперь парсится из stdout и stderr через общий обработчик; ручные пути движков сохраняются только как существующие абсолютные файлы; главное окно получило `minWidth/minHeight`, таймер bounds-персиста локализован на окно.
- [J-042] 2026-05-09 22:21:20 [Assistant]: §6.2 — добавлены безопасные настройки yt-dlp для `--limit-rate` и `--retries`: поля UI в окне загрузок, persist в `settings.json`, включение в preview argv и spawn; дубли `--limit-rate`/`-r`/`--retries` в доп. argv запрещены.
- [J-043] 2026-05-09 22:29:58 [Assistant]: §6.2/§17 — в окне yt-dlp добавлена кнопка «Открыть» для текущего каталога загрузок через guarded IPC и `shell.openPath`; «Ближайший TODO спринта» сжат до актуальных 6 ориентиров.
- [J-044] 2026-05-09 22:34:15 [SDK]: §17/§18 — добавлен `diagnostics-paths` (whitelist userData/logs/ytdlp/bin/resources), меню «Инструменты → Открыть папку…», IPC `fluxalloy:diagnostics-list-folders`/`open-folder` и preload `fluxalloy.diagnostics`; каталог `userData/logs` гарантированно создаётся при первом обращении как заготовка под §18.
- [J-045] 2026-05-09 22:39:44 [SDK]: §6.4 — профили повтора строки очереди (`ytdlpQueueRetryProfile` + UI), паузы с отменой по тому же AbortSignal; `extractYtdlpErrorSummary` для статуса; раннер делает до 3 запусков на URL при ненулевом exit.
- [J-046] 2026-05-09 22:43:44 [SDK]: §18 — добавлен `logger-service` (`userData/logs/main.log`, scope/level/timestamp, ротация 1 MiB → `main.log.1`, `uncaughtException`/`unhandledRejection`); IPC `fluxalloy:log-renderer` + `window.fluxalloy.log.send`; renderer-перехват `error`/`unhandledrejection`; `console.error` в раннере очереди заменён на `logError`.
- [J-047] 2026-05-09 22:50:11 [SDK]: §21 — подключён Vitest (+`tsconfig.tests.json`, `npm run test`/`test:watch`); чистые парсеры yt-dlp вынесены в `ytdlp-progress-parser.ts`; 62 теста для `ytdlp-extra-args`/`ytdlp-progress-parser`/`ytdlp-queue-retry`; найден и исправлен обход whitelist `-P` в доп. argv yt-dlp (toLowerCase ломал сравнение).
- [J-048] 2026-05-09 22:55:59 [SDK]: §6.4 — `ytdlp-download-history.ts` + запись из раннера после фактического `runYtdlpOnce`; IPC `fluxalloy-downloads-get/clear-history`, preload, сворачиваемая таблица в окне загрузок; тесты `outcomeFromQueueStatus`.
- [J-049] 2026-05-09 23:12:19 [Assistant]: ревью SDK-коммитов `c604794..2ce01e1`: исправлены метки журнала на `[SDK]`, спринт-блок снова приведён к 6 пунктам; история yt-dlp теперь ставит флаг записи через `runYtdlpOnce.onStarted`, то есть только после создания child process, а не до валидационных ошибок.
- [J-050] 2026-05-09 23:23:14 [Assistant]: §6.3/§21 — закрыт обход blacklist через glued short-options yt-dlp (`-Ptmp`, `-a/list`, `-oX`, `-r5M`) и добавлены Vitest-кейсы для этих форм.
- [J-051] 2026-05-09 23:26:16 [Assistant]: §18 — renderer log IPC закреплён за main window, получил token bucket rate limit и очистку control chars в `scope/message`; `attachProcessErrorHandlers()` перенесён на ранний старт main до `app.whenReady`.
- [J-052] 2026-05-09 23:28:03 [Assistant]: §6.4/§21 — запись и очистка `downloads/history.json` переведены на атомарный temp+rename; тесты истории расширены сценариями append/read newest-first и clear с валидной схемой.
- [J-053] 2026-05-09 23:31:11 [Assistant]: §17 — меню «Инструменты → Открыть папку…» пересобирается при фокусе окна и после изменения диагностически важных путей (`ytdlpDownloads`, engine bin), чтобы `enabled` отражал актуальное существование каталогов.
- [J-054] 2026-05-09 23:33:23 [Assistant]: §21 — добавлены unit-тесты `ytdlp-download-options`: шаблон имени/безопасный output pattern, нормализация `--limit-rate`, границы `--retries`; Electron-зависимость справочника yt-dlp замокана в тесте.
- [J-055] 2026-05-09 23:38:26 [Assistant]: §6.4 — в таблицу истории загрузок добавлено действие «В очередь»: URL из завершённой записи можно повторно добавить в очередь без автозапуска скачивания.
- [J-056] 2026-05-09 23:39:38 [Assistant]: §6.4 — история загрузок получила локальный фильтр по исходу (`Все`/`Успех`/`Ошибка`/`Отмена`), без изменения формата `history.json` и IPC.
- [J-057] 2026-05-09 23:43:40 [Assistant]: §6.2/§6.4 — добавлено отдельное поле `--fragment-retries` (settings/UI/IPC/snapshot/spawn/preview), запрет дубля в доп. argv оставлен; тесты yt-dlp аргументов и опций расширены.
- [J-058] 2026-05-09 23:45:44 [Assistant]: §6.4 — в окне загрузок добавлено сохранение видимого stdout/stderr лога yt-dlp через guarded IPC + save dialog; renderer передаёт только текущий обрезанный буфер.
- [J-059] 2026-05-09 23:49:11 [Assistant]: §6.4 — для завершённых/ошибочных/отменённых строк очереди добавлено действие retry `↻`: строка сбрасывается в `Ожидание` и запускается повторно как одиночная загрузка.
- [J-060] 2026-05-09 23:54:37 [Assistant]: §6.1/§6.3/§21 — экспертные аргументы yt-dlp свернуты в отдельный `details`; очередь получила фильтр по статусу и очистку завершённых строк; добавлен unit-тест `downloads-queue` на cleanup.
- [J-061] 2026-05-09 23:56:27 [Assistant]: §6.1 — над таблицей очереди добавлена живая сводка статусов (всего/ожидание/в работе/готово/ошибки/отмена), обновляется вместе со snapshot.
- [J-062] 2026-05-09 23:57:53 [Assistant]: §21 — добавлены unit-тесты `settings-store` для persisted yt-dlp полей (`ytdlpRateLimit`, `ytdlpRetries`, `ytdlpFragmentRetries`, queue retry profile) и сохранения settings.json.
- [J-063] 2026-05-09 23:59:33 [Assistant]: §7/§21 — pure helpers ffmpeg export (`parseFfmpegTimeSeconds`, `parseFfmpegSpeedToken`) экспортированы и покрыты unit-тестами вместе с encode preset/segment duration.
- [J-064] 2026-05-10 00:05:53 [Assistant]: §6.1/§6.4 — очередь и история yt-dlp сохраняют best-effort `outputPath` из stdout/stderr (`Destination`, merge, already downloaded); добавлены guarded IPC-действия открыть файл/показать в папке и тесты парсера/модели.
- [J-065] 2026-05-10 00:10:59 [Assistant]: §18/§21 — добавлены действия диагностики `Открыть main.log` и `Собрать Support ZIP`, диалог необработанной ошибки с копированием деталей/логом/архивом; ZIP builder покрыт unit-тестами.
- [J-066] 2026-05-10 00:18:13 [Assistant]: §18/§21 — stdout/stderr внешних процессов (`yt-dlp`, `ffmpeg`, `ffprobe`) пишутся в `main.log` через sanitizer без полного argv; на старте добавлен prune старых crash dumps с сохранением последних 20.
- [J-067] 2026-05-10 00:28:22 [Assistant]: §6.4/§7/§21 — из очереди и истории yt-dlp добавлено действие «В обработчик» для открытия готового файла в главном preview; экспорт получил фильтры MP4/MKV/MOV и нормализацию расширения.
- [J-068] 2026-05-10 00:32:53 [Assistant]: §7.2/§21 — контейнер экспорта MP4/MKV/MOV вынесен в toolbar и persisted settings; export-start возвращает путь результата, UI показывает имя сохранённого файла.
- [J-069] 2026-05-10 00:36:32 [Assistant]: §7.2/§21 — добавлены persisted настройки CRF libx264 и AAC bitrate в toolbar/settings/preload; ffmpeg export принимает валидированные overrides без shell-строк.
- [J-070] 2026-05-10 00:40:30 [Assistant]: §7.2/§21 — добавлены persisted настройки FPS и scale preset (исходный/480p/720p/1080p) для ffmpeg export; фильтры собираются через `-vf` без shell-строк, тесты settings/export helpers расширены.
- [J-071] 2026-05-10 00:44:44 [Assistant]: §7.2/§21 — добавлен persisted video bitrate mode для экспорта: при выборе bitrate ffmpeg получает `-b:v`, без него остаётся CRF mode; preload/settings/UI и unit-тесты расширены.
- [J-072] 2026-05-10 00:47:47 [Assistant]: §7/§21 — после успешного экспорта main запоминает результат в allowlist и renderer показывает действия «Файл экспорта»/«Папка экспорта»; IPC не открывает произвольные пути.
- [J-073] 2026-05-10 00:50:13 [Assistant]: §7/§21 — результат экспорта можно вернуть в preview через кнопку «В превью»; main использует тот же allowlist export output и существующий `grantMediaPath`.
- [J-074] 2026-05-10 00:52:27 [Assistant]: §7/§21 — toolbar получил отмену активного ffmpeg export через существующий `export.cancel`; отмена возвращается как `cancelled`, а для последнего результата добавлено копирование пути.
- [J-075] 2026-05-10 00:55:11 [Assistant]: §7/§21 — успешный ffmpeg export теперь запоминает последнюю папку результата в settings; следующий save dialog использует её как defaultPath, если каталог ещё существует.
- [J-076] 2026-05-10 00:57:45 [Assistant]: §7.6/§21 — снимок кадра теперь возвращает путь результата, запоминает последнюю папку кадра и даёт toolbar-действия открыть файл/папку/скопировать путь.
- [J-077] 2026-05-10 01:01:46 [Assistant]: §7.2/§21 — экспорт получил persisted audio mode `AAC`/`Без аудио`; при `none` ffmpeg получает `-an`, а AAC bitrate в toolbar отключается.
- [J-078] 2026-05-10 01:10:11 [Assistant]: §7.6/§21 — снимок кадра получил persisted формат `PNG`/`JPEG` в toolbar; save dialog стартует с нужным расширением, путь без расширения нормализуется helper-ом, settings/snapshot helpers покрыты тестами.
- [J-079] 2026-05-10 01:16:21 [SDK]: §21 — реестр IPC `src/shared/ipc-channels.ts`, main/preload на общих константах, Vitest на уникальность строк каналов.
- [J-080] 2026-05-10 01:18:24 [SDK]: §21 — `src/shared/ffmpeg-export-contract.ts`: типы экспорта ffmpeg для preload/main, сервис реэкспортирует из shared.
- [J-081] 2026-05-10 01:22:21 [SDK]: §21 — shared-контракты yt-dlp окна/лога/истории, диагностики, прогресса движков, формата снимка; main реэкспортирует, preload без импортов типов из main по этим доменам.
- [J-082] 2026-05-10 01:25:00 [SDK]: §21 — `ffprobe-contract`, `settings-contract`, `engine-contract`, `about-contract`, `preview-dialog-contract`; main реэкспортирует, главный preload импортирует типы только из `src/shared`.
- [J-083] 2026-05-10 01:26:59 [SDK]: §21 — `noImplicitAny` в tsconfig node/web/tests; константа `ENGINE_IDS` в `engine-contract`, main/renderer на общем списке; Vitest `tests/shared/engine-contract`.
- [J-084] 2026-05-10 01:28:36 [SDK]: §21 — `noUncheckedIndexedAccess` в tsconfig node/web/tests; точечные правки main и Vitest под безопасный доступ по индексу.
- [J-085] 2026-05-10 01:30:06 [SDK]: §21 — `exactOptionalPropertyTypes` в tsconfig node/web/tests; экспорт IPC передаёт `trim` только если задан (main/renderer).
- [J-086] 2026-05-10 01:32:06 [SDK]: §21 — `noPropertyAccessFromIndexSignature`; bracket-доступ для `Record<string, unknown>` в main и для `dataset`/версий в renderer.
- [J-087] 2026-05-10 01:33:13 [SDK]: §21 — явный `useUnknownInCatchVariables` в tsconfig node/web/tests (политика catch уже совместима; без изменений в логике).
- [J-088] 2026-05-10 01:35:57 [SDK]: §6.4 — статус неуспеха очереди: `formatYtdlpQueueFailureStatus` (сигнал ОС, stderr fallback); Vitest.
- [J-089] 2026-05-10 02:08:37 [SDK]: §6.4 — «Скачать и сразу обработать»: `ytdlpOpenInHandlerOnComplete` в settings, чекбокс в окне загрузок, после успеха yt-dlp — авто-открытие в preview через общий guard пути; тесты `resolveAllowedYtdlpDownloadOutputFile` и snapshot.
- [J-090] 2026-05-10 02:11:06 [SDK]: §6.3 — справочник yt-dlp: карта категорий токенов в main, сортировка списка, `<optgroup>` в окне загрузок; опциональный `category` в JSON переопределяет категорию; Vitest `sortYtdlpCommandHintsForUi`.
- [J-091] 2026-05-10 02:15:15 [SDK]: §6.3 — превью argv yt-dlp: контекст с реальным каталогом `-o`, первый URL из очереди или `example.com`; debounce обновления превью при снимке очереди; Vitest `sanitizeYtdlpPreviewUrl`/`payloadFromSnapshot`.
- [J-092] 2026-05-10 02:16:39 [SDK]: §6.4 — профиль повтора очереди `persistent`: 3 дополнительных запуска с паузами 5/15/45 с; типы settings/контракт/UI/select; Vitest `ytdlp-queue-retry`.
- [J-093] 2026-05-10 02:18:06 [SDK]: §6.4 — `shouldSkipYtdlpQueueRetriesAfterFailure`: при маркерах stderr не делать дальнейшие повторы очереди; Vitest; лог `[FluxAlloy]` в раннере.
- [J-094] 2026-05-10 02:21:04 [SDK]: §6.4 — пауза/продолжить yt-dlp: SIGSTOP/SIGCONT в сервисе, IPC + кнопка в окне загрузок; Windows отключено; `ytdlp-os-pause-support` + Vitest.
- [J-095] 2026-05-10 02:22:44 [SDK]: §6.4 — `classifyYtdlpQueueFailureKind`, транзиентные маркеры с приоритетом над «не повторять»; парсинг `fragment X of Y` в прогресс; реэкспорт из `ytdlp-download-service`.
- [J-096] 2026-05-10 02:24:10 [SDK]: §6.4 — статус ошибки строки очереди: суффиксы «вероятно сеть» / «отказ источника» через `formatYtdlpQueueFailureStatus(..., failureKind)`; раннер передаёт `classifyYtdlpQueueFailureKind`; Vitest.
- [J-097] 2026-05-10 02:26:20 [SDK]: §6.4 — коды выхода yt-dlp 2/100/101 в `classifyYtdlpQueueFailureKind`; `shouldSkipQueueRetriesForFailureKind`; суффиксы статуса; раннер передаёт exitCode в классификацию.
- [J-098] 2026-05-10 02:27:17 [SDK]: §6.4 — парсер прогресса: `Total progress:`, `Downloading video|item X of Y` (плейлист); Vitest.
- [J-099] 2026-05-10 02:33:05 [Assistant]: ревью SDK-изменений после `9d0a36f`: метки журнала после 01:10:11 корректно `[SDK]`, `npm run check` проходит; исправлен пропуск persisted `ytdlpOpenInHandlerOnComplete` в `loadSettings`, чтобы флаг авто-открытия yt-dlp в обработчике переживал рестарт.
- [J-100] 2026-05-10 02:37:22 [Assistant]: follow-up §6.4 после ревью: авто-открытие yt-dlp в обработчике теперь пишет причину пропуска/ошибки в лог строки; `Total progress` имеет приоритет над fragment-строкой в парсере прогресса; чек-лист уточнён по POSIX-паузе.
- [J-101] 2026-05-10 02:42:24 [Assistant]: follow-up §21 после ревью shared IPC: `restoreLastSource` получил честный shared-тип `RestoredSourceInfo`, renderer импортирует ffmpeg/snapshot unions из `src/shared`, убран shadowing `d` в downloads IPC, чек-лист тестов синхронизирован.
- [J-102] 2026-05-10 02:46:24 [Assistant]: аудит чек-листа по вопросу пользователя: исправлены устаревшие статусы/формулировки про отсутствие ffmpeg, «заглушку» yt-dlp окна, живой yt-dlp, сохранение папок, системные пресеты и частично вынесенные shared-модели.
- [J-103] 2026-05-10 02:59:02 [SDK]: §7.2/§21 — `src/shared/ffmpeg-export-argv.ts` (pure `buildFfmpegExportArgv`/`buildFfmpegExportPreviewCommand`/format helpers) с реэкспортом из main; live preview команды ffmpeg в App.tsx (collapsible `<details>` + копирование) и Vitest на shared.
- [J-104] 2026-05-10 03:05:51 [SDK]: §7.2/§21 — `shouldApplyFfmpegExportTrim` вынесен в shared и переиспользован в main-сервисе; preview команды ffmpeg в App.tsx подмешивает In/Out маркеры и `probeDurationSec` (повторяет логику spawn), показывает `-ss/-t` или подсказку о почти полном клипе; Vitest расширен на shared trim-логику.
- [J-105] 2026-05-10 03:25:34 [SDK]: §6.3 — превью argv yt-dlp: IPC `getCliOptions` принимает опциональный override каталога `-o` и черновик полей (`draft`) без записи settings; при снимке очереди обновляется только превью/предупреждение парсера, форма не сбрасывается; `mergeYtdlpDownloadCliPatchOntoSettings` + тесты нормализации/override.
- [J-106] 2026-05-10 03:27:12 [SDK]: §6.4 — `ytdlp-progress-parser`: прогресс «N of M videos», транзиентные маркеры (500/504, broken pipe, read timed out…), пути из строк Writing thumbnail/subtitles; расширены Vitest.
- [J-107] 2026-05-10 03:28:44 [SDK]: §6.4 — `extractYtdlpOutputPath`: `[ffmpeg] Destination`/merge, `[EmbedSubtitle] Embedding…`, `[Metadata] Writing metadata to`; tests Vitest.
- [J-108] 2026-05-10 03:31:03 [SDK]: §18 — сессионный `logs/session.log` (дубликат строк с main), архив прошлого запуска в `logs/sessions/session-*.log` + prune; Support ZIP и меню «Открыть session.log».
- [J-109] 2026-05-10 03:32:59 [SDK]: §7.2/§21 — `buildFfmpegExportArgv` учитывает контейнер (MKV без `-movflags +faststart`, MP4/MOV — с faststart); preview и `runFfmpegExportJob` пробрасывают `container`; Vitest на mkv/mp4 хвост argv и preview.
- [J-110] 2026-05-10 03:34:52 [SDK]: §6.4 — `extractYtdlpOutputPath`: строки FFmpeg PP (`…; Destination:`, общий `[PP] Destination:`), EmbedThumbnail (`exe: Adding thumbnail to`), `Adding metadata to`, Concat Moving/to и Concatenate…Destination; Vitest.
- [J-111] 2026-05-10 03:36:04 [SDK]: §6.4 — прогресс: `(frag N/M)` без `%`, Sleeping/Waiting for reconnect; маркеры retry: гео/bad gateway/service unavailable/connection lost; Vitest.
- [J-112] 2026-05-10 03:39:59 [SDK]: §7.2/§21 — пользовательские пресеты экспорта: типы/snapshot в shared, `ffmpegExportUserPresets` в settings + парсер списка; IPC set list / apply snapshot; тулбар App.tsx (+ превью ffmpeg); Vitest merge/snapshot/list.
- [J-113] 2026-05-10 03:41:34 [SDK]: §7.1 — `runFfmpegExportJob`: фильтр строк для IPC прогресса (`isFfmpegExportProgressStatusLine`), сброс хвоста stderr на `close`; Vitest.
- [J-114] 2026-05-10 03:42:42 [SDK]: §7.2 — пользовательские пресеты: кнопки «Имя…» (переименование) и «Обновить пресет» (перезапись снимка из тулбара в выбранный слот).
- [J-115] 2026-05-10 10:55:39 [SDK]: §4.5/§17/§18 — «О программе»: папка логов, main.log, Support ZIP через IPC; результат диалога ZIP различает отмену и ошибку.
- [J-116] 2026-05-10 10:57:13 [SDK]: §6.4 — прогресс «Waiting for …», маркеры transient (408/premature close) и skip live/premiere; `extractYtdlpOutputPath`: VideoRemuxer `into`, `→ Destination`; Vitest.
- [J-117] 2026-05-10 10:58:31 [SDK]: §6.4 — `extractYtdlpOutputPath`: ASCII `-> Destination`, SubtitlesConvertor `->`/`into`; классификация: transient signature/rate limit, skip DRM protected; Vitest.
- [J-118] 2026-05-10 10:59:54 [SDK]: §6.3 — окно yt-dlp: превью argv после выбора/сброса каталога загрузки, вставки флага из справочника, «Шаблон по умолчанию» (`schedulePreviewRefresh`).
- [J-119] 2026-05-10 11:01:25 [SDK]: §6.4 — прогресс `Resuming download at byte…`; skip повторов: нет форматов / unsupported URL / requested format…; `extractYtdlpOutputPath`: `[download] Writing metadata to:`; Vitest.
- [J-120] 2026-05-10 11:02:42 [SDK]: §6.1 — окно yt-dlp: DnD `text/plain`/`text/uri-list` на document.body → очередь; не перехватываем drop на textarea/select и текстовых input параметров §6.2.
- [J-121] 2026-05-10 11:07:48 [SDK]: §9 — сохранение форматированного JSON ffprobe в файл: `save-text-dialog-contract`, IPC `saveTextWithDialog`, кнопка «Сохранить JSON…» под превью.
- [J-122] 2026-05-10 11:09:18 [SDK]: §9 — экспорт сводки инспектора TXT/HTML (`ffprobe-summary-export`), UI + фильтр HTML в диалоге сохранения; Vitest.
- [J-123] 2026-05-10 11:11:25 [SDK]: §9 — главы: `-show_chapters`, `chapters` в `MediaProbeSuccess`, парсер `ffprobe-chapters`, таблица в превью и блок в TXT/HTML сводке; Vitest.
- [J-124] 2026-05-10 11:13:22 [SDK]: §9 — контекстное меню строк дорожек/глав: портал, копирование TSV/полей в буфер (preload clipboard).
- [J-125] 2026-05-10 11:15:02 [SDK]: §9 — таблица дорожек: `language`/`titleTag` из ffprobe tags, колонки в UI и TXT/HTML сводке; расширен TSV и ПКМ.
- [J-126] 2026-05-10 11:16:20 [SDK]: §9 — битрейт дорожки из `stream.bit_rate` (`streamBitrateKbps`), колонка и ПКМ; сводка TXT/HTML; §367 JSON блок отмечен готовым в чеклисте.
- [J-127] 2026-05-10 11:19:50 [SDK]: §9 — `dispositionSummary` из `stream.disposition` (`ffprobe-disposition`), колонка/TSV/сводка/ПКМ; Vitest.
- [J-128] 2026-05-10 11:22:13 [SDK]: §9 — видео `pix_fmt`/SAR/DAR из ffprobe в дорожках, колонки UI/ПКМ/TSV и TXT/HTML сводка; Vitest.
- [J-129] 2026-05-10 11:28:40 [SDK]: §9 §363 — отдельное окно инспектора `#inspector`: `inspector-window.ts`, IPC (`openInspectorWindow`, bootstrap, push пути), `windowBounds.inspector`, меню «Инструменты», `MediaProbePanel` + toolbar «Инспектор…».
- [J-130] 2026-05-10 11:31:22 [SDK]: §9 — цветовые поля видеопотока ffprobe (`color_space`, `color_primaries`, `color_transfer`/`color_trc`, `color_range`) в `MediaProbeTrackRow`, таблице/ПКМ/TXT/HTML сводке; Vitest.
- [J-131] 2026-05-10 11:39:45 [SDK]: §7.2 — экспорт ffmpeg: whitelist поворот/зеркало (`transpose`/`hflip`/`vflip`) в `-vf` до scale/fps; `ffmpegExportVideoTransform`, IPC, пользовательские пресеты и live preview; `npm run check` ок.
- [J-132] 2026-05-10 11:59:45 [Assistant]: Документация архитектуры и точек входа: `docs/ARCHITECTURE.md`; ссылки в `README.md` и `.cursor/rules/fluxalloy-project.md`. Удалены: дублирующее правило `.cursor/rules/fluxalloy.md`, неиспользуемый `ipcMain.on('ping')`, локальный `scripts/cursor-automation/STOP` (мешал циклу SDK).
- [J-133] 2026-05-10 12:02:33 [Assistant]: ревью SDK/соседнего агента: `openInspectorWindow` больше не выдаёт доступ к произвольному строковому пути из renderer, а принимает только уже выданные main пути; восстановлен `STOP=1/0` для Cursor SDK automation; docs/journal приведены к формату.
- [J-134] 2026-05-10 12:10:45 [Assistant]: follow-up по ревью сабагентов: bootstrap отдельного инспектора устойчив к React StrictMode double-mount; draft-превью yt-dlp не падает на нестроковых полях; сводки ffprobe не пишут отрицательные длительности глав; уточнён regex playlist-прогресса.
- [J-135] 2026-05-10 12:18:31 [Assistant]: §7.2 — crop-пресеты экспорта ffmpeg (`none`/1:1/16:9/4:3) добавлены через whitelist `-vf` после rotate/flip и до scale/fps; settings/preload/main/renderer, пользовательские пресеты, live preview и Vitest синхронизированы.
- [J-136] 2026-05-10 12:30:37 [Assistant]: UI/UX follow-up по скриншоту: toolbar главного окна переносит элементы и не раздувает ширину; меню различает main/downloads/inspector и отключает рекурсивное открытие дочерних окон; downloads window получил минимальный размер.
- [J-137] 2026-05-10 12:34:03 [Assistant]: §4.C/UI — стартовый размер главного окна изменён на FHD 1920x1080; ТЗ и чек-лист синхронизированы, сохранённые пользователем bounds продолжают иметь приоритет.
- [J-138] 2026-05-10 12:54:49 [Assistant]: UI/UX reference pass: v0-референс проверен через Playwright в состояниях «Редактор» и «Загрузки»; первый emergent-референс на момент проверки отдаёт пустой/Loading экран. В главном renderer начат перенос идеи v0 без копирования: почти чёрная монтажная палитра, компактный topbar с вкладками, FFmpeg controls вынесены из toolbar в правую сворачиваемую панель, quick yt-dlp URL свёрнут в details.
- [J-139] 2026-05-10 13:13:01 [Assistant]: §6/UI — окно загрузок перестроено ближе к v0: topbar с вкладкой «Загрузки», input band + action buttons, таблица очереди с progress bar/status dot/icon-only actions, нижние панели history/log и правый settings rail с секциями «Формат»/«Метаданные»/«Сохранение»/«Сеть»/«Экспертные argv». IPC/runner не менялись, только HTML/CSS/renderer-логика второго окна.
- [J-140] 2026-05-10 13:15:14 [Assistant]: UI/UX reference закреплён для SDK: добавлен [`docs/UX_REFERENCE_V0.md`](docs/UX_REFERENCE_V0.md) с картой v0 layout/icons/sections/click behavior; `IMPLEMENTATION_CHECKLIST.md` теперь прямо ссылается на этот документ в правилах ближайшего спринта и UI TODO.
- [J-141] 2026-05-10 13:17:20 [Assistant]: Cursor SDK automation теперь получает v0/UI как первый обязательный приоритет в `agent-contract.txt`, `initial.txt` и `continue.txt`: перед остальными задачами агент читает `docs/UX_REFERENCE_V0.md` и продолжает незакрытые UI/downloads/icons/sections пункты.
- [J-142] 2026-05-10 13:22:17 [Assistant]: SDK STOP-флаг стал самовосстанавливаемым: `run-loop` создаёт локальный игнорируемый `scripts/cursor-automation/STOP` со значением `0`, если файла нет; README уточняет это поведение.
- [J-143] 2026-05-10 13:28:23 [SDK]: §1.1/§4.1/v0 — топбар: lucide-style SVG; persist раскрытия FFmpeg/yt-dlp bar и секций загрузок в settings + IPC; Vitest whitelist `mainWindowUiPanels`/`downloadsWindowUiPanels`; статусы чеклиста §4.1/§4.C/спринт.
- [J-144] 2026-05-10 13:32:20 [SDK]: §6/v0 — таблица очереди загрузок: inline SVG icon-only действий; снимок очереди дополняется `isActiveRunner` и pause-state для кнопок паузы/отмены на активной строке (POSIX-child); спринт/§6.1/§4.C обновлены.
- [J-145] 2026-05-10 13:35:37 [SDK]: §1.1/§4.1/§6 — история загрузок: те же icon-only кнопки (`act-icons`); хелперы SVG/`RowIco` подняты выше `renderHistoryEntries`. Главное окно: persist `details` ffprobe под превью (`probe*` в `mainWindowUiPanels` + `PreviewProbeBody`); Vitest whitelist и чеклист §4.1/спринт.
- [J-146] 2026-05-10 13:39:31 [SDK]: §6/v0 — таблица очереди: колонки Формат/Размер/Скорость/ETA + прогресс (bar + первая часть `progress`); поля `DownloadsQueueRow.queue*` из `parseYtdlpDownloadProgressLine` (`sizeTotal`) и `parseYtdlpInfoFormatSnippet`; очистка меты при старте загрузки/retry; Vitest; чеклист §6.1/спринт.
- [J-147] 2026-05-10 13:42:30 [SDK]: §4.1/§6 — персист живой очереди yt-dlp: `userData/downloads/queue.json` (атомарная запись, гидратация при регистрации IPC, debounce + `will-quit` flush); сброс статуса «Загрузка…»/пауз повтора в «Ожидание» при восстановлении; парсер без Electron в `ytdlp-download-queue-persist-parse` + Vitest.
- [J-148] 2026-05-10 13:43:36 [SDK]: §1.1/v0 — окно загрузок плотнее: базовый шрифт ~11.5px, topbar ~54px, уже input band/очередь/таблица/rail/log; прогресс 3px; icon-only 24×24; monospace для URL-текстареа; нижние панели короче; `focus-visible` на кнопках/textarea.
- [J-149] 2026-05-10 13:46:08 [SDK]: §6/v0 таблица — доп. эвристики колонок: `parseYtdlpQueueFormatHint` (`[info]` регистронезависимо, «video in format», `[Merger]`/`[ffmpeg]` → «слияние → ext»), `parseYtdlpInfoQueueSizeHint` для Filesize до прогресса; runner заполняет `queueFmt`/`queueSize` без перезатира уже известного размера из `[download]`; Vitest и чеклист §6.1/спринт.
- [J-150] 2026-05-10 13:51:33 [SDK]: §1.1/v0/§6/§4.A — единые lucide-пути: `src/shared/lucide-downloads-icons.ts` + генерация `RowIco`/`emitInlineSvg` в окне загрузок; `LucideMiniIcons` (folder/download/film/settings) из того же набора; topbar загрузок: cluster инспектор/редактор/движки/«О программе» + preload IPC-мост (`bridge*`), ▸ на history/log/hints; каналы §21; Vitest shared; спринт/§6.1.
- [J-151] 2026-05-10 13:56:24 [SDK]: §1.1/v0 — окно загрузок: CSS `:root` дублирует тёмные `--fa-*` из `base.css` + алиасы прежних `--bg`/…; `focus-visible` как в редакторе (`--fa-focus-ring`, select/inputs/cmd/icon-btn/topbar); топбар-иконки с `fa-border-subtle`; шрифтовой стек/smoothing как у main; спринт.
- [J-152] 2026-05-10 14:00:12 [SDK]: §1.1/§6 — окно загрузок: `data-theme` light/dark при создании HTML из `cachedSettings.theme` + preload `onThemeChanged` на `themeChanged`; `npm run check` ок.
- [J-153] 2026-05-10 14:01:38 [SDK]: §1.1/§4.C — окно yt-dlp: HiDPI CSS (`min-resolution`/dppx) + дефолтный размер/`min*` от `scaleFactor`; `focus-visible` на переключателях rail/селектах/`summary`; чеклист §4.C/§1.1.
- [J-154] 2026-05-10 14:04:14 [SDK]: §1.1/v0 — окно загрузок: нижняя зона под таблицей — история и журнал **вертикально** (лог снизу на всю ширину), `pre` тянется в flex-ячейке; §4.A/спринт.
- [J-155] 2026-05-10 14:05:40 [SDK]: §9/§4.C — окно инспектора: тулбар на `app-icon-btn` + общие lucide (`refreshCw` в `lucide-downloads-icons`, `IconRefreshCw`/film/папка/тема); `main.css` марка бренда; чеклист.
- [J-156] 2026-05-10 14:07:41 [SDK]: §1.1/v0/§4.1 — редактор: главный `app-topbar` выше под ~70px-референс; секция «Вывод» — вложенный `details` превью ffmpeg + persist `exportCommandPreview` (IPC whitelist settings-store/main).
- [J-157] 2026-05-10 14:09:10 [SDK]: §1.1/§4.C — `main.css`: HiDPI @media topbar/toolbar/icon-btn + `focus-visible` на FFmpeg `summary`; yt-dlp: топбар ~62px DIP, вкладки плотнее, доп. dppx для topbar; в CSS внутри TS template literal без обратных кавычек в комментариях.
- [J-158] 2026-05-10 14:10:20 [SDK]: §1.1/v0/§4.C — топбар редактора: `IconCircleHelp` из shared `circleHelp`; кнопка открывает тот же «О программе», что пункт меню (`about.getInfo`); спринт обновлён (`npm run check` ок).
- [J-159] 2026-05-10 14:13:18 [SDK]: §1.1/§4.C — тема «как в системе»: `AppTheme` включает `system`, `settingsGet`/`setTheme` добавляют `effectiveTheme`, меню «Вид → Тема» трёхпозиционное, `nativeTheme.updated` синхронизирует окна; Vitest parse `theme`; загрузки через `ResolvedAppTheme`.
- [J-160] 2026-05-10 14:15:14 [SDK]: §1.1/§4.C — `window-hidpi.ts`: общий `displayMatchingRestoreRect`/пороги `min*` по `scaleFactor` для загрузок, главного редактора и инспектора; дефолтные размеры главного/инспектора от `workArea` (сохранённые bounds без изменений); Vitest порогов; чеклист §4.C/спринт.
- [J-161] 2026-05-10 14:17:55 [SDK]: §9/§4.1/§1.1 — окно инспектора: шапка `app-topbar` как у редактора; блоки ffprobe с persist через `mergeMainWindowUiPanels` (`probe*` те же что под превью); CSS `focus-visible` на `.app-probe-summary`, большие `max-height` таблицы/JSON в standalone; §4.1/спринт.
- [J-162] 2026-05-10 14:19:32 [SDK]: §4.1/§21 — после `persistMainWindowUiPanelsMerge` broadcast `mainWindowUiPanelsChanged` всем окнам; главный preload: `onMainWindowUiPanelsChanged`; `App.tsx` и инспектор обновляют UI без перечитывания JSON; канал в `ipc-channels`.
- [J-163] 2026-05-10 14:20:55 [SDK]: §1.1/§6 — окно загрузок rail v0: строки checkbox — подпись слева, pill-toggle справа; все select в rail + справочник — компактный pill + кастомный chevron (`appearance:none`, без обратных кавычек в комментариях внутри template); `hist-inline` для фильтра истории; чеклист спринт.
- [J-164] 2026-05-10 14:22:07 [SDK]: §1.1/§6.1/v0 — таблица очереди: колонка прогресса — полоска + отдельный процент, зелёная заливка при «Готово», подпись speed/ETA без дубля процента; лёгкая зебра и hover строк; `title` у статуса; выравнивание числовых колонок; чеклист спринт.
- [J-165] 2026-05-10 14:23:22 [SDK]: §6.4/§1.1 — журнал операций окна загрузок: строки DOM (теги out/err + тело), цвет stderr, обрезка по лимиту удалением старых узлов, кнопка «Очистить вид», счётчик строк/размера; «Сохранить лог» — плоский текст из узлов `.log-line`; чеклист.
- [J-166] 2026-05-10 14:33:22 [Assistant]: ревью SDK после падения на 24/75: незакоммиченный `run-loop` retry доведён до повторов той же итерации при `status=error`; старт главного окна возвращён к FHD 1920×1080 на подходящем дисплее; чеклист синхронизирован.
- [J-167] 2026-05-10 14:43:10 [Assistant]: не-SDK блок после ревью: restore `downloads/queue.json` перенумеровывает duplicate id и покрыт Vitest; `mainWindowUiPanels` принимает merge только от main/inspector + preload whitelist push-снимков; topbar bridge в окне загрузок показывает ошибки, ffprobe context menu доступен с клавиатуры и ограничен по высоте.
- [J-168] 2026-05-10 14:50:04 [Assistant]: SDK automation review: настройки вынесены в `scripts/cursor-automation/src/sdk-settings.ts` с комментариями; `run-loop` читает дефолты оттуда, `.env.example` стал списком override; полный повтор любого `status=error` снова только явный (`LOOP_RETRY_RUN_ERROR=1`), быстрые transient error-run всё ещё повторяются.
- [J-169] 2026-05-10 14:54:27 [SDK]: §1.1/v0 — редактор: `PreviewTransport` под превью (skip ±5 с, play/pause, mute, slider громкости, fullscreen по stack); топбар: `rotate-ccw`/`rotate-cw`/`scissors` → циклы transform/crop §7.2 через settings IPC; общие SVG в `EDITOR_*_ICONS` + Vitest; `npm run check` ок.
- [J-170] 2026-05-10 15:00:23 [SDK]: §1.1/v0 — топбар: компактная строка `ffmpeg • yt-dlp` (парсинг `--version`) + `app-topbar-trailing`; таймлайн: zoom-in/out (lucide), окно scrub 1×–8×, `TimelineWaveform` (Web Audio, `waveform-peaks` + Vitest), кнопка «Обрезать → экспорт»; `npm run check` ок.
- [J-171] 2026-05-10 15:03:12 [SDK]: §1.1/v0 — под waveform: линейка времени видимого окна (`timeline-ruler` + деления/playhead); строка **Видео/Аудио/Позиция** из ffprobe + оценка кадра по fps из `detail`; HiDPI @media для ruler/facts; Vitest на шаг делений; спринт/§1.1 обновлены.
- [J-172] 2026-05-10 15:04:17 [SDK]: §1.1/§4.C — второй проход DPI редактора: `@120dpi`/`@168dpi` в `main.css` для **PreviewTransport**, zoom/scrub/waveform/ruler/media facts/marker/InOut и `.app-preview-probe`; спринт вычищен от дубликатов (7 пунктов).
- [J-173] 2026-05-10 15:06:20 [SDK]: §6.1/§4.A — окно загрузок: второй DPI-проход в inline CSS (`buildDownloadsHtml`): @120dpi/@168dpi для URL-band, queue toolbar/таблица/прогресс/иконы строк, журнал/история, rail (summary, selects, checkbox-pill).
- [J-174] 2026-05-10 15:07:28 [SDK]: §1.1/v0 — клик по `app-timeline-ruler-track` и клавиатура (Home/End/стрелки) → seek в окне zoom; `cursor` + `focus-visible` в `main.css`; чеклист §1.1/спринт.
- [J-175] 2026-05-10 15:09:01 [SDK]: §1.1 — снап seek к кадру по fps ffprobe: `snapSeekTimeSec` + Vitest; scrub/линейка/стрелки/In/Out; счётчик «кадр ~» по снапу; чеклист/спринт.
- [J-176] 2026-05-10 15:11:21 [SDK]: §1.1/IPC — `MediaProbeSuccess.videoFpsApprox` из ffprobe (`ffprobe-video-fps`, fallback `nb_frames`/duration); сводки TXT/HTML; `VideoTimeline` приоритет поля над regex `detail`; Vitest + чеклист.
- [J-177] 2026-05-10 15:14:05 [SDK]: §4.1/§1.1 — главное окно: **сворачивание всей колонки FFmpeg** (`ffmpegSettingsRailOpen`→`mainWindowUiPanels`, whitelist main/settings-store), chevron + FAB «FFmpeg» на превью; HiDPI правки; Vitest settings-store.
- [J-178] 2026-05-10 15:15:58 [SDK]: §6/§4.A/v0 единые иконки — окно загрузок: в топбар-кластер добавлена lucide **`download`** (`dlTopUrl`) → скролл + фокус `#urls`; порядок: film → download → home → engines → help; Vitest на HTML; чеклист §4.A/§4.
- [J-179] 2026-05-10 15:17:27 [SDK]: §6/§4.A/v0 — вкладки «Редактор/Загрузки» в topbar окна загрузок: inline SVG **home**/**download** из shared (`emitInlineStrokeSvg`); стили `.workspace-tab-glyph`; чеклист §4.A/§4; `npm run check` ок.
- [J-180] 2026-05-10 15:18:35 [SDK]: §4.A/v0 единые иконки — `EDITOR_TOPBAR_ACTION_ICONS` (снимок, save, ban, cloud-download): данные в `lucide-downloads-icons.ts`, `LucideMiniIcons` через `renderStrokeParts`; Vitest сериализации; чеклист §4/спринт §1.1; `npm run check` ок.
- [J-181] 2026-05-10 15:19:48 [SDK]: §4.A/v0 единые иконки — **`EDITOR_THEME_ICONS`** (sun/moon) в shared, `IconSun`/`IconMoon` через `renderStrokeParts`; Vitest; чеклист §4/спринт §1.1 (`sun/moon` перенесены из «позже»); `npm run check` ок.
- [J-182] 2026-05-10 15:20:52 [SDK]: §6/§1.1/v0 — окно загрузок: **`focus-visible`** на сворачиваемых **история / журнал / hints** (`<summary>`), тот же `--fa-focus-ring`, что у rail; чеклист §1.1/спринт §6; `npm run check` ок.
- [J-183] 2026-05-10 15:22:02 [SDK]: §1.1/v0 раскрываемые секции — главное окно: **`app-url-summary:focus-visible`** (`--fa-focus-ring`), как у превью argv/`app-settings-summary`; чеклист §1.1/спринт; `npm run check` ок.
- [J-184] 2026-05-10 15:23:15 [SDK]: §1.1/§4.C — главное окно: HiDPI `@120dpi`/`@168dpi` для **быстрой yt-dlp-полосы** (`app-url-summary` min-height/padding/font, `app-url-body` padding); спринт §1.1; `npm run check` ок.
- [J-185] 2026-05-10 15:24:32 [SDK]: §1.1/v0 — главное окно: **`focus-visible`** на **`app-url-input`** и универсально на **`app-btn`** (--fa-focus-ring); чеклист §1.1/спринт; `npm run check` ок.
- [J-186] 2026-05-10 15:25:31 [SDK]: §6/§4.A — окно загрузок: HiDPI **SVG вкладок** «Редактор/Загрузки» — `.workspace-tab-glyph svg` **17px / 18px** в `@120dpi` / `@168dpi` inline CSS; спринт §6; `npm run check` ок.
- [J-187] 2026-05-10 15:26:38 [SDK]: §6/v0 topbar — окно загрузок: `.dl-topbar-ico svg` **19px / 20px** на @120dpi / @168dpi + `display: block` у глифа; спринт §6; `npm run check` ок.
- [J-188] 2026-05-10 15:27:59 [SDK]: §1.1/§6/v0 HiDPR stroke-icons — главный `main.css`: **topbar-кластер** SVG **21×22px** + вкладка **«Загрузки»** **17×18px**; downloads: **`td.act .icon-btn svg` 17×18px** @120/@168 + `display: block`; спринт; `npm run check` ок.
- [J-189] 2026-05-10 15:29:17 [SDK]: §1.1/v0 — под превью: HiDPR **транспорт** + **zoom таймлайна** SVG **21×22px**, **`app-ffmpeg-rail-restore`** / **`app-settings-rail-collapse-btn`** **20×21px** в `@120dpi`/`@168dpi`; `display: block` для глифов; спринт §1.1; `npm run check` ок.
- [J-190] 2026-05-10 15:30:36 [SDK]: §1.1/v0 доступность topbar — **`app-workspace-tab:focus-visible`** (`--fa-focus-ring`); HiDPR марк **инспектора** `IconFilm` — **14×15px** `@120dpi`/`@168dpi`; спринт §1.1; `npm run check` ок.
- [J-191] 2026-05-10 15:32:01 [SDK]: §6 downloads layout / §1.1 — окно загрузок: **`#urls`** `aria-describedby`→`urlsHint`, **`caption.sr-only`** у таблицы очереди; **Загрузки** `aria-current="page"`; главное окно — **Редактор** `aria-current`; спринт; `npm run check` ок.
- [J-192] 2026-05-10 15:33:27 [SDK]: §6/v0 нижняя зона — **caption.sr-only** для **истории**; **`#logPre`**: **`role=log`**, **`aria-live=polite`**, **`aria-relevant=additions`**, `aria-label`; спринт §6; `npm run check` ок.
- [J-193] 2026-05-10 15:34:32 [SDK]: §4.A/§1.1 — **`MediaProbePanel`**: **`caption.app-visually-hidden`** у таблиц **дорожек** и **глав** (ffprobe); спринт §1.1; `npm run check` ок.
- [J-194] 2026-05-10 15:36:05 [SDK]: §1.1/v0 progressive disclosure — **`app-url-bar`**: текст **`.app-url-hint`** + **`aria-describedby`** для поля URL; блок **`.app-url-field`**, HiDPR `.app-url-hint` в `main.css`; спринт; `npm run check`/`lint` ок.
- [J-195] 2026-05-10 15:37:23 [SDK]: §1.1/v0 FFmpeg — **«Превью команды ffmpeg»**: **`pre`** с **`aria-describedby="exportCommandPreviewHint"`**, **`id`** на **`.app-export-preview-hint`**; спринт; `npm run check` ок.
- [J-196] 2026-05-10 15:38:47 [SDK]: §4.A/§1.1 MediaProbePanel — **`app-probe-toolbar-hint`** + **`aria-describedby`** (**экспорт сводки TXT/HTML**, **JSON ffprobe**), **`pre`** JSON с **`aria-label`**; спринт §1.1; `npm run check` ок.
- [J-197] 2026-05-10 15:40:15 [SDK]: §6 окно загрузок — **a11y фильтров** (`inline-filter-field`/`hist-inline-field`, **`for`**, **`sr-only` hints**), **`role=group`** + **`aria-labelledby`** для строк **cookies/каталог**, **`label for=hintInsert`** (убран дублирующий **`aria-label`** у **queueRetrySelect**); спринт §6; `npm run check` ок.
- [J-198] 2026-05-10 15:41:40 [SDK]: §1.1/v0 progressive disclosure — **rail FFmpeg**: **`app-settings-section-hint`** под summary + **`aria-describedby`** на **grid/stack** (5 секций), HiDPR `@120dpi`/`@168dpi` для hint; спринт §1.1; `npm run check` ок.
- [J-199] 2026-05-10 15:43:11 [SDK]: §6/v0 downloads rail + queue — **`aria-describedby=urlsHint`** у **Добавить/Начать**, **`dlQueueToolbarHint`** (**`sr-only`**) для **пауза/отмена/очистки**; **rail**: **`opts-hint` id** + **`aria-describedby`** на **`settings-body`** (5 секций); спринт §6; `npm run check` ок.
- [J-200] 2026-05-10 15:44:44 [SDK]: §6/§1.1 — downloads **история/журнал**: **`sr-only`** секционные hints + **`aria-describedby`** (**кнопки**, **фильтр истории**, **`#logPre`**); редактор **`basenameForAriaLabel`** + **`video aria-label`**; §1.1 чеклист; `npm run check` ок.
- [J-201] 2026-05-10 15:46:11 [SDK]: §1.1/§9 — **`PreviewProbeBody`**: **`role=region`**, **`aria-label`**, **`probePanelOverviewHint`** (**`app-visually-hidden`**); **«Копировать»** (превью ffmpeg) **`aria-describedby=exportCommandPreviewHint`**; спринт; `npm run check` ок.
- [J-202] 2026-05-10 15:48:01 [SDK]: §4.C/v0 **circle-question** — **`AboutDialog`** (общий **`app-modal`**), главное окно на компонент; **инспектор §9**: **`IconCircleHelp`** + тот же диалог/`about.getInfo`; чеклист §4.C/спринт §1.1; `npm run check` ок.
- [J-203] 2026-05-10 15:49:39 [SDK]: §1.1/v0 rail FFmpeg — **`aria-describedby`** секционных hint id для **компактных кнопок**: последний **снимок** (`ffmpegAudioSectionHint`), управление **пресетами** (`ffmpegPresetsSectionHint`), последний **экспорт** (`ffmpegOutputSectionHint`); чеклист §1.1 progressive; `npm run check` ок.
- [J-204] 2026-05-10 15:51:08 [SDK]: §6 downloads **settings rail** — **`aria-describedby`** секционных **`*SectionHint`** на поля и кнопки (**Формат/Метаданные/Сохранение/Сеть/Эксперт**), **`pre#argsPreview`** с **`aria-label`**; спринт §6; `npm run check` ок.
- [J-205] 2026-05-10 15:54:10 [SDK]: §1.1/v0 **быстрая yt-dlp-полоса** — **`aria-describedby=quickYtdlpUrlHint`** у кнопок **«Открыть окно»** и **«Из буфера»** (рядом с полем URL); чеклист §1.1 progressive; `npm run check` ок.
- [J-206] 2026-05-10 15:55:48 [SDK]: §4.1/§6 — окно загрузок: **`suppressDetailsUiPersist`** при программном открытии журнала на **log reset** — **`toggle` больше не вызывает** **`mergeUiPanels`** (сохраняется пользовательское **log: false**); чеклист §4.A/§4.1/спринт; `npm run check` ок.
- [J-207] 2026-05-10 18:38:10 [Assistant]: ревью SDK после 37 успешных итераций и падения на 38/100: `npm run check`/`build` зелёные; исправлено подавление persist для async `toggle` в окне загрузок (автопоказ лога не пишет `log:true`), добавлен streaming-лимит waveform по размеру ответа (≤96 MiB), и trim-снимок экспорта привязан к текущему пути, чтобы не взять In/Out от прошлого файла.
- [J-208] 2026-05-10 18:49:36 [Assistant]: §6.4 — парсер yt-dlp прогресса распознаёт retry-счётчики (`Retrying (N/M)`, `Retrying fragment X (N/M)`) и размер из строк `of ~ 5.00MiB`; Vitest `ytdlp-progress-parser` расширен до 62 тестов.
- [J-209] 2026-05-10 18:55:20 [Assistant]: §7.2 trim — вынесена тестируемая валидация `parseFfmpegExportTrim` для IPC/renderer payload: принимаются только конечные неотрицательные `inSec < outSec`; main export path использует общий helper, чеклист `Trim` закрыт.
- [J-210] 2026-05-10 18:58:50 [Assistant]: §9 ffprobe — добавлен чистый `ffprobe-side-data` summarizer для Dolby Vision/HDR (`DOVI configuration record`, mastering display, content light); `ffprobe-service` добавляет summary в сведения видеодорожки, поэтому таблица инспектора и TXT/HTML экспорт получают side_data без нового IPC.
- [J-211] 2026-05-10 19:02:58 [Assistant]: §6.4 yt-dlp — `parseYtdlpQueueFormatHint` расширен на post-processing строки `ExtractAudio`, `VideoRemuxer`/`FFmpegVideoRemuxer`, `FFmpegVideoConvertor`; колонка «Формат» показывает итоговый контейнер/аудио из редких логов, Vitest `ytdlp-progress-parser` расширен до 63 тестов.
- [J-212] 2026-05-10 19:08:05 [Assistant]: runtime fix — в главном renderer убраны `window.prompt`/`window.alert` из управления пользовательскими FFmpeg-пресетами; имя нового/переименованного пресета теперь вводится через `app-modal`, лимит 8 пресетов показывается в статусбаре/модалке, `npm run check` ок.
- [J-213] 2026-05-10 19:20:10 [Assistant]: UX pivot по v0 — ТЗ/чеклист переведены с «отдельное окно загрузок как основной режим» на единый workspace `Редактор`/`Загрузки`; главный renderer получил активную вкладку `Загрузки` с URL-band, живой queue table через downloads IPC и pop-out для старого полного окна.
- [J-214] 2026-05-10 19:30:05 [Assistant]: v0 controls — заведён React pill-switch паттерн для бинарных настроек; `Без аудио` в FFmpeg rail больше не select из двух вариантов, а `role=switch` с русской подсказкой; UX_REFERENCE/ТЗ/чеклист закрепили правило: binary → pill switch, multi-choice → select.
- [J-215] 2026-05-10 19:35:20 [Assistant]: вкладка `Загрузки` усилена без новых IPC: добавлены summary cards по очереди, status filter chips, inline progress bars, status-dot chips и действия `Файл`/`Папка` для готовых строк; rail теперь объясняет, что вкладка использует тот же сервис, что pop-out.
- [J-216] 2026-05-10 19:40:40 [Assistant]: вкладка `Загрузки` получила основной settings rail в React: главный preload открыл валидированные `getCliOptions`/`setCliOptions`, UI управляет format preset, playlist/audio-only/open-after-success через pill switches, subtitles/cookies/impersonate/retry profile/selects и показывает command preview.
- [J-217] 2026-05-10 19:45:20 [Assistant]: исправлен sender pinning downloads IPC для embedded вкладки: каналы очереди/настроек/истории теперь явно принимают главное окно через `isMainWindowSender`; React-вкладка показывает последние записи истории, очистку и открытие файла/папки результата без pop-out.
- [J-218] 2026-05-10 19:47:05 [Assistant]: вкладка `Загрузки` получила базовое управление очередью без pop-out: основной preload открыл `removeRow`/`clearFinished`/`clearQueue`, UI добавил удаление строки, очистку завершённых и очистку всей очереди со статусными подсказками.
- [J-219] 2026-05-10 19:49:05 [Assistant]: вкладка `Загрузки` получила pause/resume активной yt-dlp строки: snapshot прокидывает `isActiveRunner`/pause flags в React, основной preload открыл `pauseYtdlp`/`resumeYtdlp`, строка показывает кнопку `Pause`/`Resume` при поддержке ОС.
- [J-220] 2026-05-10 19:54:05 [Assistant]: вкладка `Загрузки` получила live log yt-dlp: downloads log sink теперь fan-out в pop-out и главное окно, основной preload валидирует `DownloadsLogPayload`, React rail показывает stdout/stderr, очистку и сохранение видимого лога без pop-out.
- [J-221] 2026-05-10 19:56:40 [Assistant]: вкладка `Загрузки` получила управление каталогом yt-dlp: основной preload открыл `get/open/pick/clearOutputDirectory`, React rail показывает текущий путь, default/custom статус и кнопки `Открыть`/`Выбрать`/`По умолчанию`.
- [J-222] 2026-05-10 19:58:20 [Assistant]: вкладка `Загрузки` получила управление Netscape cookies-файлом: основной preload открыл `pickCookiesFile`/`clearCookiesFile`, React rail показывает сохранённый путь, предупреждение о приоритете файла над browser cookies и кнопки выбора/очистки.
- [J-223] 2026-05-10 20:00:20 [Assistant]: вкладка `Загрузки` получила network поля yt-dlp в React rail: `limit-rate`, `--retries` и `--fragment-retries` редактируются inline и сохраняются через существующий валидированный `setCliOptions`.
- [J-224] 2026-05-10 20:03:10 [Assistant]: актуализирован `IMPLEMENTATION_CHECKLIST.md`: добавлена оценка готовности полного итога (~38%), обновлён текущий снимок проекта, ближайший TODO спринта и статусы по основному yt-dlp workspace/editor/ffmpeg/ffprobe с учётом последних React-вкладок.
- [J-225] 2026-05-10 20:08:30 [Assistant]: UI switches — в `App.tsx` вынесен общий `PillSwitch` для бинарных настроек; `Без аудио`, `Весь плейлист`, `Только аудио`, `Открыть после успеха` теперь используют один v0-style pill pattern с `role=switch`, `aria-label` и привязкой к русским пояснениям.
- [J-226] 2026-05-10 20:12:30 [Assistant]: UI copy — в editor/downloads rail русифицированы оставшиеся англоязычные подписи быстрых controls (`Crop`, `AAC bitrate`, `Pause/Resume`, network labels, cookies/impersonate) и исправлена подсказка про доверенный cookies export.
- [J-227] 2026-05-10 20:16:10 [Assistant]: bugfix embedded downloads — `broadcastDownloadsSnapshot` больше не завязан на открытый pop-out: snapshot очереди с active/pause flags рассылается и во вкладку `Загрузки` главного окна, поэтому `Добавить`/`Удалить`/`Старт` обновляют React-таблицу без отдельного окна.
- [J-228] 2026-05-10 20:22:10 [Assistant]: аудит похожих UI no-op — основные mutating downloads IPC (`addLines`, `clearQueue`, `clearFinished`, `removeRow`, reset output/cookies) больше не возвращают `void`/ложный `0`: main/preload/renderer/pop-out используют явный `{ok,error}`/`{ok,added}`/`{ok,removed}`, а ошибки показываются в статусе/alert.
- [J-229] 2026-05-10 20:27:20 [Assistant]: follow-up no-op audit — `startDownloadsSequential` теперь синхронно отвечает ошибкой, если очередь уже занята или нет строк «Ожидание», а `moveRow` в pop-out возвращает `{ok,error}` вместо `void`; `npm run check` зелёный.
- [J-230] 2026-05-10 20:28:40 [Assistant]: UI copy — во вкладке `Загрузки` оставшиеся action labels `Start`/`Retry` и `Live log` заменены на русские `Старт`/`Повтор` и `Живой лог`.
- [J-231] 2026-05-10 20:31:20 [Assistant]: паритет вкладки `Загрузки` с pop-out — основной preload открыл `moveRow`, `openQueueOutputInHandler`, `openHistoryOutputInHandler`; React-вкладка получила ↑/↓ перестановку строк и действие «В редактор» для готовых строк/истории.
- [J-232] 2026-05-10 20:32:30 [Assistant]: UI плотность — кнопки действий строки во вкладке `Загрузки` уплотнены локальным CSS, чтобы новые ↑/↓/«В редактор» не раздували таблицу.
- [J-233] 2026-05-10 20:57:10 [Assistant]: §3 policy — зафиксирован bundled-first подход для движков: релиз должен поставлять проверенные `ffmpeg.exe`/`ffprobe.exe`/`yt-dlp.exe` в `resources/bin`; `userData/bin` остаётся fallback/update, ручные пути — override. Добавлен `bin/README.md`, `.gitignore` запрещает случайный коммит бинарников.
- [J-234] 2026-05-10 21:01:20 [Assistant]: §3 download fallback — источник FFmpeg переведён на список зеркал: сначала GitHub BtbN `ffmpeg-master-latest-win64-gpl.zip`, затем gyan.dev essentials; SHA256 теперь выбирается по конкретному архиву, legacy `FfmpegSha256` оставлен только для gyan.dev.
- [J-235] 2026-05-10 21:09:00 [Assistant]: §3 maintenance — добавлен IPC/preload/UI для удаления скачанных движков из `userData/bin` (только известные `ffmpeg`/`ffprobe`/`yt-dlp` и cache); bundled `resources/bin` и ручные override не трогаются.
- [J-236] 2026-05-10 21:14:20 [Assistant]: §3 dev bootstrap — добавлен `scripts/prepare-engines-win.mjs` и npm script `engines:prepare:win`; `predev` автоматически наполняет проектный `bin/` перед `npm run dev`, `prebuild:win` делает то же перед Windows installer.
- [J-237] 2026-05-10 21:18:10 [Assistant]: §3 engines local — фактически скачаны локальные ignored `bin/ffmpeg.exe`, `bin/ffprobe.exe`, `bin/yt-dlp.exe`; исправлена проверка версии: ffmpeg/ffprobe запускаются с `-version`, yt-dlp с `--version`, чтобы статус движков в dev не был ложной ошибкой.
- [J-238] 2026-05-10 21:25:40 [Assistant]: §6.4 downloads open fix — исправлен no-op после успешной загрузки YouTube с merge: yt-dlp запускается с UTF-8 stdout/env, а open file/folder/handler умеют найти финальный файл по `[videoId]` внутри каталога загрузок, если сохранённый путь указывал на удалённый промежуточный `.f*.mp4/.webm`.
- [J-239] 2026-05-10 21:34:10 [Assistant]: §6.4 preview compatibility — причина «В редактор переключает вкладку, но видео не играет» подтверждена как `.mkv`/Matroska preview issue в Chromium: добавлен MP4-пресет редактора по умолчанию, а для уже скачанных неподдержанных контейнеров `В редактор` создаёт `preview-cache/*.mp4` через bundled ffmpeg (copy video + AAC, fallback libx264).
- [J-240] 2026-05-10 21:40:20 [Assistant]: §6.4 preview UX — ручная проверка proxy на текущем ролике: MKV -> MP4/AAC создаётся ~15–16 секунд. Добавлен статус в UI перед открытием в редакторе и лог `mp4 preview proxy ready`, чтобы подготовка preview-cache не выглядела как зависание.
- [J-241] 2026-05-10 21:48:30 [Assistant]: §4.B/§7 preview playback — для `fluxmedia://` заменён `net.fetch(file://...)` на собственный file streaming с `Range`/`206 Partial Content`, `Accept-Ranges` и MIME по расширению; renderer теперь логирует код ошибки HTMLVideoElement в статус/лог.
- [J-242] 2026-05-10 22:12:39 [SDK]: §1.1/v0/§7 — `PillSwitch` 2-pass libx264 в rail «Формат» (только с видеобитрейтом), persist `ffmpegExportTwoPass`, двойной ffmpeg с temp `passlog`/очисткой, превью двух команд и прогресс 0–50/50–100; IPC/preload/argv; microtask для сброса `previewBlobUrl` (eslint); Vitest + чеклист.
- [J-243] 2026-05-10 22:15:41 [SDK]: §6.1/v0 — вкладка «Загрузки»: «История» и «Живой лог» под таблицей очереди, rail только настройки; копия кнопок «Добавить в очередь»/«Начать загрузку»; CSS `app-downloads-table-zone`/`app-downloads-lower-stack`/футер rail; ограничены высоты истории и лога в нижнем стеке. `npm run check` зелёный.
- [J-244] 2026-05-10 22:18:00 [SDK]: §6.1/v0 UX — действия строк очереди на React-вкладке «Загрузки» переведены в icon-only (`app-icon-btn` 26px); глифы из `lucide-downloads-icons` + `LucideMiniIcons` (chevron/play/pause/retry/file/folder/outbound/trash); в shared добавлен `x` для `RowIco`; тест bootstrap. `npm run check` зелёный.
- [J-245] 2026-05-10 22:20:27 [SDK]: §4.A/§6 — встроенная вкладка «Загрузки»: управляемые `<details>` «История»/«Живой лог» + persist в `downloadsWindowUiPanels` через главный preload `downloads.mergeUiPanels` (существующий IPC); гидратация из `settings.get`; автолог строки очереди не трогает раскрытие. `npm run check` зелёный.
- [J-246] 2026-05-11 20:35:00 [Assistant]: §6.1/§6.4 — снижен шум при активной загрузке: патч очереди/снапшота по прогрессу не чаще ~1 с (`downloads-queue-runner`), полоса в React и pop-out с плавным `transition` ширины; живой лог — строки `[download] … NN%` сэмплируются (шаг ≥0,1 п.п. или heartbeat 2,5 с), прочие строки без изменений; скорость в колонке сглаживается (EMA) и показывается в стабильных MiB/s vs KiB/s; в подписи ячейки и заголовках таблицы «Осталось» вместо ETA; добавлены `parseYtdlpSpeedToBytesPerSec` / `formatTorrentStyleSpeedFromBps` / `parseYtdlpProgressPercentNumber` и тесты. `npm run check` зелёный.
- [J-247] 2026-05-11 20:36:00 [Assistant]: снова добавлен `docs/AGENT_INSTRUCTIONS_AND_AGREEMENTS.md` (единый playbook для агента и владельца): текст восстановлен из транскрипта Cursor, в истории Git файла не было; в §16 файла зафиксировано восстановление.
- [J-248] 2026-05-11 21:05:00 [Assistant]: §6.1 — вкладка «Загрузки» rail: секции «Формат / Метаданные / Сохранение / Сеть / Эксперт и превью» на `<details>` с persist в `downloadsWindowUiPanels` (те же ключи и дефолты открытия, что pop-out); «Открыть после успеха» перенесён в метаданные; в «Формат» добавлено поле языков субтитров (`subLangs` / `subLangsLine`); подсказка про pop-out для argv; стили шеврона в `main.css`. `npm run check` зелёный.
- [J-249] 2026-05-11 21:28:00 [Assistant]: §6.1 — после `mergeDownloadsWindowUiPanelsPatch` main шлёт `downloadsWindowUiPanelsChanged` в главное окно; preload `downloads.onDownloadsWindowUiPanelsChanged` + гидратация вкладки «Загрузки» (история, лог, rail) без перезапуска. `npm run check` зелёный.
- [J-250] 2026-05-11 21:40:00 [Assistant]: §6 — дефолт шаблона `-o` без `[%(id)s]` в имени; `shortLabel` из basename вывода (срез хвоста `[id]`); скорость в UI — MB/s и KB/s (×1000); `updateDownloadsRow`: `undefined` для queue* не удаляет поле (колонка «Скорость» после «Готово»); пример в `Data/ytdlp_commands.json`. `npm run check` зелёный.
- [J-251] 2026-05-11 22:02:00 [Assistant]: §6 — ранний `shortLabel` из строки `[info] …: Downloading N format(s)` (`parseYtdlpInfoDownloadingTitlePrefix`, без 11-символьного id YouTube); runner вызывает подсказку на stdout/stderr; UX_REFERENCE колонка «Осталось»; чеклист спринта. `npm run check` зелёный.
- [J-252] 2026-05-11 23:15:00 [Assistant]: §6.1/§6.3 — вкладка «Загрузки» rail «Эксперт»: textarea `extraArgsLine` + optgroup‑селектор вставки токена; CSS `min(…rem, vh)` для таблицы/истории/лога; `Data/ytdlp_commands.json` — правка `-f`, новые флаги; UX_REFERENCE/чеклист. `npm run check` зелёный.
- [J-253] 2026-05-11 23:55:00 [Assistant]: §1.1 help links — `src/shared/external-doc-urls.ts`; внешние ссылки в быстрой полосе yt-dlp, rail «Эксперт», сводке ffprobe (`MediaProbePanel`), «О программе»; pop-out загрузок: подсказка + `setWindowOpenHandler` → `shell.openExternal`. Чеклист спринта. `npm run check` зелёный.
- [J-254] 2026-05-11 16:00:00 [Assistant]: журнал — пронумерованы все существующие строки **J-001…J-253**; в «Правило записей» зафиксированы правила **J-NNN**; выполнено обзорное ревью проекта (архитектура целостна, риски: монолитный `App.tsx`, дублирование UI вкладки/pop-out загрузок, незакрытые §8/§10/§11/локализация/DPI-матрица из чеклиста; движки Win-centric; нет e2e). Детали — в ответе ассистента в чате.
- [J-255] 2026-05-11 16:45:00 [Assistant]: из «Правило записей» удалён ошибочно добавленный блок «После паузы…» — рабочие инструкции после SDK не входят в назначение журнала; нумерация **J-NNN** сохранена.
- [J-256] 2026-05-11 21:04:12 [SDK]: §6/§4.C — встроенная вкладка «Загрузки»: HiDPI `@120dpi`/`@168dpi` в `main.css` (URL-band, сводка/фильтр, таблица, нижние панели, rail, icon-only SVG); спринт §6.1; `npm run check` ок.
- [J-257] 2026-05-11 21:06:42 [SDK]: §6.1/v0 — колонка прогресса: полоска + явный `%` (fallback текст без парсящегося %); «Начать загрузку» — primary; HiDPI `@192dpi` для вкладки; спринт; `npm run check` ок.
- [J-258] 2026-05-11 21:08:13 [SDK]: §6.1/v0 — `@media (max-width:1100px)`: rail загрузок не скрывается, сетка `1fr`+`auto`, rail под колонкой с `max-height`+scroll; подсказка в UI; чеклист §1.1/спринт; `npm run check` ок.
- [J-259] 2026-05-11 21:10:18 [SDK]: §4/§6 единые иконки — `DOWNLOADS_TOPBAR_CLUSTER_ICONS.clipboard`/`popOutWindow`, `IconClipboardPaste`/`IconPopOutWindow`, кнопки «Из буфера»/«Pop-out» на вкладке; Vitest; чеклист §4.C/спринт; `npm run check` ок.
- [J-260] 2026-05-11 21:12:08 [SDK]: §6/§4 — URL-band вкладки «Загрузки»: `IconQueuePlus`/`IconPlay`/`IconBan`/`IconQueueTrash` + `app-btn-icon-leading` на всех действиях очереди; Vitest `plus`; спринт; `npm run check` ок.
- [J-261] 2026-05-11 21:14:43 [SDK]: §6.1/v0 узкая ширина — `#downloads-ytdlp-settings-rail`, `matchMedia(1100px)`, кнопка «К настройкам»+`scrollIntoView`, `overflow-y` на workspace; чеклист §1.1/спринт; `npm run check` ок.
- [J-262] 2026-05-11 21:16:31 [SDK]: §6/§4 — «История»/«Живой лог» вкладки: `IconRefreshCw`, `IconQueueTrash`/`IconQueueX`, карточки `file`/`folder`/`outbound`, лог `IconSave`; `IconQueueX` в `LucideMiniIcons`; спринт; `npm run check` ок.
- [J-263] 2026-05-11 21:18:15 [SDK]: §6 rail v0-иконки — `IconHome` из `DOWNLOADS_TOPBAR_CLUSTER_ICONS.home`; cookies **Выбрать/Очистить**, каталог **Открыть/Выбрать/По умолчанию**, футер **Обновить/Остановить** с `app-btn-icon-leading` (`App.tsx`/`LucideMiniIcons`); спринт; `npm run check` ок.
- [J-264] 2026-05-11 21:19:57 [SDK]: §6.3/§4 — pop-out `buildDownloadsHtml`: `QUEUE_ROW_ACTION_ICONS` + `EDITOR_TOPBAR_*` для кнопок с текстом (`cmd-icon-leading` CSS); паритет иконок с вкладкой на ленте/тулбаре/истории/логе/rail; спринт; `npm run check` ок.
- [J-265] 2026-05-11 21:22:36 [SDK]: §6.3/v0 — pop-out rail: **pill-switch** (кнопки `role=switch`, Вкл/Выкл) вместо checkbox для плейлиста/только аудио/открыть в обработчике; `syncFmtPresetAudioLock`; превью argv на toggle; спринт; `npm run check` ок.
- [J-266] 2026-05-11 21:24:20 [SDK]: §6.1/v0 pop-out узкая ширина — `@media (max-width:960px)`: сетка `1fr` + `auto`, rail с `max-height`+`overflow`, `dl-main` вертикальный scroll; `#dl-ytdlp-settings-rail`, кнопка **«К настройкам»** (`matchMedia`+settings SVG); подсказка в ленте URL; спринт; `npm run check` ок.
- [J-267] 2026-05-11 21:26:49 [SDK]: DPI-матрица 100–200%: добавлены `@144dpi` для embedded downloads в `main.css`, и `@144/@192` для pop-out (`downloads-window.ts`); `npm run check` ок.
- [J-268] 2026-05-11 21:29:44 [SDK]: §6.3 pop-out справочник токенов — добавлены поиск + полный кликабельный список (группы по category) для вставки в `extraArgsInput` с авто-обновлением превью; `npm run check` ок.
- [J-269] 2026-05-11 21:31:22 [SDK]: §6.3 pop-out polish — клик по токену в длинном справочнике фокусирует и прокручивает `extraArgsInput` перед обновлением превью; `npm run check` ок.
- [J-270] 2026-05-11 21:32:59 [SDK]: §6.3 pop-out preview — `schedulePreviewRefresh()` на input/change для полей rail (tmpl/-f/sub/cookies/impersonate/network/extraArgs/previewOutDirOverride), чтобы превью argv обновлялось без «Сохранить параметры»; `npm run check` ок.
- [J-271] 2026-05-11 21:35:24 [SDK]: §1.1/§4.C DPI 150% — расширен `@144dpi` блок в `main.css` для editor: `app-url-summary`, transport strip и базовые размеры waveform/ruler; `npm run check` ок.
- [J-272] 2026-05-11 21:45:00 [Assistant]: renderer — вынесены панели «История» и «Живой лог» вкладки «Загрузки» из `App.tsx` в компоненты `components/downloads/*` без изменения поведения; `npm run check` ок.
- [J-273] 2026-05-11 22:05:00 [Assistant]: §17/security — централизован guard внешних ссылок `openAllowedExternalUrl`: main/downloads pop-out/inspector открывают через `shell.openExternal` только `http:`/`https:`, `file:`/`javascript:` отбрасываются; добавлен Vitest `external-url`; `npm run check` ок.
- [J-274] 2026-05-11 22:10:00 [Assistant]: §17/security follow-up — добавлен `installExternalNavigationGuard`: main/downloads pop-out/inspector блокируют навигацию текущего Electron-окна на внешние URL и открывают только разрешённые `http(s)` через системный браузер; `check:secrets` встроен в `npm run check`; CI больше не дублирует отдельный secrets-шаг; Vitest расширен, `npm run check` ок.
- [J-275] 2026-05-11 22:13:00 [Assistant]: `package.json` актуализирован под реальный репозиторий `W1ldGodlike/FluxAlloy`: description, `UNLICENSED`, author, homepage, repository, bugs, keywords; `npm run check` ок.
- [J-276] 2026-05-11 22:20:00 [Assistant]: §19/release — добавлены `docs/RELEASE.md` и `docs/BUNDLED_ENGINES_LICENSES.md`; README/bin README ссылаются на релизные документы; `package.json` получил `check:release` и `audit:moderate`; GitHub Actions `ci` теперь делает `npm run build` после `npm run check`; чеклист §19 обновлён. `npm run check` ок.
- [J-277] 2026-05-11 22:24:00 [Assistant]: рабочее правило спринта обновлено по запросу владельца: больше не держать ближайший спринт вокруг v0; работать крупными общими блоками продукта, v0 использовать только как ориентир при необходимых UI-правках; отчёты пользователю — минимальные.
- [J-278] 2026-05-11 22:30:00 [Assistant]: SDK automation prompt hardening — `continue.txt` больше не заставляет каждую итерацию заново читать контракт/v0; контракт/initial/continue переведены на общий продуктовый фокус и обязательный формат журнала `J-NNN`; добавлен `scripts/check-journal-numbering.mjs` и `check:journal` в `npm run check`; README SDK обновлён.
- [J-279] 2026-05-11 22:34:00 [Assistant]: SDK continue-mode уточнён под поведение живого чата `+`: использовать контекст текущего Agent instance, не перечитывать чеклист/журнал/v0 без необходимости, читать хвост журнала только перед новой записью или при риске дублирования; в контракт добавлено требование владельца работать «максимум максимум» за итерацию.
- [J-280] 2026-05-11 22:37:55 [SDK]: §19/§3 — `engines:verify-bundled` + `FLUXALLOY_ENGINES_STRICT`, поля `ffmpeg.exe`/`ffprobe.exe` в `trusted_hashes`, проверки в prepare/engine-download; CI: prepare+verify перед build; win target portable+NSIS; docs RELEASE/licenses; чеклист. `npm run check` ок.
- [J-281] 2026-05-11 22:39:25 [SDK]: §19/§3 — `engines:report-hashes` (+ `--json`), `check:release` с prepare+verify перед build, win target `zip` в electron-builder; RELEASE/чеклист спринта. `npm run check` ок.
- [J-282] 2026-05-11 22:41:05 [SDK]: §19/§3 — CI: кэш `bin/`, шаг `pack:dir`; `npm run pack:dir`; исправлен невалидный корневой `zip:` в electron-builder (zip в `win.target`); bin README + RELEASE; чеклист спринта. `npm run check` ок.
- [J-283] 2026-05-11 22:42:21 [SDK]: §19/§3 — `npm run release:win` (prepare+verify+build+`electron-builder --win` без дубля prebuild), CI `restore-keys` для кэша движков, RELEASE/bin README, спринт. `npm run check` ок.
- [J-284] 2026-05-11 22:43:30 [SDK]: §3/§19 — `FLUXALLOY_ENGINES_FORCE` в `prepare-engines-win`, `engines:prepare:win:force`, RELEASE/bin README/чеклист. `npm run check` ок.
- [J-285] 2026-05-11 22:44:20 [SDK]: §19 — `release:win:force`, CI `concurrency`+cancel-in-progress; RELEASE §4/§5, bin README, спринт. `npm run check` ок.
- [J-286] 2026-05-11 22:45:25 [SDK]: §19/§17 — CI `permissions: contents: read`; `engines:verify-bundled` при `GITHUB_ACTIONS` логирует первую строку версии каждого exe; RELEASE/спринт. `npm run check` ок.
- [J-287] 2026-05-11 22:47:09 [SDK]: §3/§19 — `FLUXALLOY_ENGINE_DOWNLOAD_TIMEOUT_MS`+`AbortSignal.timeout` в prepare и `engine-download`; `engines:report-hashes --versions`; `FLUXALLOY_LOG_ENGINE_VERSIONS` в verify; RELEASE/bin README/спринт. `npm run check` ок.
- [J-288] 2026-05-11 22:48:27 [SDK]: §19/§3 — `engines-exe-version-line.mjs` (общий `--version` для report/verify), лог timeout в prepare при env, RELEASE `CSC_IDENTITY_AUTO_DISCOVERY`, BUNDLED_LICENSES пункт про timeout; спринт. `npm run check` ок.
- [J-289] 2026-05-11 22:49:27 [SDK]: §19 — CI кэш bin: расширен `hashFiles` (verify/report/version-line); `--help` в verify/report; README релиз, RELEASE справка; спринт. `npm run check` ок.
- [J-290] 2026-05-11 22:50:35 [SDK]: §19/§3 — `engines:doctor`; `--help` у prepare/run-prepare-force; CI `fetch-depth: 1`; README/RELEASE/bin README; спринт §19 короче. `npm run check` ок.
- [J-291] 2026-05-11 22:51:38 [SDK]: §19 — CI: кэш `electron` и `electron-builder` Cache по lockfile; prepare `--help` — строка про `trusted_hashes`; RELEASE/спринт. `npm run check` ок.
- [J-292] 2026-05-11 22:52:42 [SDK]: §19 — `check:release` включает `pack:dir`; `engines:doctor` = verify + SHA + версии; README/RELEASE/bin README/спринт. `npm run check` ок.
- [J-293] 2026-05-11 22:53:56 [SDK]: §3/§19 — `scripts/validate-trusted-hashes-json.mjs` + `npm run check:trusted-hashes` в `check` (`--help`); RELEASE/спринт. `npm run check` ок.
- [J-294] 2026-05-11 22:55:05 [SDK]: §3/§19 — validate `trusted_hashes`: schema, legacy-строки, неизвестные ключи (warn / `FLUXALLOY_TRUSTED_HASHES_STRICT_UNKNOWN`); RELEASE/спринт. `npm run check` ок.
- [J-295] 2026-05-11 22:56:22 [SDK]: §3/§19 — validate: `FLUXALLOY_TRUSTED_HASHES_REQUIRE_SHA256_HEX` для непустых хешей; CI job env strict+hex; RELEASE §1/§5, спринт. `npm run check` ок.
- [J-296] 2026-05-11 22:57:27 [SDK]: §19 — CI после prepare: `engines:doctor` вместо только `engines:verify-bundled` (SHA+версии в лог); RELEASE §5, спринт. `npm run check` ок.
- [J-297] 2026-05-11 22:58:21 [SDK]: §19 — `check:release` и `release:win*` после prepare: `engines:doctor` (паритет с CI); RELEASE §2/чеклист §19/спринт, BUNDLED_ENGINES_LICENSES. `npm run check` ок.
- [J-298] 2026-05-11 22:59:23 [SDK]: §19 — README «Команды»/первичная настройка: актуальный `npm run check`; prepare `--help` → `engines:doctor`; RELEASE §4: `prebuild:win` vs `release:win`; спринт. `npm run check` ок.
- [J-299] 2026-05-11 23:00:18 [SDK]: §19 — `--help` у force/verify/report → `engines:doctor`; CI кэш `bin/`: +`run-prepare-engines-force.mjs`; RELEASE §2 справка; чеклист SHA256/спринт. `npm run check` ок.
- [J-300] 2026-05-11 23:01:18 [SDK]: §19 — README движки/релиз, `electron-builder.yml` комментарий extraResources/bin, `validate-trusted-hashes --help` vs doctor, bin README про CI cache, спринт. `npm run check` ок.
- [J-301] 2026-05-11 23:02:25 [SDK]: §19/§3 — RELEASE §1: `build` без prepare/doctor; README `predev` vs doctor; спринт §19 короче с явными next; `engine-download.ts` — таймаут в шапке. `npm run check` ок.
- [J-302] 2026-05-11 23:03:12 [SDK]: §19 — CI `on: workflow_dispatch`; RELEASE §5 + README «Релиз»; спринт. `npm run check` ок.
- [J-303] 2026-05-11 23:04:34 [SDK]: §19 — `electron-builder.yml`: явный `directories.output: dist`; RELEASE §4 + README про `dist/win-unpacked/` и необходимость `bin/`; спринт. `npm run check` ок.
- [J-304] 2026-05-11 23:05:33 [SDK]: §19 — RELEASE §1/§5: `check:release`/`ci` и `dist/win-unpacked/`; bin README (`predev` vs doctor, smoke path); шапка `prepare-engines-win.mjs`; спринт. `npm run check` ок.
- [J-305] 2026-05-11 23:10:12 [Assistant]: полный аудит `IMPLEMENTATION_CHECKLIST.md` после SDK/release/security изменений: обновлены оценка готовности, тестовые счётчики, §3 first-run/движки, SDK continue-mode, release/CI, внешние ссылки, v0-формулировки как ориентир, §19/§21 статусы.
- [J-306] 2026-05-11 23:27:52 [SDK]: §7.2 — расширен ffmpeg-экспорт: `audioGainDb` (`-filter:a volume=NdB`, шаг 3 дБ, −24…+24), `stripMetadata`/`stripChapters` (`-map_metadata`/`-map_chapters -1`), `subtitleMode` `copy`/`drop` (`-c:s copy` для MKV, `mov_text` для MP4/MOV) — main service + IPC + preload + UI rail «Аудио и кадр» + persist в settings + 7 новых тестов в `ffmpeg-export-argv`. `npm run check` ок (30 file / 273 tests).
- [J-307] 2026-05-11 23:35:42 [SDK]: §7.2 — видеофильтр chain: `videoDenoise` (`hqdn3d`, 3 ступени) и `videoSharpen` (`unsharp`, 3 ступени), порядок `-vf`: transform → crop → denoise → sharpen → scale → fps; 2 новых IPC + persist + UI в секции «Видео» + 4 новых vitest-кейса. `npm run check` ок (30 file / 277 tests).
- [J-308] 2026-05-11 23:51:12 [Assistant]: ревью SDK с `J-280`: обнаружен обрыв итерации 3/300 на `videoEqPreset`/`audioNormalize` (типчек падал из-за незавершённых переменных); полуфабрикат дозавершён в том же §7.2: `eq` presets + `loudnorm`/`dynaudnorm`, settings/IPC/preload/main/UI/tests/чеклист; `npm run test` ок (30 files / 279 tests).
- [J-309] 2026-05-11 23:59:04 [Assistant]: SDK automation token-cost hardening — `MAX_STEPS` остаётся общим лимитом 200/300, но loop теперь режет работу на короткие `Agent.create` сессии через `SDK_SESSION_STEPS`/`--session-steps` (дефолт 2) и compact handoff вместо накопления огромного conversation/cache context; обновлены README/.env.example/continue/contract/checklist; `agent:typecheck` и `npm run check` ок.
- [J-310] 2026-05-12 00:05:02 [Assistant]: SDK token economy follow-up — дефолт `SDK_SESSION_STEPS` снижен до 1 для минимального cache-read на 200/300 итерациях; в prompts/README закреплено правило владельца «максимум кода/прогресса за минимум токенов»; `agent:typecheck` ок.
- [J-311] 2026-05-12 00:07:21 [Assistant]: SDK long-loop guard — `--verbose`/`VERBOSE=1` теперь автоматически отключается при `MAX_STEPS` > 20, если явно не задан `SDK_ALLOW_VERBOSE_LONG_LOOP=1`, чтобы не раздувать terminal logs и контекст наблюдения на 200/300-step циклах; README/.env.example/help обновлены; `agent:typecheck` ок.
- [J-312] 2026-05-12 00:08:50 [Assistant]: SDK cheap-run ergonomics — добавлены npm aliases `loop:cheap`/`agent:loop:cheap` для `--max-steps 300 --session-steps 1`, а runner предупреждает о дорогом `session-steps >= 4` на long-loop; README обновлён; `agent:typecheck` ок.
- [J-313] 2026-05-12 00:10:08 [Assistant]: SDK cheap-run help polish — `run-loop --help` теперь показывает `loop:cheap` и `--max-steps 300 --session-steps 1` вместо старого примера с `session-steps 2`; `agent:typecheck` и `agent:loop:cheap -- --help` ок.
- [J-314] 2026-05-12 00:15:52 [Assistant]: token-output optimization — добавлен `npm run check:quiet` (`scripts/run-quiet-check.mjs`): тот же lint/typecheck/test/trusted-hashes/journal/secrets набор, но на успехе только короткий summary, полный stdout/stderr только при падении шага; SDK prompts переведены на `check:quiet`, verbose stream ограничен `VERBOSE_MAX_CHARS`; `check:quiet` и `agent:typecheck` ок.
- [J-315] 2026-05-12 00:18:42 [Assistant]: вернулся к product core §6.1/§6.4 — усилен `ytdlp-progress-parser`: retry теперь понимает `attempt N of M` и `Retrying in N seconds`, классификация stable-block покрывает bot/location/inappropriate/login cases, `extractYtdlpOutputPath` ловит fixup-постпроцессоры (`FixupM3u8`/timestamp и т.п.); тесты расширены до 280; `check:quiet` ок.
- [J-316] 2026-05-12 00:23:34 [SDK]: §6.4 — `ytdlp-progress-parser`: prep-строки `[download]` (m3u8 / player API JSON / webpage), маркеры skip (диск/ffmpeg/пустой файл/takedown) и transient (521–523/520, EOF protocol, SSL handshake); Vitest +2; `npm run check` ок.
- [J-317] 2026-05-12 00:25:12 [SDK]: §9 — `ffprobe-service` / сводка дорожки: в `detail` для видео добавлены `profile`, `level`, `field_order`, `chroma_location`, `bits_per_raw_sample` (когда ffprobe отдаёт); спринт §9. `npm run check:quiet` ок.
- [J-318] 2026-05-12 00:29:55 [SDK]: §6.4→§7.2 — сценарий download→encode: `ytdlpAutoExportAfterOpenInHandler` + UI (вкладка + pop-out), runner-хук после успешного авто-открытия, `resolveFfmpegExportJobOptionsFromAppSettings` + `pickUniqueAutoExportOutputPath`, рефактор `exportStart` на общий resolver; Vitest +4. `npm run check:quiet` ок.
- [J-319] 2026-05-12 00:31:06 [SDK]: §9 — сводка видеодорожки (`detail`): при наличии в JSON ffprobe — отличающийся `coded_width`×`coded_height`, `refs`, `has_b_frames` (B-кадры), `codec_tag_string` (FourCC); спринт §9. `npm run check:quiet` ок.
- [J-320] 2026-05-12 00:33:06 [SDK]: §9 — `detail` видео: SAR (кроме 1:1), DAR; поворот `matrix …°` из Display Matrix (`extractFfprobeDisplayMatrixRotation`) или `rot …°` из тега `rotate`; тест side-data; спринт §9. `npm run check:quiet` ок.
- [J-321] 2026-05-12 00:34:25 [SDK]: §9/§18 — Support ZIP `diagnostics.txt`: `appLocale`/`systemLocale`, `pid`, `cwd`, basename exe, `packaged`, строка primary display (bounds×scaleFactor+workArea) из `screen.getPrimaryDisplay`; `supportBundleRuntimeInfo` в main; Vitest. Спринт §9. `npm run check:quiet` ок.
- [J-322] 2026-05-12 00:35:23 [SDK]: §9 — сводка аудиодорожки (`detail`): при наличии в JSON ffprobe — `profile`, `sample_fmt`, `bits_per_sample` (строка `N-bit PCM`); `ffprobe-service` + типы потока. Спринт §9. `npm run check:quiet` ок.
- [J-323] 2026-05-12 00:37:23 [SDK]: §9 — `buildTrackDetail`: для видео при валидном `nb_frames` — суффикс `N frm` в колонке «Сведения»; для субтитров — `codec_tag_string` (FourCC контейнера) в `detail`. Спринт §9. `npm run check:quiet` ок.
- [J-324] 2026-05-12 00:39:04 [SDK]: §9 — сводка дорожки (`detail`): нетривиальный ffprobe `start_time` для видео/аудио/субтитров — компактная метка `start ±Nms` / `start ±Ns` (`formatFfprobeStreamStartTime`, Vitest). Спринт §9. `npm run check:quiet` ок.
- [J-325] 2026-05-12 00:41:37 [SDK]: §6.3 — вкладка «Загрузки»: поиск по справочнику argv + прокручиваемый список токенов с описаниями и кликом для вставки (как pop-out); `groupYtdlpCommandHintsByCategory` в `shared` + Vitest; стили `main.css`, подзаголовок rail. Спринт §6. `npm run check:quiet` ок.
- [J-326] 2026-05-12 00:44:05 [SDK]: §6.3 — паритет вкладка/pop-out: общий `ytdlp-hint-category-order` (порядок групп как в `sortYtdlpCommandHintsForUi`), `groupYtdlpCommandHintsByCategory` на `compareYtdlpHintCategoryKeys`; pop-out — сорт списка по тому же порядку, doc-ссылки README/форматы/шаблон вывода/постобработка, подписи поиска/aria; Vitest +3. Спринт §6. `npm run check:quiet` ок.
- [J-327] 2026-05-12 00:49:24 [SDK]: §7.2 — whitelist `deband` (`FfmpegExportVideoDebandId`): `resolveFfmpegExportVideoDebandFilter` + порядок `-vf` denoise→deband→sharpen→eq; settings/IPC/preload, `resolveFfmpegExportJobOptionsFromAppSettings`, UI rail «Видео», user preset snapshot/merge, Vitest; чеклист §7.2/спринт. `npm run check:quiet` ок.
- [J-328] 2026-05-12 00:57:05 [SDK]: §7.2 — `lut3d`: bundled `.cube` в `resources/luts/` (`film-warm`/`film-cool`/`punch`), `escapeFilePathForFfmpegFilter`/`buildFfmpegExportLut3dFilter`, порядок `-vf` …→deband→lut3d→unsharp→eq; `resolveFfmpegExportLutCubeAbsPath` (dev: `resources/luts` от корня репо, prod: `luts` под `process.resourcesPath`), `lutResourcesRoot` в `runFfmpegExportJob`, IPC превью пути + settings/UI/export; Vitest. `npm run check:quiet` ок.
- [J-329] 2026-05-12 01:01:41 [SDK]: §7.2 — зернистость `noise` (`FfmpegExportVideoGrainId` off/light/medium/strong): `resolveFfmpegExportVideoGrainFilter`, порядок `-vf` …→eq→grain→scale→fps; `ffmpegExportVideoGrain` в settings, IPC `settingsSetFfmpegExportVideoGrain`, preload/App rail «Зерно», snapshot/merge/resolve/export, Vitest +4; чеклист §7.2. `npm run check:quiet` ок (35 files / 303 tests).
- [J-330] 2026-05-12 01:05:23 [SDK]: §7.2 — `vignette` (`FfmpegExportVideoVignetteId`): `resolveFfmpegExportVideoVignetteFilter`, порядок `-vf` …→grain→vignette→scale→fps; `ffmpegExportVideoVignette`, IPC `settingsSetFfmpegExportVideoVignette`, preload/App «Виньетка», snapshot/merge/resolve/export/preview; Vitest. Спринт §7.2. `npm run check:quiet` ок (35 files / 305 tests).
- [J-331] 2026-05-12 01:09:17 [SDK]: §7.2 — `gblur` (`FfmpegExportVideoBlurId`): `resolveFfmpegExportVideoBlurFilter`, порядок `-vf` …→vignette→blur→scale→fps; `ffmpegExportVideoBlur`, IPC `settingsSetFfmpegExportVideoBlur`, preload/App «Размытие», snapshot/merge/resolve/export/preview; Vitest +5; чеклист §7.2/спринт/счётчик тестов. `npm run check:quiet` ок (35 files / 307 tests).
- [J-332] 2026-05-12 01:13:33 [SDK]: §7.2 — whitelist `hue` (`FfmpegExportVideoHueId` warmShift/coolShift/satBoost): `resolveFfmpegExportVideoHueFilter`, порядок `-vf` …→eq→hue→grain→vignette→blur→scale→fps; `ffmpegExportVideoHue`, IPC `settingsSetFfmpegExportVideoHue`, preload/App «Оттенок», snapshot/merge/resolve/export/preview; Vitest +2; чеклист §7.2/спринт/308 tests. `npm run check:quiet` ок.
- [J-333] 2026-05-12 01:18:10 [SDK]: §7.2 — деинтерлейс `yadif` (`FfmpegExportVideoDeinterlaceId` off/frame/field): `resolveFfmpegExportVideoDeinterlaceFilter`, порядок `-vf` transform→crop→**deinterlace**→denoise→…; settings/IPC/preload/App «Деинтерлейс», snapshot/merge/resolve/export/preview; Vitest; чеклист §7.2/спринт/310 tests. `npm run check:quiet` ок.
- [J-334] 2026-05-12 01:20:03 [SDK]: §9 — сводка дорожки (`detail`): `formatFfprobeStreamDurationDetail` + `ffprobe-stream-duration-detail` (Vitest): `dur …s` из ffprobe `duration`, если отличается от `format.duration`; для вложений/прочих — теги `filename`/`mimetype`; `ffprobe-service`/`buildTrackRows`. Спринт §9, счётчики тестов. `npm run check:quiet` ок.
- [J-335] 2026-05-12 01:21:39 [SDK]: §9 — видео `detail`: `formatFfprobeVideoFpsDetail` в `ffprobe-video-fps` (оба `avg_frame_rate`/`r_frame_rate`, если расходятся >0.05 fps — строка `a / b fps`); `buildTrackDetail` + Vitest; спринт §9 и счётчик **316 tests**. `npm run check:quiet` ок.
- [J-336] 2026-05-12 01:25:45 [SDK]: §7.2 — whitelist `histeq` (`FfmpegExportVideoHisteqId`): `resolveFfmpegExportVideoHisteqFilter`, порядок `-vf` …→deband→**histeq**→lut3d→…; `ffmpegExportVideoHisteq` в settings, IPC `settingsSetFfmpegExportVideoHisteq`, preload/App rail «Histeq» после Deband, snapshot/merge/resolve/export/preview; Vitest +2 и обновлён интеграционный vf; чеклист §7.2/спринт/**318 tests**. `npm run check:quiet` ок.
- [J-337] 2026-05-12 01:27:31 [SDK]: §9 — сводка аудиодорожки (`detail`): при наличии в JSON ffprobe — `codec_tag_string` (FourCC контейнера); `buildTrackRows` экспортирован для Vitest; `tests/main/ffprobe-service.test.ts` +2; чеклист §9/счётчики **37 files / 320 tests**. `npm run check:quiet` ок.
- [J-338] 2026-05-12 01:29:06 [SDK]: §9 — `buildTrackDetail`: тег `tags.encoder` в «Сведения» для видео/аудио/субтитров (`formatFfprobeTagEncoderBrief`, обрезка 64 симв.); Vitest +2; чеклист §9/спринт/**322 tests**. `npm run check:quiet` ок.
- [J-339] 2026-05-12 01:30:10 [SDK]: §9 — сводка видеодорожки (`detail`): при наличии `tags.timecode` — компактно `TC …` в «Сведения»; `buildTrackDetail` + Vitest; чеклист §9/спринт/**323 tests**. `npm run check:quiet` ок.
- [J-340] 2026-05-12 01:31:39 [SDK]: §9 — `buildTrackDetail`: видео — в `detail` добавлен нестандартный `pix_fmt` (всё кроме `yuv420p`/`yuvj420p`, чтобы не дублировать типичный 8-bit 4:2:0); вложения/прочие потоки — префикс `codec_name` перед `filename`/тегами; Vitest +3; чеклист §9/спринт/**326 tests**. `npm run check:quiet` ок.
- [J-341] 2026-05-12 01:33:32 [SDK]: §9 — видео «Сведения»: компактные HDR-метки `PQ`/`HLG`/`PQ·bt2020`/`HLG·bt2020` из `color_transfer`/`color_trc` + `bt2020` primaries (`formatFfprobeVideoHdrColorBrief` в shared, `ffprobe-service` + Vitest shared/main). Спринт §9/чеклист. `npm run check:quiet` ок (**38 files / 331 tests**).
- [J-342] 2026-05-12 01:35:19 [SDK]: §9 — сводка видео (`detail`): для не-HDR при `color_range` pc/jpeg — метка `full range` (`formatFfprobeVideoFullRangeBrief`, без дубля с HDR); `ffprobe-service` + Vitest shared/main. Спринт §9. `npm run check:quiet` ок (**335 tests**).
- [J-343] 2026-05-12 01:36:54 [SDK]: §9 — не-HDR: в «Сведения» видео склейка не-bt709 `color_primaries`/`color_space` (`formatFfprobeVideoSdGamutBrief`, без дубля HDR); `ffprobe-service` + Vitest; чеклист §9/счётчики **38 test files / 340 tests**. `npm run check:quiet` ок.
- [J-344] 2026-05-12 01:38:24 [SDK]: §9 — «Сведения» видео/аудио: `tags.creation_time` → компактно `created YYYY-MM-DD` (`formatFfprobeCreationTimeBrief` в shared, Vitest shared/main); чеклист §9. `npm run check:quiet` ок (**39 test files / 346 tests**).
- [J-345] 2026-05-12 01:42:43 [SDK]: §8 — первый срез терминала/CLI: новая вкладка Electron, preload/main IPC, `terminal-service` с allowlist `ffmpeg`/`ffprobe`/`yt-dlp`, запуск через `execFile` без shell, PATH с папкой выбранного движка, подсказки из `Data/ffmpeg_commands.json` + `Data/ytdlp_commands.json`, история сессии и вывод stdout/stderr. Чеклист §8/спринт. `npm run check:quiet` ок (**39 test files / 346 tests**).
- [J-346] 2026-05-12 01:45:21 [SDK]: §8 — argv `TERMINAL_CURRENT_FILE_PLACEHOLDER` (`__CURRENT_FILE__`): подстановка с `isGrantedMediaPath`, `TerminalRunRequest.currentFilePath` из превью редактора, кнопка «Превью-файл» + подсказка UI; `logWarn`/`logInfo` в scope `terminal` (blocked / run); `resolveTerminalCurrentFileArgs` + Vitest `tests/main/terminal-service.test.ts`; IPC `exactOptionalPropertyTypes`. Чеклист §8/спринт. `npm run check:quiet` ок (**40 test files / 350 tests**).
- [J-347] 2026-05-12 01:47:33 [SDK]: §8 — `logs/terminal-cli.log` в userData: каждый прогон/блокировка терминала (`appendTerminalCliSessionLog`, ротация ~512 KiB), stderr в логе; Support ZIP: `logs/terminal-cli.log` + поле `terminalCliLogFile` в `diagnostics.txt`; рендерер: при медиа в превью подсказки CLI сортируют **ffprobe** выше ffmpeg/yt-dlp; Vitest terminal/support-bundle. Чеклист §8/спринт. `npm run check:quiet` ок (**351 tests**).
- [J-348] 2026-05-12 01:49:20 [SDK]: §8 — сортировка подсказок CLI в боковой панели терминала с учётом **активной главной вкладки**: при `workspaceTab === 'downloads'` приоритет **yt-dlp** → ffmpeg → ffprobe; на «Редактор»/«Терминал» — прежняя схема (при медиа в превью ffprobe сверху). `App.tsx` `terminalHintToolRank` + `visibleTerminalHints` deps. Чеклист §8/спринт. `npm run check:quiet` ок (**351 tests**).
- [J-349] 2026-05-12 01:51:28 [SDK]: §8 — сценарные подсказки терминала: опциональное `fullLine` в `TerminalCommandHintEntry`, парсинг в `terminal-service` `readHints`, константы `TERMINAL_SCENARIO_HINTS_DOWNLOADS` / `TERMINAL_SCENARIO_HINTS_PREVIEW_MEDIA` в `terminal-contract`, merge+поиск+клик (целая строка vs токен) в `App.tsx`. Чеклист §8/спринт. `npm run check:quiet` ок (**351 tests**).
- [J-350] 2026-05-12 01:52:44 [SDK]: §8 — расширены сценарные подсказки: `yt-dlp -J ` (JSON метаданные); превью — `ffprobe … -show_chapters -show_format` и `ffmpeg -nostats -i … -t 10 -f null -` (decode smoke). `terminal-contract` + спринт-строка чеклиста §8. `npm run check:quiet` ок.
- [J-351] 2026-05-12 01:54:11 [SDK]: §8 — сценарии терминала: +`yt-dlp -v --skip-download`, +`yt-dlp --simulate`; превью — ffprobe `v:0` width/height/r_frame_rate/pix_fmt; `tests/shared/terminal-contract-scenarios.test.ts` (кавычки/плейсхолдер); чеклист §8/счётчики **41 files / 353 tests**. `npm run check:quiet` ок.
- [J-352] 2026-05-12 01:55:29 [SDK]: §8 — сценарные подсказки: «Загрузки» +`yt-dlp --list-subs `; превью — ffprobe `a:0` compact (codec_name/sample_rate/channels); чеклист §8/спринт (7+5 шаблонов). `npm run check:quiet` ок (**353 tests**).
- [J-353] 2026-05-12 01:56:17 [SDK]: §8 — сценарные подсказки: «Загрузки» +`yt-dlp --flat-playlist -J ` (плоский плейлист + JSON); превью — ffprobe `s:0` compact (codec_name/codec_tag_string); `terminal-contract` + спринт-строка §8. `npm run check:quiet` ок (**353 tests**).
- [J-354] 2026-05-12 01:57:25 [SDK]: §8 — сценарные подсказки: «Загрузки» +`--no-playlist -F`, +`--flat-playlist -F`, +`--list-thumbnails`; превью — ffprobe «streams compact» (index/codec_type/codec_name, `-of compact`); `terminal-contract` + спринт §8. `npm run check:quiet` ок (**353 tests**).
- [J-355] 2026-05-12 02:00:31 [SDK]: §8 — сценарные подсказки: «Загрузки» +`--skip-download --print title`, +`--skip-download --print duration_string`; превью — ffprobe `-of json -show_format -show_streams` и `-v error -show_error` (компактная диагностика контейнера/потока с плейсхолдером); `terminal-contract` + `tests/shared/terminal-contract-scenarios.test.ts` (+2 смок-кейса) + спринт §8 (13 yt-dlp / 9 ffprobe). `npm run check:quiet` ок (**355 tests**).
- [J-356] 2026-05-12 02:01:32 [SDK]: §8 — сценарные подсказки: «Загрузки» +`--skip-download --print uploader` / `--print id`; превью — ffprobe `a:0` `stream_tags=language`; `terminal-contract` + Vitest + спринт §8 (**15 yt-dlp / 10 ffprobe**). `npm run check:quiet` ок (**355 tests**).
- [J-357] 2026-05-12 02:02:49 [SDK]: §8 — сценарные подсказки: «Загрузки» +`--print webpage_url` / `--print channel` / `--print channel_id`; превью — ffprobe **v:0 color** (color_space/primaries/transfer) и **a:0 pcm** (bits_per_sample+sample_fmt); `terminal-contract` + Vitest + спринт §8 (**18 yt-dlp / 12 ffprobe**). `npm run check:quiet` ок (**355 tests**).
- [J-358] 2026-05-12 02:04:02 [SDK]: §8 — сценарные подсказки: «Загрузки» +`--print thumbnail` / `--print view_count` / `--print upload_date`; превью — ffprobe **v:0 bitrate+fps** (`bit_rate`+`avg_frame_rate`); `terminal-contract` + Vitest + спринт §8 (**21 yt-dlp / 13 ffprobe**). `npm run check:quiet` ок (**355 tests**).
- [J-359] 2026-05-12 02:05:33 [SDK]: §8 — сценарные подсказки: «Загрузки» +`--print playlist_title` / `--print playlist_count` / `--print filename`; превью — ffprobe **v:0 sar+dar** и **format_tags title+encoder**; `terminal-contract` + Vitest + спринт §8 (**24 yt-dlp / 15 ffprobe**). `npm run check:quiet` ок (**355 tests**).
- [J-360] 2026-05-12 02:06:59 [SDK]: §8 — сценарные подсказки: «Загрузки» +`--print description` / `--print categories` / `--print language`; превью — ffprobe **v:0 field_order+color_range**, **s:0** `stream_tags=title,language`; `terminal-contract` + Vitest + спринт §8 (**27 yt-dlp / 17 ffprobe**). `npm run check:quiet` ок (**355 tests**).
- [J-361] 2026-05-12 02:08:14 [SDK]: §8 — сценарные подсказки: «Загрузки» +`--print extractor` / `--print playlist_id` / `--geo-bypass -F`; превью — ffprobe **a:1** (codec/rate/channels) и **v:0 stream_tags** handler_name+encoder; `terminal-contract` + Vitest + спринт §8 (**30 yt-dlp / 19 ffprobe**). `npm run check:quiet` ок (**355 tests**).
- [J-362] 2026-05-12 02:09:26 [SDK]: §8 — сценарные подсказки: «Загрузки» +`--print format_id` / `ext` / `resolution` / `vcodec` / `acodec`; превью — ffprobe **v:0 nb_frames+duration**, **format start_time+duration**, **s:1** codec+tag; чеклист §8: подстановка превью и логирование CLI → `[x]`; спринт-строка §8 (**35 yt-dlp / 22 ffprobe**). `npm run check:quiet` ок (**355 tests**).
- [J-363] 2026-05-12 02:10:58 [SDK]: §8 — сценарные подсказки: «Загрузки» +`--dump-single-json`; превью — ffprobe **streams+attach** (compact `stream_tags=filename,mimetype` для вложений/data); `terminal-contract` + Vitest + спринт §8 (**36 yt-dlp / 23 ffprobe**). `npm run check:quiet` ок (**355 tests**).
- [J-364] 2026-05-12 02:12:38 [SDK]: §8 — сценарные подсказки «Загрузки»: +`--list-extractors`, +`--version`, +`-4 -F`, +`--no-cache-dir -F`; `terminal-contract` + Vitest + спринт §8 (**40 yt-dlp**). `npm run check:quiet` ок.
- [J-365] 2026-05-12 02:14:28 [SDK]: §8 — сценарные подсказки «Загрузки»: +`--print tags` / `--print filesize_approx` / `--ignore-errors --flat-playlist -J` / `--write-info-json --skip-download` / `--no-warnings -F`; превью — ffprobe **v:0** codec/profile/level, **a:0** profile+bit_rate, **v:0** refs+has_b_frames; `terminal-contract` + Vitest + спринт §8 (**45 yt-dlp / 26 ffprobe**). `npm run check:quiet` ок (**355 tests**).
- [J-366] 2026-05-12 02:16:55 [SDK]: §8 — сценарные подсказки: «Загрузки» +`--print fps` / `--print is_live` / `--print live_status` / `--print availability` / `--print age_limit`; превью — ffprobe **streams disposition** (default/forced/captions/attached_pic), **format nb_streams+nb_programs+format_name**, **v:0 count_frames** (`-count_frames` → `nb_read_frames`); `terminal-contract` + Vitest + спринт §8 (**50 yt-dlp / 29 ffprobe**). `npm run check:quiet` ок (**355 tests**).
- [J-367] 2026-05-12 02:19:09 [SDK]: §8 — сценарные подсказки: «Загрузки» +`--print like_count` / `comment_count` / `aspect_ratio`, +`--playlist-items 1 -F`, +`--extractor-args youtube:player_client=web -F`; превью — ffprobe **a:0 disposition**, **v:0 pix_fmt+color_space+color_range**; `terminal-contract` + Vitest + спринт §8 (**55 yt-dlp / 31 ffprobe**). `npm run check:quiet` ок (**355 tests**).
- [J-368] 2026-05-12 02:20:28 [SDK]: §8 — сценарные подсказки: «Загрузки» +cookies **edge**, `--print` **duration**/**width**/**height**/**tbr**; превью — ffprobe **v:0 coded+display**, **format creation_time**, **s:0 disposition**; `terminal-contract` + Vitest + спринт §8 (**60 yt-dlp / 34 ffprobe**). `npm run check:quiet` ок (**355 tests**).
- [J-369] 2026-05-12 02:22:44 [SDK]: §8 — сценарные подсказки: «Загрузки» +`--print` **abr**/**vbr**/**asr**, +`--write-thumbnail --skip-download`, +`--write-auto-sub --skip-download`; превью — ffprobe **v:0 time_base+start_pts**, **a:0 time_base+start_pts**, **v:0 bit_rate+max_bit_rate**; `terminal-contract` + Vitest + спринт §8 (**66 yt-dlp / 37 ffprobe**). `npm run check:quiet` ок (**355 tests**).
- [J-370] 2026-05-12 02:25:20 [SDK]: §8 — сценарные подсказки «Загрузки»: +`--write-description` / `--write-url-link` / `--check-formats` / cookies **firefox** / `--print` **has_drm** / **playable_in_embed**; превью — ffprobe **format=filename**, **v:0 stream_tags=rotate**, **a:0 channel_layout+bit_rate**; `terminal-contract` + Vitest + спринт §8 (**72 yt-dlp / 40 ffprobe**). `npm run check:quiet` ок (**355 tests**).
- [J-371] 2026-05-12 02:27:00 [SDK]: §8 — сценарные подсказки «Загрузки»: +`--print` **channel_url** / **uploader_id** / **was_live** / **media_type** / **release_year**, +`--no-check-certificates -F`; превью — ffprobe **format=bit_rate**, **a:0 stream_tags=title,handler_name**, **v:0 r_frame_rate**; `terminal-contract` + Vitest + спринт §8 (**78 yt-dlp / 43 ffprobe**). `npm run check:quiet` ок (**355 tests**).
- [J-372] 2026-05-12 02:28:41 [SDK]: §8 — сценарные подсказки «Загрузки»: +`--print` **filesize** / **format_note** / **subtitles** / **automatic_captions** / **chapters**, +`--flat-playlist --skip-download --print title`; превью — ffprobe **format_tags major_brand+compatible_brands**, **s:2 compact**, **v:0 closed_captions+is_avc**; `terminal-contract` + Vitest + спринт §8 (**84 yt-dlp / 46 ffprobe**). `npm run check:quiet` ок (**355 tests**).
- [J-373] 2026-05-12 02:30:21 [SDK]: §8 — сценарные подсказки «Загрузки»: +`--print original_url` / `webpage_url_domain`, +`--flat-playlist --skip-download --print id`, +`--print playlist_index`, +`--write-sub --skip-download`, +`--write-comments --skip-download`; превью — ffprobe **t:0** и **d:0** (codec_name+codec_tag_string); `terminal-contract` + Vitest + спринт §8 (**90 yt-dlp / 48 ffprobe**). `npm run check:quiet` ок (**355 tests**).
- [J-374] 2026-05-12 02:31:58 [SDK]: §8 — сценарные подсказки: «Загрузки» +`-g -f bestaudio/best`, +`--flat-playlist --simulate`, +`--skip-download --print filepath` / `epoch`; превью — ffprobe **v:0 codec_tag_string**, **format=probe_score**, **a:2** compact; `terminal-contract` + Vitest + спринт §8 (**94 yt-dlp / 51 ffprobe**). `npm run check:quiet` ок (**356 tests**).
- [J-375] 2026-05-12 02:34:05 [SDK]: §8 — сценарные подсказки «Загрузки»: +`-6 -F`, +`--flat-playlist --print url`, +`--write-pages --skip-download`, +`--print heatmap`; превью — ffprobe **format_long_name**, **v:0 chroma_location**, ffmpeg **-frames:v 1** null decode; `terminal-contract` + Vitest + спринт §8 (**98 yt-dlp / 53 ffprobe**). `npm run check:quiet` ок (**356 tests**).
- [J-376] 2026-05-12 02:36:07 [SDK]: §8 — сценарные подсказки «Загрузки»: +`--limit-rate 1M`, +`--retries 10 --fragment-retries 10`, +`--socket-timeout 30`, +`--force-ipv4 -F`, +`--no-part -F`; превью — ffprobe **-show_programs** compact, **v:0 side_data_list**, **-show_chapters -of csv**; `terminal-contract` + Vitest + спринт §8 (**103 yt-dlp / 56 ffprobe**). `npm run check:quiet` ок (**356 tests**).
- [J-377] 2026-05-12 02:38:02 [SDK]: §8 — сценарные подсказки «Загрузки»: +`--concurrent-fragments 4`, +`--merge-output-format mkv`, +`--prefer-free-formats -F`, +`--format-sort +res:720 -F`, +`--hls-prefer-native -F`, +`--live-from-start`, +`--sleep-requests 1`, +`--playlist-end 10 -J`; превью — ffprobe **v:0/a:0 stream=start_time,duration**, **v:0 bits_per_raw_sample**; `terminal-contract` + Vitest + спринт §8 (**110 yt-dlp / 59 ffprobe**). `npm run check:quiet` ок (**356 tests**).
- [J-378] 2026-05-12 02:39:57 [SDK]: §8 — сценарные подсказки «Загрузки»: +`--geo-bypass-country US -F`, +`--extractor-retries 5`, +`--http-chunk-size 10M`, +`--no-overwrites -F`, +`--windows-filenames -F`, +`--newline -F`, +`--skip-unavailable-fragments`, +`--download-archive archive.txt`, +`--break-on-reject -F`, +`--trim-file-names 80 -F`; превью — ffprobe **v:1** compact, **format size+duration**, ffmpeg **-t 5 -c copy** null remux; `terminal-contract` + Vitest + спринт §8 (**120 yt-dlp / 61 ffprobe**). `npm run check:quiet` ок (**356 tests**).
- [J-379] 2026-05-12 02:41:44 [SDK]: §8 — сценарные подсказки «Загрузки»: +`--no-mtime`, +`--continue`, +`--abort-on-error`, +`--playlist-start 5 --playlist-end 15 -J`, +`--max-filesize 512M -F`, +`--restrict-filenames -F`, +`--color never -F`, +`--embed-metadata`, +`--embed-thumbnail`, +`--wait-for-video 10`, +`--skip-playlist-after-errors 5`, +`--output-na-placeholder NA --skip-download --print title`; превью — ffprobe **s:1** codec+language, **a:1 disposition**, **format_tags=language**; ffmpeg **-err_detect ignore_err -t 2** null decode; `terminal-contract-scenarios.test.ts` + спринт §8 (**132 yt-dlp / 64 ffprobe / 4 ffmpeg**). `npm run check:quiet` ок (**357 tests**).
- [J-380] 2026-05-12 02:44:14 [SDK]: §8 — сценарные подсказки «Загрузки»: +`--referer`, +`--add-header`, +`--proxy`, +`--impersonate chrome`, +`--match-filter`, +`--batch-file`, +`--load-info-json`, +`--yes-playlist -J`, +`--no-config`, +`--cookies` (файл), +`--sleep-interval`, +`--age-limit` (все `fullLine` без кавычек); превью — ffprobe **format_tags artist+album**, **v:0 avg_frame_rate**; ffmpeg **-vn -sn -t 3** audio smoke; Vitest + спринт §8 (**144 yt-dlp / 66 ffprobe / 5 ffmpeg**). `npm run check:quiet` ок (**357 tests**).
- [J-381] 2026-05-12 02:46:05 [SDK]: §8 — сценарные подсказки: «Загрузки» +`--lazy-playlist -J`, +`--print` season_number/episode_number/track/artists/album, +`--remux-video mkv`, +`--sub-format srt -F`, +`--convert-thumbnails jpg`, +`--force-ipv6 -F`; превью — ffprobe **v:0 codec_long_name**, **format_tags=encoder**, **a:3**/**s:3** compact; ffmpeg **-an -sn -t 2** video decode smoke; Vitest + спринт §8 (**154 / 70 / 6**). `npm run check:quiet` ок (**357 tests**).
- [J-382] 2026-05-12 02:48:59 [SDK]: §8 — сценарные подсказки «Загрузки» +`--dateafter 20240101 -F` / `--max-downloads 5` / `--match-title trailer -F` / `--write-link --skip-download` / `--sponsorblock-mark all` / `--extract-audio --audio-format mp3` / `--audio-quality 192K --extract-audio` / `--print n_entries`; превью — ffprobe **format pretty**/**format flat**/**v:0 -show_packets -read_intervals %+#5**/**v:0 -show_frames -read_intervals %+#5**, ffmpeg **-af loudnorm=print_format=summary -t 60 -vn -sn**; `terminal-contract` + `tests/shared/terminal-contract-scenarios.test.ts` (+9 проверок) + спринт §8 (**162 yt-dlp / 74 ffprobe / 7 ffmpeg**). `npm run check:quiet` ок (**358 tests**).
- [J-383] 2026-05-12 02:54:22 [SDK]: §8 — IntelliSense v1 в строке argv терминала: merge JSON+сценарии (`terminalMergedSortedHints`) → `filterTerminalInlineSuggestions` (до 14), выпадающий список под полем (Tab/↑↓, Escape, клик), `applyTerminalInlinePick` в `shared/terminal-inline-suggest.ts` + Vitest `terminal-inline-suggest`; `App.tsx`/`main.css`, чеклист §8/спринт/этап 6 и счётчики **42 files / 369 tests**. `npm run check:quiet` ок.
- [J-384] 2026-05-12 02:57:14 [SDK]: §8 — сценарные подсказки «Загрузки»: +`--embed-chapters`, +`--mark-watched --skip-download`, +`--write-all-thumbnails --skip-download`, +`--no-check-formats -F`, +`--playlist-reverse|-random -J`, +`--user-agent curl/8.5.0 -F`, +`--throttled-rate 100K -F`; превью — ffprobe **-show_program_version**, **a:0 packets %+#3**, ffmpeg **-ss 10 -t 2** seek-smoke; `terminal-contract` + Vitest + спринт §8 (**170 / 76 / 8**). `npm run check:quiet` ок (**370 tests**).
- [J-385] 2026-05-12 02:59:36 [SDK]: §8 — сценарные подсказки «Загрузки»: post-process/вывод без кавычек в `fullLine` — `--embed-subs`, `--convert-subs srt`, `-o %(title)s.%(ext)s`, `--split-chapters`, `--remove-chapters sponsor`, `--write-playlist-metafiles`, `--force-overwrites`, `--no-continue`, `--recode-video mp4`, `--download-sections *0:00-2:00`, `--break-match-filters`, `--no-post-overwrites`; `terminal-contract` + Vitest + спринт §8 (**182 / 76 / 8**). `npm run check:quiet` ок (**370 tests**).
- [J-386] 2026-05-12 03:02:28 [SDK]: §8 — сценарные подсказки «Загрузки»: +`--add-metadata`, +`--hls-prefer-ffmpeg -F`, +`--ffmpeg-location ffmpeg`, +`--paths home:ytdl-out`, +`--no-download-archive`, +`--encoding utf-8`, +`--break-per-input -F`, +`--check-all-formats -F`, +`--socket-timeout 60`, +`--xattrs`; превью — ffprobe **format_tags=comment,synopsis**, **s:0 codec_time_base+time_base**, **v:0 extradata_size+initial_padding**; `terminal-contract` + Vitest + спринт §8 (**192 / 79 / 8**). `npm run check:quiet` ок (**371 tests**).
- [J-387] 2026-05-12 03:03:54 [SDK]: §9 — сводка дорожек: в `buildTrackDetail` для **subtitle** добавлен `formatFfprobeCreationTimeBrief` (`tags.creation_time` → `created YYYY-MM-DD`), как для видео/аудио; Vitest `ffprobe-service.test.ts`. `npm run check:quiet` ок (**371 tests**).
- [J-388] 2026-05-12 03:06:09 [SDK]: §9 — сводка субтитров: в `buildTrackDetail` для **subtitle** добавлен `tags.handler_name` (обрезка как у encoder, без дубля с `title`); `collapseFfprobeDetailSnippet` + Vitest `ffprobe-service.test.ts` (+2). Чеклист §9/спринт. `npm run check:quiet` ок (**373 tests**).
- [J-389] 2026-05-12 03:07:53 [SDK]: §8 — сценарные подсказки «Загрузки»: +`yt-dlp -U`, +`--compat-options no-youtube-unavailable-videos -F`; превью — ffprobe **s:0 bit_rate**; `terminal-contract` + Vitest + спринт §8 (**194 / 80 / 8**). `npm run check:quiet` ок (**373 tests**).
- [J-390] 2026-05-12 03:10:22 [SDK]: §8 — сценарные подсказки «Загрузки»: +`yt-dlp --rm-cache-dir`, +`--cache-dir cache -F`, +`--keep-fragments -F`, +`--buffer-size 16K -F`, +`--abort-on-unavailable-fragments`, +`--sub-langs en.*,ru.* -F`, +`--print release_date` / `album_artist` / `track_number`; превью — ffprobe **format=duration_ts**, **format_tags=copyright**, **v:0 stream_tags=BPS,DURATION**, **s:0 stream_tags=duration**, **format=probe_size**, ffmpeg **scale=320:-1 -t 1 -f null -**; `terminal-contract` + Vitest + спринт §8 (**204 yt-dlp / 85 ffprobe / 9 ffmpeg**). `npm run check:quiet` ок (**373 tests**).
- [J-391] 2026-05-12 03:13:01 [SDK]: §8 — копирование строки из лога терминала: stdout/stderr разбиты по `\n`, строка + кнопка «Копир.» (hover/focus-visible) → `clipboard.writeText`, подсказка в шапке вкладки; стили `main.css`. Спринт §8. `npm run check:quiet` ок (**373 tests**).
- [J-392] 2026-05-12 03:15:03 [SDK]: §8 — сценарные подсказки «Загрузки»: cookies **brave**; `--print` **series**/**season**/**episode**/**display_id**/**webpage_url_basename**/**fulltitle**; **--sponsorblock-remove sponsor**; **--downloader native -F**; **--legacy-server-connect -F**; **--no-call-home -F**; **--datebefore 20991231 -F**; **--embed-info-json**; `terminal-contract` + Vitest + спринт §8 (**218 yt-dlp** / 85 ffprobe / 9 ffmpeg). `npm run check:quiet` ок (**373 tests**).
- [J-393] 2026-05-12 03:17:03 [SDK]: §8 — IntelliSense argv: экспорт `DEFAULT_TERMINAL_INLINE_SUGGEST_MAX`, `stepTerminalSuggestIndex` (clamp stale-индекса при сужении списка + Home/End), `App.tsx` подсказка/клавиши, Vitest `terminal-inline-suggest.test.ts`, чеклист §8/спринт. `npm run check:quiet` ок (**378 tests**).
- [J-394] 2026-05-12 03:19:02 [SDK]: §9 — сводка дорожек: в `buildTrackDetail` для видео/аудио добавлена метка `max …` при заметном превышении `max_bit_rate` над номинальным `bit_rate` (без дубля с колонкой битрейта); Vitest `ffprobe-service.test.ts` (+3). Спринт §9. `npm run check:quiet` ок (**381 tests**).
- [J-395] 2026-05-12 03:21:29 [SDK]: §8 — сценарные подсказки «Загрузки»: +`--netrc -F`, +`--force-generic-extractor -F`, +`--skip-download --print` channel_follower_count / average_rating / is_private, +`--write-all-urls --skip-download`, +`--dump-pages --skip-download`, +`--no-progress -F`; превью — ffprobe **v:0 stream_tags=creation_time**, **format_tags=handler_name**, ffmpeg **-vn -sn -acodec copy -t 3** null; `terminal-contract` + Vitest + спринт §8 (**226 yt-dlp / 87 ffprobe / 10 ffmpeg**). `npm run check:quiet` ок (**382 tests**).
- [J-396] 2026-05-12 03:24:09 [SDK]: §8 — сценарные подсказки «Загрузки»: +`--no-playlist -J`, +`--hls-use-mpegts -F`, +`--write-subs --skip-download`, +`--max-sleep-interval 10 -F`, +`--retry-sleep 5 -F`, +`--min-filesize 100K -F`, +`--file-access-retries 5 -F`; превью — ffprobe **a:0 bits_per_raw_sample**, **v:0 index+codec_name**; `terminal-contract` + Vitest + спринт §8 (**231 yt-dlp / 89 ffprobe / 10 ffmpeg**). `npm run check:quiet` ок (**384 tests**).
- [J-397] 2026-05-12 03:26:30 [SDK]: §8 — сценарные подсказки «Загрузки»: `--print` **playlist** / **playlist_autonumber** / **modified_timestamp** / **release_timestamp** / **upload_timestamp** / **stretched_ratio** / **location** (`--skip-download`); превью — ffprobe **v:0 profile+level**, **s:2 disposition**; `terminal-contract` + Vitest + спринт §8 (**238 / 91 / 10**). `npm run check:quiet` ок (**386 tests**).
- [J-398] 2026-05-12 03:28:12 [SDK]: §9 — сводка дорожек: в `buildTrackDetail` для видео/аудио/субтитров добавлен ненулевой `start_pts` как `pts …@time_base`; Vitest `ffprobe-service.test.ts` (+2). Спринт §9. `npm run check:quiet` ок (**388 tests**).
- [J-399] 2026-05-12 03:31:28 [SDK]: §8 — сценарные подсказки «Загрузки»: +`--extractor-args youtube:player_client=android|tv_embedded|ios -F`, `--print` alternate_title / extractor_key / channel_is_verified, +`--flat-playlist --skip-download --print webpage_url`, +`--geo-bypass-country DE -F`; превью — ffprobe **a:2 disposition**, **v:1 profile+level**, **s:1 start_time+duration**, ffmpeg **-map 0:v:0 -c:v copy**; `terminal-contract` + Vitest + спринт §8 (**246 / 94 / 11**). `npm run check:quiet` ок (**390 tests**).
- [J-400] 2026-05-12 03:33:39 [SDK]: §8 — сценарные подсказки «Загрузки»: cookies **opera|chromium|vivaldi**, +`--geo-bypass-country FR -F`, +`--extractor-args youtube:player_client=mweb -F`, `--print` **requested_formats** / **requested_subtitles**; превью — ffprobe **s:0 time_base+start_pts**, ffmpeg **volumedetect -t 10**; `terminal-contract` + Vitest + спринт §8 (**254 / 95 / 12**). `npm run check:quiet` ок (**392 tests**).
- [J-401] 2026-05-12 03:36:14 [SDK]: §8 — сценарные подсказки «Загрузки»: cookies **safari**; **web_creator** / **web_embedded** `-F`; **GB** `-F`; `--print formats`; `--simulate -f bestvideo+bestaudio/best`; `--multi-streams -F`; `--compat-options 2024 -F`; **no-playlist** `--print title`; **flat-playlist** `--print extractor`; превью — ffprobe **format_tags=genre,date**, ffmpeg **silencedetect** 30s; `terminal-contract` + Vitest + спринт §8 (**264 / 96 / 13**). `npm run check:quiet` ок (**394 tests**).
- [J-402] 2026-05-12 03:38:07 [SDK]: §8 — сценарные подсказки «Загрузки»: +`--no-remote-playlist -J`, +`--geo-bypass-country` **JP**/**CA** `-F`, `--print thumbnails`, **web_safari** `-F`, `--print` playlist_channel / playlist_channel_id / playlist_uploader / playlist_uploader_id, flat `--print _type`; превью — ffprobe **v:0 disposition**, **a:1 bit_rate**, ffmpeg **astats** 5s; `terminal-contract` + Vitest + спринт §8 (**274 / 98 / 14**). `npm run check:quiet` ок (**396 tests**).
- [J-403] 2026-05-12 03:41:12 [SDK]: §8 — сценарные подсказки «Загрузки»: +`--merge-output-format mp4`, +`--no-keep-video`, +`--external-downloader ffmpeg`, +`--parse-metadata title:%(title)s`, **geo** AU/BR/IT `-F`, `--print` playlist_webpage_url / webpage_url_scheme, `--video-multistreams` / `--audio-multistreams` `-F`; превью — ffprobe **a:0 stream_tags=encoder**, ffmpeg **ebur128** 12s; `terminal-contract` + Vitest + спринт §8 (**284 / 99 / 15**). `npm run check:quiet` ок (**398 tests**).
- [J-404] 2026-05-12 03:43:21 [SDK]: §8 — сценарные подсказки «Загрузки»: +`--quiet -F`, +`--no-cookies -F`, +`--compat-options 2025 -F`, +`--break-on-existing -F`, +`--mtime`, +`--check-formats-threshold 1.5 -F`, +`--no-sponsorblock -F`, +`--allow-dynamic-mpd -F`; превью — ffprobe **a:0|s:0 codec_long_name**, ffmpeg **aphasemeter** 10s; `terminal-contract` + Vitest + спринт §8 (**292 / 101 / 16**). `npm run check:quiet` ок (**400 tests**).
- [J-405] 2026-05-12 03:46:16 [SDK]: §8 — сценарные подсказки «Загрузки»: +`--console-title -F` / `--no-external-downloader -F` / `--clean-infojson` / `--no-write-info-json -F` / `--external-downloader-args ffmpeg_i:-nostdin -F` / `--flat-playlist --print urls --skip-download` / `--progress-template predownload:Preparing %(info.title)s -F` / `--sleep-subtitles 5 -F` / `--sub-format best -F` / `--geo-bypass-country NL -F`; превью — ffprobe **a:1 stream_tags=encoder**, ffmpeg **idet** 5s; `terminal-contract` + Vitest + спринт §8 (**302 / 102 / 17**). `npm run check:quiet` ок (**402 tests**).
- [J-406] 2026-05-12 03:49:44 [SDK]: §8 — сценарные подсказки «Загрузки»: +`--force-keyframes-at-cuts`, +`--no-hls-use-mpegts -F`, +`--compat-options no-direct-merge -F`, гео **ES**/**PL**/**SE** `-F`, +`--no-embed-metadata`, +`--playlist-items 1:10 -F`, +`--merge-output-format webm`, +`--ignore-no-formats-error -F`, +`--no-write-thumbnail -F`, +`--extract-audio --audio-format aac`, +`--no-embed-thumbnail`; превью — ffprobe **format_tags=publisher,encoded_by**, ffmpeg **blackdetect** 30s; `terminal-contract-scenarios.test.ts` + спринт §8 (**315 / 103 / 18**). `npm run check:quiet` ок (**404 tests**).
- [J-407] 2026-05-12 03:53:08 [SDK]: §8 — сценарные подсказки «Загрузки»: +`--print-to-file title …txt --skip-download`, +`--enable-file-urls -F`, +`--source-address 198.51.100.2 -F`, +`--print annotations|storyboards`, +`--sponsorblock-mark all --sponsorblock-chapter-title %(category)s`, +`--concat-playlist never -F`, +`--fixup warn -F`, +`--use-extractors youtube -F`, +`--default-search auto: -F`; превью — ffmpeg **cropdetect**/**freezedetect**/**signalstats**; `terminal-contract-scenarios.test.ts` + спринт §8 (**325 / 103 / 21**). `npm run check:quiet` ок (**406 tests**).
- [J-408] 2026-05-12 03:56:32 [SDK]: §8 — сценарные подсказки «Загрузки»: +`--ignore-dynamic-mpd -F`, +`--sponsorblock-api https://sponsor.ajay.app -F`, +`--config-locations yt-dlp.conf -F`, гео **MX/KR/IN/TR/NO/CH** `-F`, +`--xfwd -F`; превью — ffprobe **show_chapters json**, **s:0/a:1 start_time+duration**, ffmpeg **highpass** 5s; `terminal-contract-scenarios.test.ts` + спринт §8 (**335 / 106 / 22**). `npm run check:quiet` ок (**408 tests**).
- [J-409] 2026-05-12 03:59:57 [SDK]: §8 — сценарные подсказки «Загрузки»: TV Everywhere/фрагменты — `--no-cookies-from-browser -F`, `--downloader-args ffmpeg:-nostdin -F`, `--include-ads -F`, `--twofactor 123456`, `--video-password PASSWORD`, `--ap-mso Rogers -F`, `--ap-username user@example.com -F`, `--concurrent-downloads 2 -F`, гео **NZ**/**ZA** `-F`; превью — ffprobe **s:1/a:1 time_base+start_pts**, ffmpeg **dynaudnorm** 5s; `terminal-contract` + Vitest + спринт §8 (**345 / 108 / 23**). `npm run check:quiet` ок (**409 tests**).
- [J-410] 2026-05-12 04:02:15 [SDK]: §8 — сценарные подсказки «Загрузки»: +`--ap-password PASSWORD`, +`--client-certificate client.pem`, +`--geo-verification-proxy http://127.0.0.1:8888 -F`, гео **AT/DK/FI/GR/PT/BE/IE** `-F`; превью — ffprobe **v:0 stream_tags=location**, **a:0 sample_fmt**, ffmpeg **-fflags +genpts -c copy -t 2**; `terminal-contract` + Vitest + спринт §8 (**355 / 110 / 24**). `npm run check:quiet` ок (**411 tests**).
- [J-411] 2026-05-12 04:04:43 [SDK]: §8 — сценарные подсказки «Загрузки»: +`--client-certificate-key key.pem`, +`--impersonate firefox|edge -F`, гео **CZ/HU/RO/BG/HR/LV/LT/EE/IS** `-F`, +`--convert-thumbnails png`; превью — ffprobe **format_tags=lyrics**, **a:1 channel_layout+sample_fmt**, ffmpeg **scenedetect** 20s; `terminal-contract` + `terminal-contract-scenarios.test.ts` + спринт §8 (**369 / 112 / 25**). `npm run check:quiet` ок (**413 tests**).
- [J-412] 2026-05-12 04:08:09 [SDK]: §8 — сценарные подсказки «Загрузки»: +`--extract-audio --audio-format` opus|flac|wav|m4a, +`--no-mark-watched -F`, +`--no-write-comments -F`, +`--no-write-description -F`, гео **MY/SG/TH/VN/AR/UA** `-F`; превью — ffprobe **v:0 stream_tags=stereo_mode**, **a:0 duration_ts**, **format=size,bit_rate,nb_streams**, ffmpeg **aresample=44100** 3s; `terminal-contract` + `terminal-contract-scenarios.test.ts` + спринт §8 (**382 / 115 / 26**). `npm run check:quiet` ок (**415 tests**).
- [J-413] 2026-05-12 04:11:21 [SDK]: §8 — сценарные подсказки «Загрузки»: гео **PH/ID/PK/BD/EG/CL/PE/KE/CO/NG** `-F`, +`--extractor-args generic:noplaylist -F`, +`--skip-playlist-after-errors 10 -F`, +`--retries 15 -F`, +`--fragment-retries 15 -F`; превью — ffprobe **format_tags=minor_version**, ffmpeg **afftdn** 3s; `terminal-contract-scenarios.test.ts` + спринт §8 (**396 / 116 / 27**). `npm run check:quiet` ок (**417 tests**).
- [J-414] 2026-05-12 04:14:49 [SDK]: §8 — сценарные подсказки «Загрузки»: +`--bidi-workaround -F`, +`--daterange 20000101-20991231 -F`, +`--playlist-start 2 -F`, гео **SK/SI/LU/MT/CY/BA/RS** `-F`; превью — ffprobe **format_tags=description,keywords**, **format_tags=location**, ffmpeg **acompressor** 5s; `terminal-contract` + `terminal-contract-scenarios.test.ts` + спринт §8 (**406 / 118 / 28**). `npm run check:quiet` ок (**419 tests**).
- [J-415] 2026-05-12 04:17:51 [SDK]: §8 — сценарные подсказки «Загрузки»: гео **MN/KZ/GE/AM/AZ/IQ/LK/TN/MA/DZ/GH/ET/UY/BO/CR/PA** `-F`; +`--downloader ffmpeg|aria2c -F`, +`--no-wait-for-video -F`, +`--verbose -F`; превью — ffprobe **v:2** codec+size+profile/level, ffmpeg **silenceremove** 60s; `terminal-contract` + `terminal-contract-scenarios.test.ts` + спринт §8 (**426 / 119 / 29**). `npm run check:quiet` ок (**421 tests**).
- [J-416] 2026-05-12 04:21:25 [SDK]: §8 — сценарные подсказки «Загрузки»: +`--trim-filenames 180 -F`, +`--hls-split-discontinuity -F`, +`--dynamic-mpd-buffer-size 100 -F`, +`--no-write-pages -F`, +`--socket-timeout 120 -F`; превью — ffprobe **v:0 ticks_per_frame**, ffmpeg **treble=g=1** 3s; `terminal-contract` + `terminal-contract-scenarios.test.ts` + спринт §8 (**431 / 120 / 30**). `npm run check:quiet` ок (**423 tests**).
- [J-417] 2026-05-12 04:27:13 [SDK]: §8 — сценарные подсказки «Загрузки»: `-S +res:1080,+codec:av01` / `+br:5000000,+res:720` `-F`, `--hide-progress -F`, `--playlist-items -1` / `2:4` `-F`, `--print genres|cast`, `--ppa FFmpeg:-threads:1 -F`; превью — ffprobe **format_tags=software**, **episode_sort+season_number+episode_id**, ffmpeg **volume=3dB** / **lowpass=f=3500**; `terminal-contract` + `terminal-contract-scenarios.test.ts` + спринт §8 (**438 / 122 / 32**). `npm run check:quiet` ок (**425 tests**).
- [J-418] 2026-05-12 04:30:24 [SDK]: §9 — сводка дорожек: в `buildTrackDetail` для видео при ненулевом `closed_captions` — **CEA-608/708**; для H.264 при `is_avc=0` — **Annex-B**; `parseFfprobeOptionalInt`; Vitest `ffprobe-service.test.ts` (+3). Спринт §9. `npm run check:quiet` ок (**428 tests**).
- [J-419] 2026-05-12 04:33:29 [SDK]: §9 — сводка дорожек: в `buildTrackDetail` для аудио — теги ReplayGain (`replaygain_*` / `REPLAYGAIN_*`) в `detail` как **RG tr** / **RG al** (`tagStringFirstMatch` + `appendFfprobeReplayGainAudioDetail`); Vitest `ffprobe-service.test.ts` (+1). Спринт §9. `npm run check:quiet` ок (**429 tests**).
- [J-420] 2026-05-12 04:35:55 [SDK]: §9 — сводка аудио: ReplayGain **peak** из тегов (`replaygain_*_peak` / `REPLAYGAIN_*_PEAK`) в `detail` как `RG tr pk` / `RG al pk` (после gain при наличии); `appendFfprobeReplayGainAudioDetail`; Vitest `ffprobe-service.test.ts`. Спринт §9/чеклист. `npm run check:quiet` ок (**429 tests**).
- [J-421] 2026-05-12 04:40:10 [SDK]: §8 — сценарные подсказки «Загрузки»: +`--yes-playlist -F`, гео **AD/MC/LI/SM/VA/GI/JE/GG/IM/FO/GL** `-F`; превью — ffprobe **a:0 time_base+avg_frame_rate+r_frame_rate**, ffmpeg **highpass+lowpass** 4s; `terminal-contract` + `terminal-contract-scenarios.test.ts` + спринт §8 (**450 yt-dlp / 123 ffprobe / 33 ffmpeg**). `npm run check:quiet` ок (**431 tests**).
- [J-422] 2026-05-12 04:44:36 [SDK]: §8 — сценарные подсказки «Загрузки»: +`--check-all-urls -F`, +`--no-windows-filenames -F`, +`--replace-in-metadata title,_,- -F`, +`--no-playlist --simulate`, +`--skip-download --print dislike_count`, +`--flat-playlist --skip-download --print duration`; превью — ffprobe **v:0 is_intra_only**, ffmpeg **adeclick** 5s; `terminal-contract` + Vitest + спринт §8 (**456 / 124 / 34**). `npm run check:quiet` ок (**433 tests**).
- [J-423] 2026-05-12 04:48:22 [SDK]: §8 — сценарные подсказки «Загрузки»: +`--list-extractor-descriptions`, +`--print-traffic -F`, +`--extract-audio --audio-format vorbis`, +`--print-to-file id flux-ytdlp-id.txt --skip-download`; превью — ffprobe **format_tags composer+conductor**, ffmpeg **agate** 5s; `terminal-contract` + Vitest + спринт §8 (**460 / 125 / 35**). `npm run check:quiet` ок (**435 tests**).
- [J-424] 2026-05-12 04:52:50 [SDK]: §8 — расширены сценарии терминала: +`--no-update -F`, +`--no-color -F`, +`--color always -F`, +`--allow-unplayable-formats -F`, +`--extract-audio --audio-format alac`, +`--extract-audio --audio-format ac3`, +`--audio-quality 0 --extract-audio`, +`--postprocessor-args ffmpeg:-threads 1 -F`, гео **TW**/**MD** `-F`; превью — ffprobe **format_tags=performer**, **v:0 stream_tags=alpha_mode**, ffmpeg **extrastereo** 3s; `terminal-contract` + Vitest + чек-лист §8 (**470 / 127 / 36**). `npm run check:quiet` ок (**437 tests**).
- [J-425] 2026-05-12 04:56:05 [SDK]: §8 — сценарные подсказки «Загрузки»: +`--print-to-file` webpage_url / duration_string / uploader / channel_url (flux-ytdlp-*.txt) + гео **BY/AL/MK** `-F`; превью — ffprobe **format_tags=purchase_date**, **sort_artist,sort_album,sort_title**, ffmpeg **aphaser** 4s; `terminal-contract` + `terminal-contract-scenarios.test.ts` + спринт §8 (**477 / 129 / 37**). `npm run check:quiet` ок (**439 tests**).
- [J-426] 2026-05-12 04:59:16 [SDK]: §8 — сценарные подсказки «Загрузки»: `--print-to-file` view_count / channel / extractor / playlist_title / upload_date (flux-ytdlp-*.txt), гео **ME/PS/TL** `-F`, `--extract-audio --audio-format wma`; превью — ffprobe **format_tags=service_provider,service_name**, **a:0 bits_per_coded_sample**, ffmpeg **flanger** 4s; `terminal-contract` + `terminal-contract-scenarios.test.ts` + спринт §8 (**486 / 131 / 38**). `npm run check:quiet` ок (**441 tests**).
- [J-427] 2026-05-12 05:01:41 [SDK]: §8 — inline IntelliSense argv: навигация **PgUp/PgDn** (шаг `DEFAULT_TERMINAL_INLINE_SUGGEST_PAGE_STEP`), `stepTerminalSuggestIndex`, подсказка в `App.tsx`, Vitest `terminal-inline-suggest.test.ts`; спринт/§8 чек-лист. `npm run check:quiet` ок (**444 tests**).
- [J-428] 2026-05-12 07:40:36 [SDK]: §9 — сводка дорожек: **ticks_per_frame**>1 → `tpf N`; **bits_per_coded_sample** → `bpc …-bit` (видео/аудио, без коллизии с `coded WxH`); Vitest `ffprobe-service.test.ts`; спринт §9/чек-лист. `npm run check` ок (**449 tests**).
- [J-429] 2026-05-12 07:42:36 [SDK]: §9 — `side_data_list`: типы с **spherical** (напр. Spherical Mapping) в «Сведения» как **360°**; `summarizeFfprobeSideDataList` + Vitest `ffprobe-side-data` / `ffprobe-service.test.ts`. `npm run check:quiet` ок (**450 tests**).
- [J-430] 2026-05-12 07:47:51 [SDK]: §6.2 — профиль/контейнер для `--cookies-from-browser`: `ytdlpCookiesBrowserProfile` в settings, снимок `cookiesArgvBrowserProfile`, сборка argv `browser:suffix` в `buildYtdlpSpawnArgvTokens`, UI во вкладке «Загрузки» + pop-out (`downloads-window`), merge IPC/preview, `validateYtdlpCookiesBrowserProfile` в `ytdlp-extra-args`; Vitest `ytdlp-extra-args.test.ts`. `npm run check:quiet` ок (**451 tests**).
- [J-431] 2026-05-12 07:51:20 [SDK]: §9 — сводка дорожек: `ffprobeContainerFourccDisplay` скрывает `unknown` и `[0][0][0][0]`; иначе hex `codec_tag` (`tag 0x…`); `extradata_size`>0 → `exdata N B` (видео/аудио/субтитры); Vitest `ffprobe-service.test.ts`; спринт §9. `npm run check:quiet` ок (**453 tests**).
- [J-432] 2026-05-12 07:54:39 [SDK]: §8 — сценарные подсказки «Загрузки»: +`--no-abort-on-error -F`, +`--no-restrict-filenames -F`, гео **PR/GU/VI/AS/MP/UM** `-F`, `--print-to-file` description→flux-ytdlp-desc.txt / filename→flux-ytdlp-fn.txt; превью — ffprobe **format_tags=isrc**, ffmpeg **deesser** 4s; `terminal-contract` + `terminal-contract-scenarios.test.ts` + спринт §8 (**496 / 132 / 39**). `npm run check:quiet` ок (**455 tests**).
- [J-433] 2026-05-12 07:57:27 [SDK]: §8 — сценарные подсказки «Загрузки»: +`--no-prefer-free-formats -F`, `--print-to-file` categories/tags/language/automatic_captions/chapters/acodec/vcodec → flux-ytdlp-*.txt; превью — ffprobe **s:0 stream_tags=encoder**, ffmpeg **vibrato** 4s; `terminal-contract` + `terminal-contract-scenarios.test.ts` + спринт §8 (**504 / 133 / 40**). `npm run check:quiet` ок (**457 tests**).
- [J-434] 2026-05-12 08:00:05 [SDK]: §8 — сценарные подсказки «Загрузки»: `--print-to-file` like_count/duration/subtitles/channel_id/playlist_id/heatmap → flux-ytdlp-*.txt; +`--lazy-playlist -F` +`--no-continue -F`; превью — ffprobe **format_tags=part,compilation**, ffmpeg **crystalizer** 4s; `terminal-contract` + `terminal-contract-scenarios.test.ts` + спринт §8 (**512 / 134 / 41**). `npm run check:quiet` ок (**459 tests**).
- [J-435] 2026-05-12 08:02:04 [SDK]: §9 — сводка дорожек ffprobe: `initial_padding`>0 → `pad N smp` для видео/аудио/субтитров, нулевые значения скрываются; Vitest `ffprobe-service.test.ts`; спринт §9. `npm run check:quiet` ок (**461 tests**).
- [J-436] 2026-05-12 08:05:17 [SDK]: §9 — сводка видео: SDR нетипичный `color_transfer`/`color_trc` (не bt709) в `detail` через `formatFfprobeVideoSdrTransferBrief`, без дубля с gamut (transfer = prim/space → скрыть); `ffprobe-video-color-brief` + Vitest; спринт §9/чеклист. `npm run check:quiet` ок (**466 tests**).
- [J-437] 2026-05-12 08:08:22 [SDK]: §8 — сценарные подсказки «Загрузки»: `--no-playlist-reverse -F`; `--print-to-file` comment_count / webpage_url_basename / display_id / thumbnail / release_timestamp / filepath / resolution / format_id / ext → flux-ytdlp-*.txt; гео **BM/KY/JM/BB/BS** `-F`; превью — ffprobe **v:0 codec_time_base+time_base**, ffmpeg **asetrate*1.01→aresample** 3s; `terminal-contract` + `terminal-contract-scenarios.test.ts` + чеклист §8 (**528 / 135 / 42**). `npm run check:quiet` ок (**468 tests**).
- [J-438] 2026-05-12 08:10:27 [SDK]: §9 — сводка дорожек: `appendFfprobeNbFramesDetail` — `nb_frames`→`N frm` для **видео/аудио/субтитров** (аудио/субтитры ранее не выводились); Vitest `ffprobe-service.test.ts`; чеклист §9. `npm run check:quiet` ок (**469 tests**).
- [J-439] 2026-05-12 08:12:46 [SDK]: §9 — вложения/прочие потоки (`attachment`/`data`/…): в `buildTrackDetail` добавлены FourCC/`codec_tag`, `exdata`, `start_time`/`start_pts`, `stream.duration`, заметный `max_bit_rate`, `nb_frames`, `tags.encoder`/`creation_time` (как у прочих дорожек); Vitest `ffprobe-service.test.ts`; чеклист §9 (строка про вложения). `npm run check:quiet` ок (**470 tests**).
- [J-440] 2026-05-12 08:14:52 [SDK]: §9 — сводка дорожек: субтитры — `appendMaxBitrateDetailIfNotable` в `buildTrackDetail`; вложения/прочие — `initial_padding`→`pad N smp`; Vitest `ffprobe-service.test.ts`; чеклист §9. `npm run check:quiet` ок (**472 tests**).
- [J-441] 2026-05-12 08:17:30 [SDK]: §8 — сценарные подсказки «Загрузки»: `--print-to-file` width/height/fps/tbr/filesize_approx/protocol → flux-ytdlp-*.txt; `--reject-title trailer -F`; гео **LC/GD/VC/KN/DM** `-F`; превью — ffprobe **format_tags=copyright,encoded_by**, ffmpeg **compand** 4s; `terminal-contract` + `terminal-contract-scenarios.test.ts` + спринт §8 (**540 / 136 / 43**). `npm run check:quiet` ок (**474 tests**).
- [J-442] 2026-05-12 08:20:44 [SDK]: §8 — сценарные подсказки «Загрузки»: `--print-to-file` playlist_index / playlist_autonumber / playlist_count / playlist_uploader_id / uploader_id / average_rating / availability / age_limit → flux-ytdlp-*.txt; гео **AW/CW/SX/TC/VG** `-F`; превью — ffprobe **format_tags=album_artist**, **track,disc**, ffmpeg **dynaudnorm** 4s; `terminal-contract` + `terminal-contract-scenarios.test.ts` + чеклист §8 (**552 / 138 / 44**). `npm run check:quiet` ок (**476 tests**).
- [J-443] 2026-05-12 08:23:48 [SDK]: §8 — сценарные подсказки «Загрузки»: `--print-to-file` webpage_url_domain / original_url / abr / vbr / filesize / format_note / playlist_uploader → flux-ytdlp-*.txt; гео **AG/MS/AI/GP/BQ** `-F`; +`--max-downloads 5 -F` +`--playlist-random -F` +`--force-overwrites -F`; превью — ffprobe **format_tags=lyrics,synopsis**, ffmpeg **asoftclip** 4s; `terminal-contract` + `terminal-contract-scenarios.test.ts` + чеклист §8 (**567 / 139 / 45**). `npm run check:quiet` ок (**478 tests**).
- [J-444] 2026-05-12 08:26:11 [SDK]: §8 — сценарные подсказки «Загрузки»: `--print-to-file` fulltitle / alt_title / artist / album / release_year / is_live / live_status / channel_follower_count → flux-ytdlp-*.txt; гео **CK/NU/TK/TO/WS** `-F`; +`--skip-unavailable-fragments -F` +`--abort-on-error -F`; превью — ffprobe **a:0 channels+channel_layout**, ffmpeg **aecho** 4s; `terminal-contract` + `terminal-contract-scenarios.test.ts` + чеклист §8 (**582 / 140 / 46**). `npm run check:quiet` ок (**480 tests**).
- [J-445] 2026-05-12 08:28:50 [SDK]: §8 — сценарные подсказки «Загрузки»: `--print-to-file` series/season_number/episode_number/episode/episode_id/season_id/playlist_channel_id/asr/has_drm/playable_in_embed/was_live/media_type → flux-ytdlp-*.txt; гео **PF/NC/FJ/VU/SB/FM/MH/PW** `-F`; +`--no-break-on-reject -F`; превью — ffprobe **s:1 disposition**, ffmpeg **tremolo**/**bandpass** 4s; `terminal-contract` + `terminal-contract-scenarios.test.ts` + чеклист §8 (**603 / 141 / 48**). `npm run check:quiet` ок (**482 tests**).
- [J-446] 2026-05-12 08:32:27 [SDK]: §8 — сценарные подсказки «Загрузки»: `--print-to-file` **_type** / **playlist_url** / **manifest_url** / **stretched_ratio** / **requested_formats** → flux-ytdlp-*.txt; гео **NR/TV/KI/WF** `-F`; +`--progress-delta 5 -F`; превью — ffprobe **a:1** codec+channels+layout, ffmpeg **highshelf** 3s; `terminal-contract` + `terminal-contract-scenarios.test.ts` + чеклист §8 (**613 / 142 / 49**). `npm run check:quiet` ок (**484 tests**).
- [J-447] 2026-05-12 08:34:42 [SDK]: §9 — сводка аудио ffprobe: `tags.language`/`title`/`handler_name` добавлены в `detail`, `handler_name` не дублируется при совпадении с `title`; Vitest `ffprobe-service.test.ts`; чеклист §9. `npm run check:quiet` ок (**485 tests**).
- [J-448] 2026-05-12 08:36:53 [SDK]: §9 — сводка видео: в `buildTrackDetail` для **video** — `tags.language`/`title`/`handler_name` в `detail` (collapse + без дубля `handler=title`, как у аудио); Vitest `ffprobe-service.test.ts`; чеклист §9. `npm run check:quiet` ок (**486 tests**).
- [J-449] 2026-05-12 08:40:31 [SDK]: §9 — DRY ffprobe detail: общий хелпер `appendTrackTagsLangTitleHandler` (язык + title с collapse + handler с collapse и без дубля `handler=title`) применён в video/audio/subtitle блоках `buildTrackDetail`; побочно — subtitle `tags.title` теперь тоже обрезается по `FFPROBE_DETAIL_ENCODER_MAX`; +Vitest на длинный subtitle title. `npm run check:quiet` ок (**487 tests**).
- [J-450] 2026-05-12 08:44:27 [SDK]: §8 — сценарные подсказки: `--print-to-file` **formats** / **url** / **thumbnails** / **location** → flux-ytdlp-*.txt; гео **AX/SJ/SH** `-F`; +`--xattr-set-filesize -F`; превью — ffprobe **v:1** codec+WxH, ffmpeg **apulsator** 3s; `terminal-contract` + `terminal-contract-scenarios.test.ts` + чеклист §8 (**621 / 143 / 50**). `npm run check:quiet` ок (**489 tests**).
- [J-451] 2026-05-12 08:47:26 [SDK]: §8 — сценарные подсказки «Загрузки»: `--print-to-file` epoch / requested_subtitles / playlist_channel / n_entries / dislike_count; `--no-playlist-metafiles -F`; гео **BV/TF/HM** `-F`; превью — ffprobe **d:1** codec+tag, ffmpeg **chorus** 4s; `terminal-contract` + `terminal-contract-scenarios.test.ts` + чеклист §8 (**630 / 144 / 51**). `npm run check:quiet` ок (**491 tests**).
- [J-452] 2026-05-12 08:51:54 [SDK]: §8 — сценарные подсказки «Загрузки»: гео **IO/PN/AQ/GS/PM** `-F`; `--print-to-file` release_date→flux-ytdlp-reldate.txt / modified_timestamp→flux-ytdlp-mts.txt / upload_timestamp→flux-ytdlp-upts.txt / aspect_ratio→flux-ytdlp-aspect.txt / episode_sort→flux-ytdlp-epsort.txt; превью — ffprobe **format_tags=encoder+WMFSDKVersion**, ffmpeg **afade in** 3s; `terminal-contract` + `terminal-contract-scenarios.test.ts` + чеклист §8 (**640 / 145 / 52**). `npm run check:quiet` ок (**493 tests**).
- [J-453] 2026-05-12 08:55:59 [SDK]: §8 — сценарные подсказки «Загрузки»: гео **FK/EH/DJ/KG/TJ/NP/LA/KH/BN** `-F`; `--print-to-file` channel_is_verified / is_private / composers / creators / track_number → flux-ytdlp-*.txt; превью — ffprobe **format_tags=encoding_tool**, ffmpeg **afade t=out** 3s; `terminal-contract` + `terminal-contract-scenarios.test.ts` + чеклист §8 (**654 / 146 / 53**). `npm run check:quiet` ок (**495 tests**).
- [J-454] 2026-05-12 09:00:15 [SDK]: §8 — сценарные подсказки «Загрузки»: гео **MM/BT/MV/MZ/ZW/BW/NA/LS/MW/SZ** `-F`; `--print-to-file` genre / album_type / license / track / album_artist / comment → flux-ytdlp-*.txt; превью — ffprobe **format=probe_score**, ffmpeg **atempo=0.95** 3s; `terminal-contract` + `terminal-contract-scenarios.test.ts` + спринт §8 (**670 / 147 / 54**). `npm run check:quiet` ок (**497 tests**).
- [J-455] 2026-05-12 09:04:30 [SDK]: §8 — сценарные подсказки «Загрузки»: гео **TD/NE/ML/SN/LY/SO/ER/SS/YE/MR** `-F`; `--print-to-file` lyrics / disc_number / publisher / mood → flux-ytdlp-*.txt; превью — ffprobe **v:1 codec_name+codec_long_name**, ffmpeg **alimiter** 3s; `terminal-contract` + `terminal-contract-scenarios.test.ts` + спринт §8 (**684 / 148 / 55**). `npm run check:quiet` ок (**499 tests**).
- [J-456] 2026-05-12 09:08:34 [SDK]: §8 — сценарные подсказки «Загрузки»: гео **CM/GA/CG/CD/CF/GQ/ST/BI/RW/UG/TZ/ZM** `-F`; `--print-to-file` artist_sort / album_sort / conductor / performers / copyright / uploader_url / producer / director → flux-ytdlp-*.txt; превью — ffprobe **format_tags=major_brand,minor_version,compatible_brands**, ffmpeg **stereotools** 3s; `terminal-contract` + `terminal-contract-scenarios.test.ts` + спринт §8 (**704 / 149 / 56**). `npm run check:quiet` ок (**501 tests**).
- [J-457] 2026-05-12 09:13:40 [SDK]: §8 — сценарные подсказки «Загрузки»: `--no-build-paths -F`; гео **BJ/TG/BF/CI/LR/SL/GN/GW** `-F`; `--print-to-file` arranger/remixer/engineer/lyricist/grouping/compilation/show → flux-ytdlp-*.txt; превью — ffprobe **format_tags=gapless_playback,compilation**, ffmpeg **speechnorm** 4s; `terminal-contract` + `terminal-contract-scenarios.test.ts` + чеклист §8 (**720 / 150 / 57**). `npm run check:quiet` ок (**503 tests**).
- [J-458] 2026-05-12 09:18:08 [SDK]: §8 — сценарные подсказки «Загрузки»: `--print-to-file` album_artists/cast/network → flux-ytdlp-*.txt; гео **SV/HN/NI/GT/BZ/DO/HT** `-F`; **`--limit-rate 500K -F`**; превью — ffprobe **format_tags=BPM,initial_key**, ffmpeg **bs2b=profile=j2** 4s; `terminal-contract` + `terminal-contract-scenarios.test.ts` + чеклист §8 (**731 / 152 / 58**). `npm run check:quiet` ок (**505 tests**).
- [J-459] 2026-05-12 09:26:50 [SDK]: §7.2 — выбор видеокодека экспорта **libx264** / **libx265** (`FfmpegExportVideoCodecId`, `ffmpegExportVideoCodec` в settings, IPC/preload, `resolveFfmpegExportJobOptionsFromAppSettings`, `buildFfmpegExportArgv`: для HEVC в MP4/MOV — `-tag:v hvc1`; 2-pass и ошибка spawn — только H.264; user preset snapshot/merge; UI rail «Видео»). Vitest `ffmpeg-export-argv` + `ffmpeg-export-service`. `npm run check:quiet` ок (**506 tests**).
- [J-460] 2026-05-12 09:30:39 [SDK]: §8 — сценарные подсказки «Загрузки»: гео **BR/VE/EC/PY/CU/GY/SR** `-F`; `--print-to-file webpage_url_scheme`→flux-ytdlp-wuscheme.txt; превью — ffprobe **format_tags=artist,album**, ffmpeg **bass=g=2:f=120** 4s; `terminal-contract` + `terminal-contract-scenarios.test.ts` + чеклист §8 (**739 / 153 / 59**). `npm run check:quiet` ок (**508 tests**).
- [J-461] 2026-05-12 09:37:11 [SDK]: §8 — сценарные подсказки «Загрузки»: `--print-to-file` **playlist** / **annotations** / **storyboards** / **playlist_webpage_url** → flux-ytdlp-*.txt; +`--retries 20 -F` +`--fragment-retries 20 -F`; превью — ffprobe **s:0 index+codec_name**, ffmpeg **superequalizer=3b=4** 4s; `terminal-contract` + `terminal-contract-scenarios.test.ts` + чеклист §8 (**745 / 153 / 60**). `npm run check:quiet` ок (**510 tests**).
- [J-462] 2026-05-12 09:41:54 [SDK]: §8 — сценарные подсказки «Загрузки»: `--print-to-file` **timestamp** / **extractor_key** / **track_id** / **album_id** / **dynamic_range** → flux-ytdlp-*.txt; гео **TT** `-F`; превью — ffprobe **format_tags=show,episode_sort**, ffmpeg **lowshelf=g=2:f=200** 4s; `terminal-contract` + `terminal-contract-scenarios.test.ts` + чеклист §8 (**751 / 154 / 62**). `npm run check:quiet` ок (**512 tests**).
- [J-463] 2026-05-12 09:45:20 [SDK]: §9 — `summarizeFfprobeSideDataList`: Stereo 3D→«3D», ATSC Audio Service Type→«ATSC svc N» (без `service_type` — «ATSC audio svc»); аудио `buildTrackDetail` добавляет ту же сводку `side_data_list`, что и видео; Vitest `ffprobe-side-data` + `ffprobe-service`; чеклист §9 (спринт). `npm run check:quiet` ок (**514 tests**).
- [J-464] 2026-05-12 09:49:48 [SDK]: §8 — сценарные подсказки «Загрузки»: `--print-to-file` **audio_ext** / **video_ext** / **player_url** → flux-ytdlp-*.txt; **`--concurrent-fragments 4 -F`**; гео **RE/MU/SC** `-F`; превью — ffprobe **format_tags=genre,date**, ffmpeg **extrastereo=m=1.2** 4s; `terminal-contract` + `terminal-contract-scenarios.test.ts` + чеклист §8 (**758 / 155 / 63**). `npm run check:quiet` ок (**516 tests**).
- [J-465] 2026-05-12 09:55:51 [SDK]: §8 — сценарные подсказки «Загрузки»: `--print-to-file` **audio_channels** / **chapter** / **chapter_id** / **chapter_number** / **start_time** / **end_time** / **quality** / **formats_table** → flux-ytdlp-*.txt; гео **AF/AO/HK/IL/KW** `-F`; +`--clean-info-json -F`, +`--update-to stable`; превью — ffprobe **format_tags=podcast,podcasturl**, ffmpeg **acrusher=…:bits=8:mode=log** 4s; `terminal-contract` + `terminal-contract-scenarios.test.ts` + чеклист §8 (**773 / 156 / 64**). `npm run check:quiet` ок (**519 tests**).
- [J-466] 2026-05-12 10:02:27 [SDK]: §8 — сценарные подсказки «Загрузки»: `--print-to-file` **format** / **language_preference** / **autonumber** → flux-ytdlp-*.txt; гео **OM/QA/BH/AE/SA** `-F`; превью — ffprobe **d:2** codec+tag, ffmpeg **pan=stereo|c0=0.6*c0+0.4*c1|…** 4s; `terminal-contract` + `terminal-contract-scenarios.test.ts` + чеклист §8 (**781 / 157 / 64**). `npm run check:quiet` ок (**521 tests**).
- [J-467] 2026-05-12 10:07:01 [SDK]: §8 — сценарные подсказки «Загрузки»: `--print-to-file` **filename_sanitized** / **requested_downloads** → flux-ytdlp-*.txt; **`--extractor-retries 5 -F`**; гео **JO/LB/UZ/TM** `-F`; превью — ffprobe **a:4** codec+rate+ch, **s:4** codec+tag, ffmpeg **acompressor** 4s; `terminal-contract` + `terminal-contract-scenarios.test.ts` + чеклист §8 (**788 / 159 / 65**). `npm run check:quiet` ок (**523 tests**).
- [J-468] 2026-05-12 10:13:18 [SDK]: §8 — сценарные подсказки «Загрузки»: `--print-to-file` **modified_date** / **live_title** → flux-ytdlp-*.txt; **`--http-chunk-size 1M -F`**; гео **CV/GM/KM** `-F`; превью — ffprobe **format_tags=catalog_number,barcode**, ffmpeg **aresample=async=1:first_pts=0** 4s (перед финальным **acompressor**); `terminal-contract` + `terminal-contract-scenarios.test.ts` + чеклист §8 (**794 / 160 / 66**). `npm run check:quiet` ок (**524 tests**).
- [J-469] 2026-05-12 10:20:01 [SDK]: §8 — сценарные подсказки «Загрузки»: `--print-to-file` **section_start** / **section_end** / **played_count** / **referrer** → flux-ytdlp-*.txt; **`--playlist-reverse -F`**; гео **YT/MG/PG** `-F`; превью — ffprobe **v:2** width+height+r_frame_rate, ffmpeg **acontrast=25** 3s; `terminal-contract` + `terminal-contract-scenarios.test.ts` + чеклист §8 (**802 / 161 / 67**). `npm run check:quiet` ок (**526 tests**).
- [J-470] 2026-05-12 10:24:17 [SDK]: §8 — terminal runner: `parseTerminalCommandLine` теперь принимает префикс инструмента без учёта регистра (`FFMPEG`/`Ffprobe`/`YT-DLP`), нормализуя в allowlist id; добавлен Vitest `runTerminalCommand` на case-insensitive префикс (mock `execFile` + `engine-service`), обновлён чеклист §8. `npm run check:quiet` ок (**527 tests**).
- [J-471] 2026-05-12 10:26:46 [SDK]: §18 — Support ZIP `diagnostics.txt` теперь дополняется сводкой использования каталогов (`logs`, `crashDumps`, `preview-cache`, `downloads/ytdlp`: files/dirs/bytes), чтобы ускорить диагностику cache/temp и роста логов без отдельного UI; обновлён чеклист §18. `npm run check:quiet` ок (**527 tests**).
- [J-472] 2026-05-12 10:29:57 [SDK]: §2.2 — зафиксирован подход состояния renderer для сворачиваемых панелей: выделен hook `useMainWindowUiPanels` (общие defaults, гидрация snapshot'ов и persist toggle через settings IPC), `App.tsx` переведён на этот hook без дублирования merge-логики; обновлён спринт-блок чеклиста (`§1.1/§2.2/§5` → `[~]`). `npm run check:quiet` ок (**527 tests**).
- [J-473] 2026-05-12 10:31:39 [SDK]: §9 — `summarizeFfprobeSideDataList` ужесточён для `Audio Service Type`: `ATSC svc N` формируется только из валидного целого кода (поддержаны `service_type` и `audio_service_type`, строковые числа тоже), нечисловые значения безопасно дают fallback `ATSC audio svc`; добавлены edge-case тесты `ffprobe-side-data`. `npm run check:quiet` ок (**527 tests**).
- [J-474] 2026-05-12 10:34:05 [SDK]: §8 — `filterTerminalInlineSuggestions` теперь дедуплицирует merge-подсказки по нормализованному token (`· -F` == `-F`) и trim `fullLine`, чтобы не показывать дубли сценариев/JSON в inline IntelliSense; добавлены Vitest-кейсы на схлопывание decorated/plain дублей и сохранение разных `fullLine`-вариантов при одном token. `npm run check:quiet` ок (**529 tests**).
- [J-475] 2026-05-12 10:36:24 [SDK]: §9 — `Audio Service Type` в `summarizeFfprobeSideDataList` дополнительно ограничен валидным диапазоном ATSC-кодов (`0..7`): вне диапазона (напр. `8`, `12`) теперь стабильно даёт fallback `ATSC audio svc`; расширен edge-case тест `ffprobe-side-data`. `npm run check:quiet` ок (**529 tests**).
- [J-476] 2026-05-12 10:39:25 [SDK]: §8 — терминал CLI: рядом с compact inline IntelliSense добавлен полный dropdown-пикер подсказок (до 240 пунктов, `optgroup` по `ffmpeg`/`ffprobe`/`yt-dlp`, вставка выбранного `token/fullLine` в argv с reset выбора), обновлён help-текст вкладки «Терминал». `npm run check:quiet` ок (**529 tests**).
- [J-477] 2026-05-12 10:42:51 [SDK]: §1.1/§2.2 — добавлен стартовый renderer i18n-слой `src/renderer/src/locales/ui-text.ts` (`ru/en`, выбор по `navigator.language`), на него переведены UI-литералы в `AboutDialog` (title/labels/buttons/status/docs link) и `Versions` (`aria-label`), обновлён спринт-блок чеклиста по локализации. `npm run check:quiet` ок (**529 tests**).
- [J-478] 2026-05-12 10:45:32 [SDK]: §8 — сценарные подсказки «Загрузки»: добавлены `yt-dlp --print-to-file` шаблоны для **extractor_key** / **uploader_url** / **thumbnail** (новые flux-ytdlp-*.txt цели), расширен smoke-набор `terminal-contract-scenarios.test.ts`; обновлён спринт-блок (`§8`: **805** yt-dlp / 161 ffprobe / 67 ffmpeg). `npm run check:quiet` ок (**530 tests**).
- [J-479] 2026-05-12 10:48:43 [SDK]: §8 — терминал CLI: вместо базового `<select>` для полного списка подсказок добавлен полноценный dropdown-блок с live-фильтром, группировкой по `ffmpeg`/`ffprobe`/`yt-dlp` и вставкой `token/fullLine` по клику; обновлены стили (`app-terminal-dropdown*`), спринт-блок чеклиста синхронизирован. `npm run check:quiet` ок (**530 tests**).
- [J-480] 2026-05-12 10:52:14 [SDK]: §8 — расширен JSON-справочник `Data/ytdlp_commands.json` для CLI IntelliSense (добавлены `-J`, `--dump-single-json`, `--list-extractors`, `--version`, `--print`, `--print-to-file`, `--force-ipv4`, `--force-ipv6`, `--no-cache-dir`); в `ytdlp-commands-hints` обновлена категоризация новых токенов для стабильной группировки в UI. `npm run check:quiet` ок (**530 tests**).
- [J-481] 2026-05-12 10:55:21 [SDK]: §2.2 — вынесено состояние/гидрация/persist панелей «Загрузки» главного окна в новый hook `useDownloadsWindowUiPanels` (history/log + секции format/metadata/saving/network/expert, fallback как в pop-out: отсутствующий ключ = defaultOpen); `App.tsx` переведён на hook без локального дублирования mergeUiPanels/hydrate-логики, спринт-блок чеклиста синхронизирован. `npm run check:quiet` ок (**530 tests**).
- [J-482] 2026-05-12 11:00:02 [SDK]: §8 — усилены инварианты `terminal-contract-scenarios`: добавлен guard на минимальный объём `TERMINAL_SCENARIO_HINTS_DOWNLOADS` (>=805) и уникальность `fullLine`; по падению теста устранён фактический дубль сценария (`yt-dlp --geo-bypass-country BR -F` → `XK`, обновлён смоук-кейс geos в Vitest), спринт-блок чеклиста синхронизирован. `npm run check:quiet` ок (**531 tests**).
- [J-483] 2026-05-12 16:32:03 [Assistant]: §9 — расширена compact-сводка `ffprobe side_data_list`: AFD, Closed Captions, Replay Gain, Skip Samples, SMPTE timecode; `Display Matrix` больше не дублируется в side-data summary после `matrix …°`; добавлены unit + integration тесты `ffprobe-side-data`/`ffprobe-service`. `npm run check:quiet` ок (**535 tests**).
- [J-484] 2026-05-12 16:34:25 [Assistant]: §9 — продолжена compact-сводка `ffprobe side_data_list`: добавлены CPB properties (`CPB max/avg …`) и GOP timecode (`GOP TC …`) с unit + integration тестами; чеклист синхронизирован. `npm run check:quiet` ок (**537 tests**).
- [J-485] 2026-05-12 16:37:22 [Assistant]: §9 — compact side-data дополнена `Producer Reference Time` (`PRFT …`) и `AV1 film grain`; добавлены unit + integration тесты, чеклист обновлён. `npm run check:quiet` ок (**539 tests**).
- [J-486] 2026-05-12 16:47:09 [Assistant]: §15 — добавлен внутренний viewer базы знаний: main `knowledge-service` безопасно листает/читает только `Help/*.md` (dev + packaged `resources/Help`), preload API `knowledge.listArticles/readArticle`, renderer `KnowledgeDialog` с оглавлением/поиском/просмотром markdown-text; кнопка в topbar, CSS и Vitest `knowledge-service`. `npm run check:quiet` ок (**43 files / 542 tests**).
- [J-487] 2026-05-12 16:53:28 [Assistant]: чеклист приведён к правилам — `## Ближайший TODO спринта` сжат до 6 коротких направлений, SDK prompts уточнены, добавлен `check:checklist` и включён в `check`/`check:quiet`, чтобы блок больше не превращался в changelog/архив. `check:quiet` и `agent:typecheck` ок.
- [J-488] 2026-05-12 17:10:00 [Assistant]: §0 — в блоке `### Этапы` этап 1 помечен `[x]` (инициализация и рабочий workspace вместо устаревшего «пустого окна»); остальные этапы без изменения статуса. `npm run check:quiet` ок (**43 files / 542 tests**).
- [J-489] 2026-05-12 17:25:45 [SDK]: §19/§3 — после `pack:dir` добавлена автопроверка дерева `dist/win-unpacked` (`scripts/verify-win-unpacked-layout.mjs`, `npm run verify:win-unpacked`, в `check:release` и CI); `docs/RELEASE.md` и чеклист §19/§22/спринт. `npm run check` ок.
- [J-490] 2026-05-12 17:31:07 [SDK]: §8 — сценарные подсказки «Загрузки»: `--print-to-file` для **keywords**, **playlist_channel_url**, **starring**, **title_sort**, **season**, **section_number**, **isrc**, **track_sort**, **alt_description**, второй вывод **view_count** → `flux-ytdlp-viewcount.txt`; `terminal-contract` + Vitest `terminal-contract-scenarios` (**815** yt-dlp сценариев в наборе). `npm run check:quiet` ок (**543 tests**).
- [J-491] 2026-05-12 17:36:23 [SDK]: §15 — парсер `Help/*.md` в `shared/knowledge-markdown.ts` (заголовки, списки, `**`/`*`/`` ``, ссылки, fenced code; схлопывание дубля H1 с заголовком статьи); `KnowledgeMarkdownBody` + стили; внутренние `*.md` → навигация по slug, `https` → `<a target=_blank>`; chrome диалога через `ui-text` (EN/RU); Vitest `knowledge-markdown.test.ts`; чеклист §15/спринт + счётчики тестов. `npm run check:quiet` ок (**547 tests**).
- [J-492] 2026-05-12 17:40:06 [SDK]: §15 — markdown viewer: блоки цитаты `>` в `parseKnowledgeMarkdown` (`MdBlock` `blockquote`, склейка строк `>…` как у абзацев), рендер `<blockquote class="app-knowledge-bq">`, стили в `main.css`; абзацы не поглощают строки с `>`; Vitest на blockquote+inline. `npm run check:quiet` ок (**548 tests**).
- [J-493] 2026-05-12 17:42:10 [SDK]: §8 — inline IntelliSense argv: при открытом списке подсказок **Enter** (без Shift) подставляет активный пункт, как Tab; без списка Enter по-прежнему запускает команду; help-текст вкладки «Терминал» и чеклист §8. `npm run check:quiet` ок (**548 tests**).
- [J-494] 2026-05-12 17:44:42 [SDK]: §8 — inline IntelliSense: при открытом списке **Shift+Tab** двигает выбор вверх (как ↑), **Tab** — подставить активную подсказку (раньше Shift+Tab шёл в ту же ветку, что и Tab и мог ошибочно подставлять); help на вкладке «Терминал» и строка §8 чеклиста. `npm run check:quiet` ок (**548 tests**).
- [J-495] 2026-05-12 17:49:16 [SDK]: §8 — полный список CLI-подсказок терминала: клавиатура в поле фильтра (↑↓/Home/End/PgUp/PgDn через `stepTerminalSuggestIndex`, Enter — вставка активной строки), `terminalDropdownListIndex` + `terminalDropdownActiveIndex`, подсветка `.app-terminal-dropdown-item-active`, `scrollIntoView` в списке, сброс индекса при смене фильтра; help + §8 чеклист + `main.css`. `npm run check:quiet` ок (**548 tests**).
- [J-496] 2026-05-12 17:51:00 [SDK]: §8 — поле фильтра полного списка подсказок: **Escape** сбрасывает непустой фильтр и индекс выбора (в т.ч. когда список пуст из‑за фильтра); при уже пустом фильтре — `blur` поля; help на вкладке «Терминал» и строка §8 чеклиста. `npm run check:quiet` ок (**548 tests**).
- [J-497] 2026-05-12 17:53:13 [SDK]: §15 — knowledge markdown: thematic break (`---`, `* * *`, `___` и варианты с пробелами) как `MdBlock` `hr`, `isKnowledgeThematicBreak` + разрыв абзаца перед HR; `KnowledgeMarkdownBody` + `.app-knowledge-hr`; Vitest `isKnowledgeThematicBreak` + parse; чеклист §15 (viewer). `npm run check:quiet` ок (**550 tests**).
- [J-498] 2026-05-12 17:56:08 [SDK]: §15 — `parseKnowledgeMarkdown`: маркер `+` для ненумерованных списков; перенос строки пункта ul/ol при отступе ≥4 пробела или таб (`listContinuationBody` + `breaksListContinuation`); абзацный цикл учитывает `[-*+]`. Vitest на `+` и continuations; §15 чеклист (viewer). `npm run check:quiet` ок (**551 tests**).
- [J-499] 2026-05-12 17:59:05 [SDK]: §15 — открытие статей из подсказок: `KnowledgeDialog` + проп `initialSlug` (выбор статьи после `listArticles`), топбар сбрасывает slug; вкладка «Терминал» — inline-кнопка `.app-knowledge-link` → `Help/ffmpeg-terminal-hints.md`; строки `ui-text` RU/EN; §15 чеклист (`[~]` deep-link). `npm run check:quiet` ок (**551 tests**).
- [J-500] 2026-05-12 18:01:14 [SDK]: §15 — локализованные `title` (RU/EN): топбар «?», закрытие диалога, поиск, пункты TOC (`fileName`·`slug`), внутренние/внешние ссылки в `KnowledgeMarkdownBody`, deep-link из «Терминала»; новые ключи `ui-text.ts`. Чеклист §15 tooltips → `[~]`. `npm run check:quiet` ок (**551 tests**).
- [J-501] 2026-05-12 18:06:08 [SDK]: §15 — EN-тела статей: `readKnowledgeArticle` при `preferredUiLocale=en` читает `Help/en/{slug}.md` при наличии (безопасный join), иначе корень; `KnowledgeDialog` передаёт `getUiLocale()`, заголовок/parsing из ответа read; `Help/en/ffmpeg-terminal-hints.md`; контракт `KnowledgeReadArticleRequest`, preload + Vitest `knowledge-service`. Чеклист §15/спринт + счётчик тестов. `npm run check:quiet` ок (**554 tests**).
- [J-502] 2026-05-12 18:08:05 [SDK]: §15 — добавлен `Help/en/getting-started.md` (EN-тело для `getting-started` при EN UI chrome; внутренние ссылки на те же `.md` slug’и). Код без изменений. `npm run check:quiet` ок (**554 tests**).
- [J-503] 2026-05-12 18:10:09 [SDK]: §15 — EN-тела: `Help/en/keyboard-shortcuts.md`, `logging-and-diagnostics.md`, `processing-social-presets.md`, `processing-advanced-fields.md`, `tools-terminal-inspector.md`, `hardware-encoding.md` (согласованы с текущим приложением и цепочкой ссылок из `getting-started`). Код без изменений. `npm run check:quiet` ок (**554 tests**).
- [J-504] 2026-05-12 18:11:49 [SDK]: §15 — EN: добавлены `Help/en/downloads-dragdrop.md`, `Help/en/session-and-queues.md`; в `Help/en/getting-started.md` — ссылки на clipboard/DnD и сессии/очереди. Код без изменений. `npm run check:quiet` + `check:checklist` ок.
- [J-505] 2026-05-12 18:13:24 [SDK]: §15 — EN Help: добавлены `Help/en/start.md`, `extract-frames.md`, `processing-url-combo.md` для `preferredUiLocale=en`; уточнена строка §15 чеклиста про EN-тела. Код без изменений. `npm run check:quiet` ок (**554 tests**).
- [J-506] 2026-05-12 18:18:20 [SDK]: §8/§1.1 — вкладка «Терминал» в `App.tsx`: RU/EN строки и help-intro (lead + `<code>` токен + tail с `{pageStep}`/`{maxInline}`/`{maxDd}`) вынесены в `locales/ui-text.ts` + форматтеры `formatTerminalIntroTail`, `formatTerminalPreviewTooltip`, `formatTerminalExitLine`, `formatTerminalCopyLineAria`; обновлены §8/§1.1/§15/спринт в чеклисте. `npm run check:quiet` ок (**554 tests**).
- [J-507] 2026-05-12 19:30:00 [Assistant]: §19/CI — в `.github/workflows/ci.yml` упрощён блок `on:`: убран избыточный `push.branches: ['**']` (по умолчанию GitHub триггерит push на все ветки).
- [J-508] 2026-05-12 20:45:00 [Assistant]: §19/GitHub — добавлен `.github/dependabot.yml` (npm weekly, github-actions monthly); в `docs/RELEASE.md` §5 дописаны проверки настроек Actions, форки и расшифровка писем *No jobs were run*. Локально `main` синхронен с `origin`, `npm run check:quiet` ок (**554 tests**).
- [J-509] 2026-05-12 20:52:00 [Assistant]: §19 — в `docs/RELEASE.md` §5 добавлена отсылка к включению Dependabot в *Code security and analysis* при отсутствии PR от бота.
- [J-510] 2026-05-12 21:10:00 [Assistant]: §9/§18 — добавлены Vitest smoke `probeMediaFile`: отсутствие ffprobe в пустых `bundledBin`/`userBin` и override на не-ffprobe (`execPath`) дают `ok: false` без crash (`tests/main/ffprobe-probe-media.integration.test.ts`); чеклист §9 + снимок счётчика тестов. `npm run check:quiet` ок (**556 tests**).
- [J-511] 2026-05-12 21:22:00 [Assistant]: §9 — тест «невалидный JSON ffprobe» вынесен в `ffprobe-probe-media-json-mock.test.ts` с `vi.mock('child_process')` (ESM не даёт `spyOn` на namespace); `ffprobe-service` остаётся на `execFile` из `child_process`. Чеклист + счётчик тестов. `npm run check:quiet` ок (**557 tests**).
- [J-512] 2026-05-12 21:28:00 [Assistant]: §9 — в `ffprobe-probe-media-json-mock.test.ts` добавлены кейсы пустого stdout и `execFile` error с приоритетом stderr; чеклист **559 tests**. `npm run check:quiet` ок.
- [J-513] 2026-05-12 21:42:00 [Assistant]: §12/§17 — добавлен сервис обслуживания `diagnostics-maintenance`: оценка/очистка `preview-cache` и частичных yt-dlp файлов без удаления готовых медиа; IPC/preload `diagnostics.maintenanceSnapshot/cleanMaintenance`; кнопки «Размер временных»/«Очистить временное» в «О программе» + RU/EN строки; Vitest `diagnostics-maintenance`. `npm run check:quiet` ок (**47 files / 562 tests**).
- [J-514] 2026-05-12 22:31:00 [Assistant]: §12 — очистка временного в `AboutDialog` стала двухшаговой: первый клик включает `app-btn-danger` и статус-подсказку, второй запускает `diagnostics.cleanMaintenance`; кнопка «Размер временных» сбрасывает подтверждение. `npm run check:quiet` ок (**47 files / 562 tests**).
- [J-515] 2026-05-12 22:36:00 [Assistant]: §12 — выборочная очистка выведена в UI: в «О программе» появились отдельные двухшаговые кнопки общего временного, `preview-cache` и `.part` yt-dlp; тест `diagnostics-maintenance` покрывает target-only cleanup. `npm run check:quiet` ок (**47 files / 563 tests**).
- [J-516] 2026-05-12 22:41:00 [Assistant]: §12 — статус «Размер временных» теперь показывает total и разбивку по `preview-cache`/`yt-dlp .part`; локализован шаблон summary-with-details.
- [J-517] 2026-05-12 22:45:00 [Assistant]: §12 — обслуживание расширено на старые orphan temp-директории ffmpeg two-pass `fa-x264tw-*` в системном temp: target `ffmpegTemp`, порог 6 часов по mtime содержимого, отдельная двухшаговая кнопка в «О программе»; тест защищает свежий temp. `npm run check:quiet` ок (**47 files / 564 tests**).
- [J-518] 2026-05-12 22:53:00 [Assistant]: §18 — Support ZIP `diagnostics.txt` теперь включает `maintenanceTargets` из `diagnostics-maintenance` (total/cleanable + preview/ytdlpPartials/ffmpegTemp); `support-bundle.test` проверяет preview/ytdlp partial строки.
- [J-519] 2026-05-12 22:57:00 [Assistant]: §17 — whitelist диагностических папок расширен `systemTemp` (системная временная папка рядом с ffmpeg temp `fa-x264tw-*`); `diagnostics-paths`/contract/list/open принимают новый id.
- [J-520] 2026-05-12 23:04:00 [Assistant]: §13 — добавлена основа истории обработок: `processing/history.json`, contract/API для kind/outcome/query, запись ffmpeg export/snapshot/auto-export из main, preload IPC `processingHistory.get/clear`; `processing-history.test` покрывает порядок, фильтры и очистку. Попутно `diagnosticsCleanMaintenance` снова принимает `ffmpegTemp`.
- [J-521] 2026-05-12 23:21:00 [Assistant]: §13 — история обработок выведена в правую FFmpeg-панель: фильтры kind/outcome/query, refresh/clear, открытие файла/папки/превью через `processingHistory.openOutput`; раскрытие сохраняется в `mainWindowUiPanels`. `processing-history.test` добавил lookup по id.
- [J-522] 2026-05-12 23:29:00 [Assistant]: §13 — добавлена недельная сводка истории обработок (`processingHistory.weeklySummary`: total/success/error/cancelled/kind/duration) и JSON-экспорт видимых записей из UI через `saveTextWithDialog`; тест покрывает 7-day summary.
- [J-523] 2026-05-12 23:36:00 [Assistant]: §13 — «Повторить обработку» частично закрыто: в истории обработок кнопка «Повторить» открывает исходный `inputPath` в редакторе через новый IPC `processingHistory.openInputInHandler` и общий безопасный preview handler.
- [J-524] 2026-05-12 23:41:00 [Assistant]: §13 — встроенная React-история загрузок получила кнопку «Повторить»: URL из записи повторно добавляется в очередь через `downloads.addLines`, parity с pop-out history.
- [J-525] 2026-05-12 23:43:00 [Assistant]: §13 — встроенная история загрузок получила фильтр по outcome (all/success/error/cancelled) и JSON-экспорт видимых записей через общий `saveTextWithDialog`.
- [J-526] 2026-05-12 23:45:00 [Assistant]: §13 — «Повторить» в истории обработок теперь доступно и для записей без `outputPath`; встроенная история загрузок показывает chips-сводку за 7 дней по исходам.
- [J-527] 2026-05-12 23:49:00 [Assistant]: §13 — недельная сводка загрузок перенесена в main `ytdlp-download-history` (`getHistoryWeeklySummary`) и отдана через preload; тест покрывает 7-day outcome summary.
- [J-528] 2026-05-12 23:58:00 [Assistant]: §1.1 — `ui-text` RU/EN для встроенных панелей истории загрузок и истории обработок; форматтеры `formatDownloadsHistoryTime`, `formatProcessingDurationLabel`, outcome/kind labels в `locales/ui-text.ts`; `App.tsx` — локализованные title/hint save dialog и status для repeat/open handler/processing history.
- [J-529] 2026-05-13 00:05:00 [Assistant]: §1.1 — `DownloadsLogPanel`: заголовок, пустое состояние, «Сохранить», «Очистить» через `ui-text` (EN: live log / save / empty hint; clear — общий ключ с историей загрузок).
- [J-530] 2026-05-13 00:18:00 [Assistant]: §1.1 — вкладки workspace + быстрая полоса yt-dlp + основной UI вкладки «Загрузки» (hint, кнопки, сводка, фильтры, таблица, aria для действий строк) и `uiTextVars` для `{id}`/`{n}`; чипы фильтра очереди через `useMemo` + `uiText`.
- [J-531] 2026-05-13 00:35:00 [Assistant]: §1.1 — правый rail «Загрузки» (format/metadata/saving/network/expert, footer, loading): все подписи, hints, placeholders, aria, doc-links и кнопки через `ui-text` RU/EN.
- [J-532] 2026-05-13 00:50:00 [Assistant]: §1.1 — статусбар движков (`engineSummaryText`), топбар редактора (tooltips/hidden labels), пустое превью + aria dropzone/`<video>`, хром панели FFmpeg и секция «Видео» до bitrate — через `ui-text` + `uiTextVars` для имени файла в aria.
- [J-533] 2026-05-13 01:05:00 [Assistant]: §1.1 — FFmpeg rail: фильтры видео (деинтерлейс→blur), секция «Формат» (масштаб/FPS/2-pass/поворот/обрезка), «Аудио и кадр» до кнопок последнего снимка — RU/EN в `ui-text`.
- [J-534] 2026-05-13 01:20:00 [Assistant]: §1.1 — FFmpeg rail: секции «Пресеты»/«Вывод», подсказки `exportPreviewHint`, превью argv (Pass 1/2, aria/title копирования), кнопки последнего экспорта и «В превью» через `ui-text`/`uiTextVars`; `App.tsx` подключён к уже добавленным ключам. `npm run check:quiet` ок (**618 tests**).
- [J-535] 2026-05-13 02:00:00 [Assistant]: §1.1 — `PillSwitch` Вкл/Выкл; все подписи `<select>` пресетов экспорта (кодек, контейнер, фильтры, субтитры, снимок) через `ui-text` + `ffmpegExportSelectOptions` (`useMemo` в `App.tsx`); модалки имени пресета и путей движков; отмена сохранения лога yt-dlp — стабильный код `DOWNLOADS_VISIBLE_LOG_SAVE_CANCELLED` в `downloads-log-contract` + main/renderer. `npm run check:quiet` ок (**618 tests**).
- [J-536] 2026-05-13 02:35:00 [Assistant]: §1.1 — статусбар `App.tsx`: терминал (CLI exit / copy line), превью `<video>` (MEDIA_ERR_* + blob-fallback), быстрый ввод URL в очередь, прогресс экспорта (`{tail}`), загрузка/очистка движков, пути движков, снимок/экспорт/отмена/копирование путей и ffmpeg-команды, DnD grant — RU/EN ключи `status*` в `ui-text.ts`; хелпер `previewVideoMediaErrorDetailLabel`. `npm run check:quiet` ок (**618 tests**).
- [J-537] 2026-05-13 03:10:00 [Assistant]: §1.1/§6 — единый контракт `shared/ytdlp-queue-status.ts` (строки статуса очереди + предикаты + разбор паузы повтора); main (`downloads-queue`, runner, persist-parse, history, `ytdlp-progress-parser`, `downloads-window` IPC + встроенный JS) переведён на константы; встроенное окно загрузок — `QS_*` из `JSON.stringify`; renderer: фильтры/тон строки и кнопки retry через `isYtdlpQueueStatus*` + `formatDownloadsQueueRowStatus` в `ui-text`. `npm run check:quiet` ок (**618 tests**).
- [J-538] 2026-05-13 12:00:00 [Assistant]: §1.1 — отдельное окно инспектора: подпись движка в топбаре через `inspectorStandaloneTopbarEngineLabel` в `ui-text` RU/EN.
- [J-539] 2026-05-13 13:10:00 [Assistant]: §1.1/§21 — в индекс Git добавлены ранее неотслеживаемые `shared/*-locale.ts`/`ytdlp-command-hint-token-categories.ts` и сопутствующие правки main/preload/renderer/tests/Data (единый слой строк для окна загрузок, IPC, логов раннера, валидации CLI, payload/progress).
- [J-540] 2026-05-13 13:25:00 [Assistant]: §8 — уточнены RU-компактные `token` для части сценарных подсказок yt-dlp в `terminal-contract.ts` (без изменения `fullLine`).
- [J-541] 2026-05-13 14:05:00 [Assistant]: §8 — продолжение RU-компактных `token` для сценариев yt-dlp (плейлист/загрузчики/HLS/DASH/-S/ANSI/постпроцессор и др.; `fullLine` без изменений).
- [J-542] 2026-05-13 14:12:00 [Assistant]: §8 — `terminal-contract.ts`: перенос длинной строки `summary` под `--clean-infojson` под prettier (без смысловых правок).
- [J-543] 2026-05-13 14:18:00 [Assistant]: §8 — тот же блок `--clean-infojson`: многострочный `summary:` (устранён prettier warning в quiet lint).
- [J-544] 2026-05-13 15:00:00 [Assistant]: §8 — RU-компактные `token` для сценариев yt-dlp: `--extractor-retries`, `--http-chunk-size`, `--playlist-reverse`, `--update-to stable` (`fullLine` без изменений).
- [J-545] 2026-05-13 16:00:00 [Assistant]: §8 — RU `token`: yt-dlp `--extractor-retries`/`-playlist-reverse` (доп. к J-544); ffprobe превью — compact/disposition/nb_streams/профиль/count_frames; плюс правки `generic`/program_version/loudnorm из рабочей копии.
- [J-546] 2026-05-13 17:00:00 [Assistant]: §8 — ffprobe сценарии превью: RU-компактные `token` для disposition/time_base/brands/программ/глав/потоков a:2–a:3/s:2–s:3/format_* /packets|frames; `fullLine` без изменений.
- [J-547] 2026-05-13 18:00:00 [Assistant]: §8 — превью: RU/`ffmpeg:` для пакетов ffprobe, smoke-ffmpeg (scale/acopy/map/volumedetect/…), блок фильтров `-af`/`-vf` (секунды → «с»), disposition доп. потоков; мелкие правки токенов PCM/size/comment из рабочей копии; `fullLine` без изменений.
- [J-548] 2026-05-13 19:00:00 [Assistant]: §8 — превью ffprobe: уточнены токены v:0 side_data/s:0 PTS/v:2 summary/d:1–2/a:4/s:4/v:1; RU для format→JSON и тегов album_artist/WMF/encoding_tool; `fullLine` без изменений.
- [J-549] 2026-05-13 20:00:00 [Assistant]: §8 — начало набора `TERMINAL_SCENARIO_HINTS_DOWNLOADS`: RU-компактные `token` для `--print`/`--write*`/`--list*`/`--simulate`/плейлист/geo/версии и др. (первые ~50 записей); `fullLine` без изменений.
- [J-550] 2026-05-13 21:00:00 [Assistant]: §8 — `TERMINAL_SCENARIO_HINTS_DOWNLOADS`: доведены RU-компактные `token` для оставшихся `--print`/`--write*`/TLS/кэш/info.json и метаданных; `fullLine` без изменений.
- [J-551] 2026-05-13 22:00:00 [Assistant]: §8 — `terminal-contract.ts`: RU-компактные `token` (yt-dlp: сухой verbose, UA/TLS/generic/throttling, клиенты YouTube; ffprobe: теги/вывод pretty|flat); `fullLine` без изменений.
- [J-552] 2026-05-13 23:00:00 [Assistant]: §8 — `TERMINAL_SCENARIO_HINTS_PREVIEW_MEDIA` + yt-dlp: RU `token` для метатегов контейнера/дорожек, заголовков HTTP, HLS/xattr; `fullLine` без изменений.
- [J-553] 2026-05-14 00:00:00 [Assistant]: §8 — `terminal-contract.ts`: RU `token` для ffprobe таймбаз/extradata/тегов/MKV, loudnorm, yt-dlp xfwd/прокси/самообновление; `fullLine` без изменений.
- [J-554] 2026-05-14 01:00:00 [Assistant]: §8 — `TERMINAL_SCENARIO_HINTS_PREVIEW_MEDIA`/yt-dlp: RU `token` (метатеги genre/date, ffmpeg дымовые фильтры/afade/scale/map, sponsor); `fullLine` без изменений.
- [J-555] 2026-05-14 02:00:00 [Assistant]: §8 — оставшиеся `token` дымовых `-af` в превью-ffmpeg: RU-компактные подписи вместо имён фильтров; `fullLine` без изменений.
- [J-556] 2026-05-14 03:00:00 [Assistant]: §8 — yt-dlp `--print-to-file`/`в файл:` (пакет RU-токенов) + ffprobe JSON/flat/главы + genpts; `fullLine` без изменений.
- [J-557] 2026-05-14 04:00:00 [Assistant]: §8 — yt-dlp `в файл:`: оставшиеся метатеги/музыка/главы/форматы/URL — RU `token`; `fullLine` без изменений.
- [J-558] 2026-05-14 05:00:00 [Assistant]: §8 — yt-dlp: уточнены `token` для archive/ANSI/UA/HLS/ffmpeg; единый регистр `ffmpeg` в подсказках загрузчика; `fullLine` без изменений.
- [J-559] 2026-05-14 06:00:00 [Assistant]: §8 — `terminal-contract.ts`: доведены RU `token` (UA curl, HLS+ffmpeg, путь к бинарнику, print-to-file без basename, токены загрузчика/постпроц./превью — lowercase ffmpeg); `fullLine` без изменений.
- [J-560] 2026-05-14 07:00:00 [Assistant]: §8 — yt-dlp `token`: .part/чанк/SponsorBlock/xattr/TS-HLS/info.json-строки/HTTP-повторы/BiDi/трассировка, referer, «чистый .info.json»; `fullLine` без изменений.
- [J-561] 2026-05-14 08:00:00 [Assistant]: §8 — `terminal-contract.ts`: снова UA/HLS/путь/загрузчик/postproc → `ffmpeg`; блок превью: все `token` `FFmpeg:` → `ffmpeg:`; TS вместо MPEG-TS; «повторы запроса»; «без комментариев в JSON»; `fullLine` без изменений.
- [J-562] 2026-05-14 09:00:00 [Assistant]: §8 — `token`: yt-dlp (сетевой фрагмент, referer в файл, file-URL, PEM); ffprobe (JSON/PTS→«метки», mkv, pretty/flat, TS-программы, FourCC/PCM→RU); `fullLine` без изменений.
- [J-563] 2026-05-14 10:00:00 [Assistant]: §8 — повторно: UA/HLS/путь/загрузчик/postproc, `HTTP-фрагмент`→сетевой; все превью-токены `· FFmpeg:`→`· ffmpeg:` (replace); Accept-Language в токене; `fullLine` без изменений.
- [J-564] 2026-05-14 11:00:00 [Assistant]: §8 — `token`: сетевой фрагмент 1M, Accept-Language, DRM→«защита контента», unix-формулировки для epoch/timestamps; случайную массовую правку `summary` откатили; `fullLine` без изменений.
- [J-565] 2026-05-14 12:00:00 [Assistant]: §8 — `token`: нижний регистр контейнеров/кодеков (mkv/mp4/webm/jpg/png/srt/aac/mp3…), прямая (-g) аудио, шаблон -o, HTML→«страницы в HTML», netrc/xfwd/mp4-бренды; `summary` не трогали; `fullLine` без изменений.
- [J-566] 2026-05-14 13:00:00 [Assistant]: §8 — `token`: DRM→защита контента, сетевой фрагмент 1M, Accept-Language, json/ffprobe-формулировки, generic-экстрактор, сырая HTML; `fullLine` без изменений.
- [J-567] 2026-05-14 14:00:00 [Assistant]: §8 — `token`: aria2→aria2, pem в скобках, avc/av1 в нижнем регистре, TV Everywhere с «—», HLS встроенный, принудительно IPv4/IPv6; `fullLine` без изменений.
- [J-568] 2026-05-14 15:00:00 [Assistant]: §8 — `token`: mpd/ebu r128/tls нижний регистр, BiDi из токена, кросс-пан стерео, сводка версии ffprobe; `fullLine` без изменений.
- [J-569] 2026-05-14 16:00:00 [Assistant]: §8 — `token`: youtube/hls/ts/html в нижнем регистре, cookie-браузеры, referer youtube, трассировка tls, ipv4/ipv6, api sponsorblock; `fullLine` без изменений.
- [J-570] 2026-05-14 17:00:00 [Assistant]: §8 — `token`: tls-браузеры, sponsorblock, windows/win, accept-language, tv everywhere, mib; нижний регистр ISO в «гео»; `fullLine` без изменений.
- [J-571] 2026-05-14 18:00:00 [Assistant]: §8 — `token`: ua/utf-8/yt/ansi/wma/ascii, sar/dar/isrc/ts, wmf sdk/bpm, суффиксы k/m; `fullLine` без изменений.
- [J-572] 2026-05-14 19:00:00 [Assistant]: §8 — `token`: ip, вч/нч в ffmpeg/ffprobe, икм, «название по»; `fullLine` без изменений.
- [J-573] 2026-05-14 20:05:00 [Assistant]: §8 — RU в `summary` для `terminal-contract.ts`: `scripts/apply-terminal-summary-ru.mjs` (таблица замен, URL→«ссылка») + `scripts/inject-flux-summary-pole.mjs` (глосс `(поле …)` для строк с `flux-ytdlp`); второй проход в apply после `pairs`; npm `locales:terminal-summaries-ru` и `locales:terminal-flux-pole`; в шапках скриптов — напоминание прогонять apply дважды после мерджа, пока не будет 0/0; `fullLine` не меняли. Локально: `terminal-contract-scenarios` (122), `terminal-inline-suggest` + `terminal-service` (27).
- [J-574] 2026-05-14 20:10:00 [Assistant]: §8 — `token`: дорожки данных d:*, extradata→доп. двоичник, archive/webpage_scheme/симуляция/mux; `fullLine` без изменений.
- [J-575] 2026-05-14 20:15:00 [Assistant]: `IMPLEMENTATION_CHECKLIST.md` — §8: зафиксированы npm `locales:terminal-summaries-ru` / `locales:terminal-flux-pole`, отсылка к **J-573**; спринт §8 дополнен; снимок `npm run test` — **48 files / 583 tests**.
- [J-576] 2026-05-14 20:20:00 [Assistant]: §8 — повторный прогон `locales:terminal-summaries-ru` (устранён откат `допишите URL` в `terminal-contract.ts`); в `terminal-contract-scenarios.test` добавлен регрессионный кейс: нет «допишите URL», при `flux-ytdlp` в `summary` есть `(поле `; чеклист: снимок **584 tests**.
- [J-577] 2026-05-14 20:25:00 [Assistant]: §15/§8 — в `Help/ffmpeg-terminal-hints.md` и `Help/en/ffmpeg-terminal-hints.md` добавлен раздел про встроенные сценарии (`terminal-contract.ts`), npm `locales:terminal-summaries-ru` / `locales:terminal-flux-pole` и регрессионный тест.
- [J-578] 2026-05-14 20:26:00 [Assistant]: §8 — `token`: intra→ключевые кадры, гео to (Тонга), json без «вывод»; `fullLine` без изменений.
- [J-579] 2026-05-14 20:30:00 [Assistant]: §15 — `Help/tools-terminal-inspector.md` и `Help/en/tools-terminal-inspector.md`: в блоке «Терминал» расширена отсылка к `ffmpeg-terminal-hints.md` (JSON из `Data/*` + раздел встроенных сценариев / `terminal-contract.ts`).
- [J-580] 2026-05-14 20:35:00 [Assistant]: `IMPLEMENTATION_CHECKLIST.md` — §15: в пункте про deep-link из «Терминала» зафиксированы перекрёстные отсылки `tools-terminal-inspector` → встроенные сценарии и npm `locales:terminal-summaries-ru`.
- [J-581] 2026-05-14 20:40:00 [Assistant]: §15 — `Help/en/ffmpeg-terminal-hints.md`: уточнена EN-формулировка раздела Built-in scenarios (без смешения «ссылка»/URL в одной цитате; явно **допишите ссылку** vs **допишите URL**).
- [J-582] 2026-05-14 20:41:00 [Assistant]: §8 — `token`: гео ISO us/in/no/at/be/is/my/id/so/do/re/ad — страна в скобках; `fullLine` без изменений.
- [J-583] 2026-05-14 20:45:00 [Assistant]: §15 — `Help/ffmpeg-terminal-hints.md`: в разделе «Встроенные сценарии» та же ясность, что и в EN — явно **допишите ссылку** vs **допишите URL** и порядок npm-скриптов.
- [J-584] 2026-05-14 20:50:00 [Assistant]: `README.md` — в «Полезное» добавлена строка про правки `summary` в `terminal-contract.ts` и `npm run locales:terminal-summaries-ru` со ссылкой на `Help/ffmpeg-terminal-hints.md`.
- [J-585] 2026-05-14 20:51:00 [Assistant]: §8 — `token`: гео tr/ch/cr/pa/mc/li/va/ne + гео-обход de/fr/es/pl/se — страна в скобках; `fullLine` без изменений.
- [J-586] 2026-05-14 21:00:00 [Assistant]: §8 — `token`: гео mx…gl (≈57 ISO) — страна/территория в скобках; `fullLine` без изменений.
- [J-587] 2026-05-14 21:05:00 [Assistant]: `scripts/check-journal-numbering.mjs` — явная ошибка при дубликате `[J-NNN]` (строка и номер первого вхождения); `docs/ARCHITECTURE.md` — пункт про `terminal-contract.ts` и `npm run locales:terminal-summaries-ru` + ссылка на `Help/ffmpeg-terminal-hints.md`.
- [J-588] 2026-05-14 21:10:00 [Assistant]: `IMPLEMENTATION_CHECKLIST.md` — §0: в пункте про журнал добавлены `check:journal` и явное упоминание дубликатов `J-NNN`.
- [J-589] 2026-05-14 21:12:00 [Assistant]: §8 — `token`: оставшиеся гео ISO (Карибы, океания, Африка, Ближний Восток и др.) — подсказка в скобках; `fullLine` без изменений.
- [J-590] 2026-05-14 21:15:00 [Assistant]: §8 — `token`: гео-обход gb/jp/ca/au/br/it/nl — страна в скобках; `fullLine` без изменений.
- [J-591] 2026-05-14 21:25:00 [Assistant]: §8 — `TERMINAL_SCENARIO_HINTS_PREVIEW_MEDIA`: +22 сценария (ffprobe a:5–a:6, v:3, s:5–s:6, d:3, теги disc/track/lyricist/composer/encoded_by+muxing_app, side_data v:0/a:0; ffmpeg rubberband/firequalizer/acrossover/tilt/asubboost/aemphasis/apad/haas_effect/pan mono→stereo/silenceremove/map pcm/scale lanczos/fps telecine); дубликат `fullLine` probe_score заменён на `nb_chapters`. `npm run check:quiet` ок (**584 tests**).
- [J-592] 2026-05-14 21:40:00 [Assistant]: §8 — `TERMINAL_SCENARIO_HINTS_PREVIEW_MEDIA`: +30 сценариев (ffprobe: color_transfer, encoder+handler_name, первая глава с read_intervals, s:3/a:3 disposition, v:4/d:4/t:1, v:1 side_data_list, artist+sort_artist, private v:0, s:2 start_pts, a:2 index+codec_tag; ffmpeg: hue→серый, unsharp, hqdn3d, transpose, crop, setsar, fps, scale iw/2, pan→mono, aformat 48k, tblend, mpdecimate, silenceremove RMS, yadif, eq, -map 0:v:0). `npm run check:quiet` ок.
- [J-593] 2026-05-14 21:55:00 [Assistant]: §8 — `TERMINAL_SCENARIO_HINTS_DOWNLOADS`: +8 (обложка PNG, print id с no-cache-dir/no-warnings, -q title, user-agent curl, referer example.org, Accept-Language, flat-playlist print entries); `TERMINAL_SCENARIO_HINTS_PREVIEW_MEDIA`: +24 (ffprobe color_primaries/space, v:0 duration_ts, pix_fmt+profile, два пакета v:0, format time_base, a:0 max_bit_rate, s:0 codec_long_name; ffmpeg map a:0, boxblur, gblur, edgedetect, vignette, colorbalance, negate, hue, gradfun, dctdnoiz, weave, fieldorder, tpad, sab, shuffleplanes, extractplanes y, swapuv). `npm run check:quiet` ок.
- [J-594] 2026-05-14 22:10:00 [Assistant]: §8 — `TERMINAL_SCENARIO_HINTS_DOWNLOADS`: +6 (flat-playlist print urls другой порядок argv, no-check-certificates, prefer-free-formats, merge-output-format mkv, retries=2, socket-timeout 25); `TERMINAL_SCENARIO_HINTS_PREVIEW_MEDIA`: +18 (ffprobe v:0 start_pts, a:0 index, format_tags comment/replaygain_track_gain, v:0 color_range, s:1 codec_long_name; ffmpeg hflip/vflip/transpose=0, deshake, median, decimate, pullup, fps=film=24, tmix, curves lighter, colorchannelmixer, rotate 2°, yadif send_frame, fps=60). `npm run check:quiet` ок.
- [J-595] 2026-05-14 22:25:00 [Assistant]: §8 — `TERMINAL_SCENARIO_HINTS_DOWNLOADS`: +6 (limit-rate 500K+print title, extractor/fragment-retries, write-info-json+print filename, playlist-items 1 title, no-playlist+channel_url); `TERMINAL_SCENARIO_HINTS_PREVIEW_MEDIA`: +20 (ffprobe v:0 field_order, a:2 handler_name, format_tags sort_album, format name+flags, s:2 start_time, a:3 index+codec; ffmpeg nlmeans, atadenoise, removegrain, pp al, dedot, owdenoise, estdif, w3fdif, kerndeint, transpose=3, scale neighbor, format yuv420p, stereo3d anaglyph, palettegen+paletteuse). `npm run check:quiet` ок.
- [J-596] 2026-05-14 22:40:00 [Assistant]: §8 — `TERMINAL_SCENARIO_HINTS_DOWNLOADS`: +6 (concurrent-fragments 3, sleep-requests 0.5, geo-bypass-country DE, impersonate chrome, write-pages+title, -4 ipv4); `TERMINAL_SCENARIO_HINTS_PREVIEW_MEDIA`: +16 (ffprobe s:3/d:5/a:7, pict_type 10 кадров, format_tags encoder+major_brand; ffmpeg histeq, deflicker, lagfun, noise, shufflepixels, lenscorrection, fps=24000/1001, tinterlace+fieldorder, pseudocolor rainbow, colorhold). `npm run check:quiet` ок.
- [J-597] 2026-05-14 22:55:00 [Assistant]: §8 — `TERMINAL_SCENARIO_HINTS_DOWNLOADS`: +6 (impersonate edge/firefox, -6 ipv6, no-mtime+print filename, windows-filenames, xattrs); `TERMINAL_SCENARIO_HINTS_PREVIEW_MEDIA`: +14 (ffprobe s:4 start_pts, v:5, d:6, главы id+start, a:0 sample_aspect_ratio; ffmpeg bwdif, il, colormatrix 601→709, smartblur, chromakey, yadif 1:-1:0, phase, lumakey, limiter, colorbalance rgb). `npm run check:quiet` ок.
- [J-598] 2026-05-14 23:10:00 [Assistant]: §8 — правки `TERMINAL_SCENARIO_HINTS_PREVIEW_MEDIA`: scale bicubic+full_chroma_inp с `w=iw:h=ih`, shuffleplanes `2:1:0`, вместо signalstats+metadata — avgblur; `npm run check:quiet` ок.
- [J-599] 2026-05-14 23:25:00 [Assistant]: §8 — `TERMINAL_SCENARIO_HINTS_DOWNLOADS`: +10 (max-downloads 7, sleep-requests 0.7, concurrent-fragments 5, fragment-retries 6, socket-timeout 45, retries 4, limit-rate 750K, file-access-retries 3, geo-bypass FR/JP + print title); `TERMINAL_SCENARIO_HINTS_PREVIEW_MEDIA`: +20 (ffprobe: album, musicbrainz_trackid, v:6, d:7, t:2, s:7, chapter_tags title, pkt_duration_time 3 кадра; ffmpeg: fftdnoiz, bitplanenoise, extractplanes u/v, setpts, drawbox, format yuv444p, hue h/s, transpose=1, volume, bass, highpass 140, atempo 1.08). `npm run check:quiet` ок.
- [J-600] 2026-05-14 23:40:00 [Assistant]: §8 — `TERMINAL_SCENARIO_HINTS_DOWNLOADS`: +12 (max-downloads 11, sleep-requests 1.1, concurrent-fragments 8, fragment-retries 9, socket-timeout 90, retries 7, limit-rate 1M+print, extractor-retries 4, file-access-retries 6, geo-bypass US/GB/IT + print); `TERMINAL_SCENARIO_HINTS_PREVIEW_MEDIA`: +20 (ffprobe: mood, v:7, d:8, t:3, s:8, chapter start_time, pkt_pos 2 кадра, format start_time_real+duration, a:1 side_data_list, a:8; ffmpeg: blackframe, scdet, vaguedenoiser, chromashift, scale decrease, tblend multiply, firequalizer -4, stereotools, alimiter 0.9, geq). `npm run check:quiet` ок.
